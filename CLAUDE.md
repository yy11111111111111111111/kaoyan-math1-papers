# 真题分析仓库

本仓库存放考研数学（一）真题题面（Markdown）。题面在 `papers/`，另有 `solutions/` 存放 PDF 转换而来的官方解析稿。

**解析稿仅供用户人工核对使用。** 除非用户在本文件 §7 中显式开启，Claude Code 在做考点判断与解法推断时**不得读取 `solutions/`**——判断依据只能来自题面本身（见 §3）。标准答案以纸质版为准，由用户人工核对。

本仓库与考研 Vault 是**两个独立项目**，Vault 的写入规范、pending 生命周期、Error Log 约束**在此完全不适用**。本文件是本仓库唯一的规则来源。

---

## 0. 这个仓库是什么

**一句话**：考研数学一真题库，外加一层从真题建起来的考法分析与方法族材料。

内容分四层，用途与可信度都不同，**不要混着读**：

| 目录 | 是什么 | 谁写的 | 怎么对待 |
|---|---|---|---|
| `papers/` | 真题题面（1987–2026，缺 1994） | PDF→OCR 转换 | 已知 15 题 OCR 损伤、3 题残缺、2022 整年乱码。是转换产物，不是权威文本 |
| `solutions/` | 第三方解析稿 | PDF 转换 | **仅供人工核对**；推断阶段禁止读取（见 §7） |
| `分析/` | 统计派生物 + 人工建立的方法族与认知模型 | 见 §4 与 `分析/10_...索引.md` | **不是同一类东西**，覆盖规则分开，见 §4 与 §4.6 |
| `原创题组/` | 原创复习题包 | 出题者 | 各包内自带验收状态，未完成独立验收的不得当已验收使用 |

**接手本仓库的分析工作前，先读 `分析/10_高等数学_资料与覆盖索引.md`。**
它不含内容，只说明数据血缘、每份材料的主张类型与不确定性、当前任务该查哪几份、
以及哪些话不许升级成预测。本文件（`CLAUDE.md`）管规则，那份管路由。

多 agent 协作的状态总线是 `分析/协作/看板.md`；审查报告落 `分析/审查/`。

---

## 1. 职责

在本仓库中，Claude Code 负责：

- 解析题面，提取机械字段
- **判断每道题的考点归属**（本仓库允许知识判断）
- 题型分类
- 同题/变体识别
- 频次与分布统计
- 从题面推断解题路径（标为待核对）

§4 列出的分析产物**可重算**：重跑即整体覆盖，不追加、不保留历史版本。

⚠️ **「可重算」不覆盖整个 `分析/`。** 该目录下还有人工建立的方法族、认知模型、审查与协作记录，它们不是 `/analyze` 的产物，重跑不得触碰——清单见 §4.6。

---

## 2. 输入格式

题面文件位于 `papers/` 下，一年一文件。命名不统一，实际存在三种：

- `<年份>年考研数学(一)真题.md`（1987–2023 主流）
- `<年份>年数学一真题.md` / `<年份>年考研数学一真题.md`（2025、2026）
- `<年份>考研数学一真题[+答案].md`、`<年份>年数学(一)真题及参考答案.md`（2024，三份并存）

年份一律从文件名中的四位数字提取。

`papers/数学二/`、`papers/images/`、`papers/README.md` **不是题面**，解析时跳过。

### 2.1 卷内结构

题面由 PDF OCR 转换而来，结构随年代变化，分三个时期：

**A. 分节式（1987–2003）**——章节标题为 `# 一、`…`# 十五、`，其中：

- 标题含「填空题」「选择题」的节，节内以 `（1）`…`（n）` 分小题，每个小题算一道题
- 其余节为大题，整节算一道题；节内的 `(1)(2)` 是同一道题的小问，不拆

**B. 三段式（2004–2023）**——`# 一、` `# 二、` `# 三、` 三节，分别为填空/选择/解答（2004–2006 顺序为填空→选择→解答，2007 起为选择→填空→解答），题号在全卷内连续编号（1…23 或 1…24）。

**C. 新三段式（2024–2026）**——同为三段，题号 1–22（选择 10 + 填空 6 + 解答 6）。解答题内的 `(1)(2)` 为小问，不拆为独立题目。

### 2.2 题号

统一记作 `<年份>-<卷内题号>`，如 `2023-15`。

分节式（1987–2003）无全卷连续题号，记作 `<年份>-<节号><小题号>`，如 `1987-一1`、`1987-六`（整节大题无小题号）。

### 2.3 分值

分值不在每题上标注，只在节标题里（如「每小题 4 分」「本题满分 8 分」）。逐题分值由节标题推得；节标题未给出的，分值留空，不猜。

### 2.4 答案

`papers/` 下 2024、2025、2026 三个年份的文件**混有 `【答案】`/`【解析】` 段落**。这些段落是题面文件的一部分，但：

- 提取题干时须剔除
- **不得**据此填写 `解法.md`，也不得据此把 `status` 改为 `已核对`

---

## 3. 考点粒度

考点标签必须落在**可检索的具体结论或方法**这一层。

- 过粗（禁止）：`高等数学`、`线性代数`、`概率论`
- 过细（禁止）：一题一标签，等于没有归类
- 正确：`斜渐近线求法`、`格林公式化曲线积分为二重积分`、`实对称矩阵正交相似对角化`、`全概率公式`、`二维正态的条件分布`

一题可挂多个考点，主考点排第一。

判断依据只能来自题面本身。题面信息不足以确定考点时，标 `?` 并在 `待确认.md` 中列出，**不猜**。

---

## 4. 输出文件

`/analyze` 的产物写入 `分析/`，**且仅限本节 §4.0–§4.5 列出的这 8 个文件**：

`预检报告.md` · `考点标注.tsv` · `考点标签全集.md` · `考点频次.md` ·
`题型分布.md` · `同题识别.md` · `解法.md` · `待确认.md`

这 8 个之外，`分析/` 下的任何文件都**不是** `/analyze` 的产物，重跑一律不得删除或覆盖（见 §4.6）。
即使它们与本节产物同处一个目录、即使它们看起来像分析结果。

### 4.0 `预检报告.md`

`/analyze` 步骤 1 的产物：文件清单、逐年题数、题号连续性与格式异常。异常只报告，不修补。

### 4.0.1 `考点标注.tsv` 与 `考点标签全集.md`

步骤 3 的产物。`考点标注.tsv` 是逐题标注的原始数据（列：年份、题号、题型、主考点、次考点…），4.1–4.4 各文件均由它派生；`考点标签全集.md` 是去重后的标签清单，供用户确认粒度。两者随重跑整体覆盖。

⚠️ **重建 TSV 会静默作废上层声明。** `考点标注.tsv` 是全库唯一的人工标注层，
`分析/` 下 20 余份文件在 frontmatter 里写着 `source_tsv_sha256`，声明自己基于哪一版 TSV。
TSV 一旦重建，这些声明全部过期，**而没有任何机制会自动报错**。
因此重建 TSV 前必须：① 明确告知会作废哪些文件的声明；② 重建后跑
`python3 分析/tests/check_source_sha.py` 找出全部失配项，逐份处理，不得放着不管。

### 4.1 `考点频次.md`

按出现次数降序：

| 考点 | 次数 | 年份 | 题号 |
|---|---|---|---|

同一年内重复计入。次数为 1 的单列一节，不与高频混排。

### 4.2 `题型分布.md`

题型 × 数量 × 该题型下的考点分布。题型取值需在本文件 §5 中固定后使用。

### 4.3 `同题识别.md`

实质相同、仅换数字或换包装的题，归为一组：

```
### 组 N：<共同考法一句话>
- 题号列表
- 差异：<具体差在哪>
```

判定标准：**解题路径相同**即为同组，不看题干措辞。判定不确定的归入本文件末尾「疑似」一节，不混入确定组。

### 4.4 `解法.md`

每题一节，写从题面推出的解题路径：切入点、关键步骤、易错处。

**每节顶部标 `status: 待核对`。** 用户对照纸质答案核对后手动改为 `已核对` 或写下差异。Claude Code **不得**自行改动该字段。

### 4.5 `待确认.md`

考点无法判定、题面疑似残缺、题号冲突等，逐条列出。

⚠️ **本文件是混合体，不能整体覆盖。** 机器可重新检出的部分（考点标 `?`、题号冲突、
分值无法判定）可随重跑更新；但以下内容是**人工输入**，重跑必须原样保留：

- 用户裁定（如 §一 剔除 3 题、§四 整年排除、§五 重复文件取舍，均注明裁定日期）
- 人工核对发现（§八 题面内 `【答案】` 有误）
- 方法族独立审查的发现（§九）

判断依据：**凡带日期与出处署名的条目，都是人工输入**。拿不准就保留，不删。

### 4.6 重跑不得触碰的文件

`分析/` 下共 96 个文件，§4.0–§4.5 的 8 个是 `/analyze` 产物，**其余 90 个不是**。
它们是人工建立的分析层与过程记录，`/analyze` 重跑**不得删除、不得覆盖、不得改写**：

| 类别 | 文件 | 为什么不能重算 |
|---|---|---|
| **路由入口** | `10_高等数学_资料与覆盖索引.md`、`00_本目录来源说明.md` | 说明血缘与合并口径，本身不是数据派生物 |
| **方法族**（9 份） | `方法族-高数-*.md` | 按解题路线人工建族，各自带 `status_summary` 与 `freeze_status`；走过「审查→修复→独立复核」链路，重算等于丢弃全部审查历史 |
| **认知模型** | `高数考法与认知模型.md`、`高数逐考点认知模型.md` | 索引 §1 要点 3 明确：这两份的「模型 / 触发信号 / 元模型」列是人写的判断，**TSV 里没有这些字段**，重算无法恢复 |
| **方法速查**（3 科） | `高数/线性代数/概率 方法速查.md` | 223 条操作步骤，人工编写 |
| **考法统计**（3 科） | `高数/线性代数/概率 考法统计.md` | 按科目重组的考法地图 |
| **统计与建模** | `命题模型.md`、`功能位模板.md`、`考试价值排序.md`、`强主张审计-2026-08-27.md` | 检验设计与主张审计，代码在 `tests/` |
| **题面汇编**（4 份） | `高数真题题面_*.md` | 由 `tests/build_problem_bank.py` 生成，不是 `/analyze` 步骤 |
| **教学与协作** | `教学协议-考法驱动.md`、`交接说明-教学AI.md`、`AGENT_COLLAB_PROMPT.md`、`METHOD_FAMILY_HANDOFF.md`、`GPT接入包.md`、`致GPT-协作说明.md`、`实况简报-高数与线代.md`、`2027预测台账.md`、`考纲.md` | 协议、交接与外部口径 |
| **过程记录** | `协作/`（12 份）、`审查/`（34 份） | **append-only**。审查报告是「这块是谁在哪个分支上审的」的唯一回溯凭据，删了就再也对不上 |
| **检验代码** | `tests/`（8 份） | `tests/README.md` 说明了它存在的理由：一次性脚本导致过一个复现不稳的 p 值在 10 份文件里传播一年 |

**判断规则**：不在 §4.0–§4.5 那 8 个文件名单里的，一律按受保护处理。
新增文件若属于 `/analyze` 产物，必须先在 §4 增列条目，再生成。

---

## 5. 固定取值

**科目**（严格取值，不新增）：`数学一`

**题型**（严格取值，不新增）：`选择` / `填空` / `解答`

题型由所在节的标题判定（「选择题」→`选择`，「填空题」→`填空`，其余节及「解答题」→`解答`）。节标题缺失或无法判定的进 `待确认.md`。

---

## 6. 禁止

- 不修改 `papers/`、`solutions/` 下任何**题面与解析文件**
  - 例外：`papers/README.md` 是索引文件、不含题面，用户已于 2026-08-23 授权维护。除此之外 `papers/`、`solutions/` 下一律不动
- 不编造题面中不存在的信息
- 不自行把 `解法.md` 的 `status` 改为已核对
- 不按「重要性」「难度」排序——排序只允许用频次、年份、题号
- 不在 `分析/` 之外写文件
  - 例外一：`papers/README.md`（索引文件，用户已于 2026-08-23 授权维护）
  - 例外二：`原创题组/`（原创复习题包。它不是真题分析产物，因此不进 `分析/`；
    各包自带验收状态，`/analyze` 与本文件 §4 的覆盖规则对它完全不适用）
  - 例外三：根目录 `README.md`、`CLAUDE.md`、`.gitignore` 等仓库门面与规则文件
- 未经 §7 开启，不读取 `solutions/` 用于考点判断或解法推断
- 不删除或覆盖 §4.6 列出的文件——包括「重跑时顺手清理」「看起来像旧版本」这类理由
- 不因 `分析/` 下已有同类文件就认为它可重算：先对照 §4 的 8 个文件名单

---

## 7. 待用户裁定的开关

以下选项影响分析范围与依据。用户已于 2026-08-15 裁定如下，改动本节即生效。

| 开关 | 取值 | 说明 |
|---|---|---|
| 允许参考 `solutions/` | **仅限核对**（2026-08-23 改） | **推断阶段仍然禁止**：考点判断与解法推断只依据题面，`解法.md` 的独立性不变。**核对阶段允许**：可读 `solutions/` 与已写好的 `解法.md` 比对，差异逐条列出并改正。⚠️ 改正后**不得**自行把 `status` 改为 `已核对`——`solutions/` 是 PDF 转换的第三方稿、已知会出错（见 `待确认.md` §八），只能另标「已对解析」；`已核对` 仍以用户的纸质版为准 |
| 分析年份范围 | **2004 年起** | 三段式定型之后的年份，题型结构与现行考纲一致。1987–2003 暂不纳入，可后续补跑 |
| 2022 题面来源 | **排除** | `papers/2022年考研数学(一)真题.md` 全文乱码，整年排除 |
| 1994 题面来源 | **排除** | `papers/` 无 1994 题面，整年排除（且不在 2004 起的范围内） |
| 2024 采用文件 | **`papers/2024考研数学一真题+答案.md`** | 另两份 2024 文件排除，不参与统计 |

**本轮生效范围：2004–2026，扣除 2022，共 22 个年份、502 题。**

### 7.1 `solutions/` 的清理裁定

§6 默认禁止改动 `solutions/`。以下是用户明确裁定后执行的例外，逐条记录：

| 日期 | 裁定 | 执行情况 |
|---|---|---|
| 2026-09-07 | 删除 `solutions/英语一/2023_markdown/` 与 `2024_markdown/` | **已执行**。删除前逐文件校验：两目录与同级 `2023/`、`2024/` 共 15 个文件全部 sha256 相同，是纯重复，无独有内容 |
| 2026-09-07 | `solutions/英语一/`（34 MB）迁往独立仓库，**迁出后再删，不是直接删** | **未执行**。当前会话的 GitHub 授权不含创建仓库权限（`create_repository` 返回 403）。目标仓库建成前不得从本仓库移除 |

`solutions/英语一/` 是考研英语材料，与本仓库的数学一主题不符；同类情况的政治材料已于
`c9bb5fd` 迁往 `kaoyan-politics` 仓库。英语材料按同样方式处理，但**目标仓库尚不存在**。
