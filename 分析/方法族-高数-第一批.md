---
doc_id: calc.method-families.batch1
doc_version: 3.2.1
scope: 高数前三个 method family；远端基线重新吸收外部 v3.2 artifact，并同步 GPT v3.2 full-file audit 的状态裁定
schema_version: CALC-METHOD-FAMILY-v1.3
source_tsv_sha256: cbdf1a55989a8f60
generated_at: 2026-08-27
author_upgrade_ceiling: candidate
supersedes: v3.1.0
reconstruction_provenance: >
  Codex 的后续工作**未推送远端**，不存在可从 Git 恢复的 v3.3 / schema v1.3.1 / 相关 commit。
  本版本由 Claude Code 基于三项输入重新落地：
  ① 远端真实基线 15fca6c（方法族为 v3.1.0 / schema v1.2）；
  ② 用户提供的完整外部 artifact v3.2.0（sha256 前缀 1d6960c0a6e07c16，1143 行）；
  ③ GPT 对完整 v3.2 的 full-file audit。
  **不继承任何未持久化的 Codex 产物**，也不伪造其历史。
status_summary:
  calc.limit.method-selection:          candidate
  calc.vector-integral.route-selection: challenged      # GPT v3.2 full-file audit
  calc.extrema.constraint-selection:    challenged      # GPT v3.2 full-file audit
---

# 方法族 · 高数第一批（v3.2）

## 0. schema v1.3 与审核 lineage

### 0.1 v1.3：输出对象、后续组合与 terminal 性质分离

```yaml
produces: transformed_problem | candidate_set | subproblems | result_object
followup_actions:
  mode: all_of | any_of | optional_any_of | sequence
  actions: [...]
terminal_when:
  - 当前 action 的直接输出在什么 task/context 下已经满足 response requirement
```

三个字段互不代替：

```text
transformed_problem 得到一个等价的、或足以继续求解的新问题/新表示
candidate_set      得到候选对象集合，还必须分类、比较或筛选
subproblems        原问题被拆成多个仍需分别完成的子问题
result_object      得到数值、函数、点集、界或定性结论等数学结果对象

followup_actions   先按 followup item 的 when（若有）与所引 action 的 applies_when 过滤，再由 mode 解释剩余项的逻辑关系
terminal_when      只判断当前输出在声明 task/context 下是否已经满足整题 response requirement
```

`result_object` **不等于**「整题已完成」。例如 A1 无约束 Hessian 路线可以直接产生一组已分类的内部候选，但被闭区域 framework 调用时仍不是全局最值答案。反过来，`transformed_problem` 也可能在设问只要求渐近表示时成为 terminal。terminal 性质只能由 `terminal_when` 按 task/context 判定，不能永久编码进 action 类型。

`followup_actions.mode` 的语义：

```text
all_of          当前上下文适用的动作都要完成，次序未声明
any_of          当前上下文适用的动作至少选择一个完成
optional_any_of 可以不继续；若继续，则从适用动作中选择至少一个
sequence        按 actions 的记录顺序完成当前上下文适用的动作
```

若这四档无法表达新的真实关系，必须登记 `pending_schema_declaration`，不得临场扩枚举。`actions` 可以直接引用 family action ID，也可以写成当前 framework 内定义明确的后续数学操作；需声明目标或条件时写成 `{ action, target?, when? }`。执行器先核对 item 的 `when`（若有）和所引 action 自身的 `applies_when`，过滤后再应用 `mode`。

`subroute_of` 继续只表示：额外条件收窄后，父路线的同一机制得到特殊实现，执行子路线时不存在一个独立的「调用父路线」步骤。跨 action 的实际调用一律写入 `followup_actions`，旧字段 `invokes` 与 `requires_followup` 自 v1.3 起废弃。

### 0.2 v3.1 full-file audit 的正式裁定

GPT 已对 `doc_version: 3.1.0` 做完整文件审核。正式状态为：

```yaml
calc.limit.method-selection: candidate
calc.vector-integral.route-selection: candidate
calc.extrema.constraint-selection: candidate
pedagogical_validation: { all_families: untested }
```

extrema 在 v3.1 中的 `partially_verified` 来自依据变更说明进行的 **conditional review**，不是完整文件验收；本轮按 full-file audit 降回 `candidate`。历史不覆盖，逐族写入 `status_history`。

### 0.3 既有 review 历史（保留）

| artifact | review_kind | 结果 |
|---|---|---|
| v1.0.0 | full-file review | 三族均保持 candidate；要求改 schema 与数学边界 |
| v2.0.0 / v3.0.0 | iterative review | 两级 router 等局部结构获认可，但三族仍需修订 |
| v3.1.0 | conditional review（依据变更说明） | extrema 临时升 partially_verified；明确保留 open items |
| v3.1.0 | full-file audit | 三族全部 candidate；要求先关闭 schema v1.3 与本文件列出的 P0，再申请升级 |

## 1. `calc.limit.method-selection`（v3.2）

```yaml
method_family_rule:
  schema_version: CALC-METHOD-FAMILY-v1.3
  family_id: calc.limit.method-selection
  name: 一元极限的方法选择
  status: candidate
  pedagogical_validation: { status: untested }
  status_history:
    - from: null
      to: candidate
      review_kind: author_creation
      artifact_version: 1.0.0
      reviewer: Claude Code
      reason: 首次按 method-family schema 建档，作者自主产出上限为 candidate
      open_items: [数学 guard、route scan 与 schema 均待独立审核]
    - from: candidate
      to: candidate
      review_kind: full-file_audit
      artifact_version: 3.1.0
      reviewer: GPT
      reason: 核心乘除/加减机制基本正确，但 schema 与若干 action 边界尚未关闭
      open_items: [CALC-METHOD-FAMILY-v1.3, A0/A2 terminal 语义, A5 scope, A7 拆分, A8 Riemann-sum guard]

  scope:
    objects:
      - 考研数学一常见初等函数极限
      - 考研数学一常见数列求值极限
    target_tasks: [求极限值, 由极限值反求参数, 定无穷小的阶]
    exclusions:
      - 极限存在性的抽象证明
      - 多元重极限
      - 级数收敛性判定
      - 单侧极限的分支讨论 / 含绝对值或取整的分段讨论（尚未纳入，见 open_gaps）
    scope_note: >
      v2 写的是「所有一元函数极限、数列极限」，过宽——更高级或特殊的极限方法
      会随时击穿这个 family。现按当前题库覆盖收窄。

  rule_kind: structural_rule

  pre_solution_features:
    - { feature_id: F1, description: "直接代入后是否为待定型；若是，是哪一种（0/0 · ∞/∞ · 1^∞ · 0·∞ · ∞−∞ · 0^0）", acquisition: 先代入，读出各部件趋向, role: decisive }
    - feature_id: F2
      description: 是否存在**决定性主阶相消**（替换误差与最终要辨别的尺度同阶）
      acquisition: >
        找到可能相消的**最低共同非零阶**，只展开到足以判断该阶是否被抵消为止。
        不要机械地「各部件展到一阶」——某些对象的一阶本来就是零。
      role: decisive
    - { feature_id: F3, description: 对象是数列（离散 n）还是函数（连续 x）, acquisition: 读自变量, role: decisive }
    - { feature_id: F4, description: 结构类型（含变限积分 / n 项和 / 递推式 / 幂指式）, acquisition: 扫描整体结构, role: decisive }

  candidate_actions:
    - action_id: A0
      description: 直接代入 / 连续性传递
      action_kind: solver
      produces: result_object
      terminal_when: [代入所得对象已满足当前设问，且定义域与连续性条件均已核对]
      role: primary
      applies_when: [极限可由连续性逐层传递，且代入后的所有运算有定义]
    - action_id: A1
      description: 等价无穷小替换
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [化简替换后的表达式并继续求当前目标] }
      terminal_when: [设问本身只要求当前渐近等价对象，且替换精度足以确定它]
      role: primary
      applies_when: [存在可识别的标准无穷小部件]
    - action_id: A2
      description: 泰勒展开定阶（带皮亚诺余项）
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [读取首个未相消项并完成极限值、阶数或参数条件] }
      terminal_when: [所得渐近表示已直接满足当前 response requirement]
      role: primary
      applies_when: [部件在极限点处具有所需阶数的展开且余项受控]
    - action_id: A3
      description: 洛必达法则
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [对求导后的新极限重新执行 F1–F4 路由] }
      terminal_when: [求导后的极限已经存在且其值直接满足当前设问]
      role: alternative
      applies_when: ["0/0 或 ∞/∞ 型", 分子分母满足洛必达法则的全部条件]
    - action_id: A4
      description: 夹逼准则
      action_kind: solver
      produces: result_object
      terminal_when: [上下界极限相同且该共同极限就是当前要求的对象]
      role: alternative
      applies_when: [能构造同极限的上下界]
    - action_id: A5
      description: 递推数列：证明单调有界 + 由递推式解极限方程
      action_kind: solver
      produces: result_object
      terminal_when: [已证明收敛、合法通过递推关系取极限，并由范围或唯一性选定正确不动点]
      role: primary
      applies_when: [F3 为数列, F4 含递推式]
    - action_id: A6
      description: 代数恒等变形（因式分解 / 有理化 / 通分）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions: { mode: any_of, actions: [A0, A1, A2] }
      terminal_when: [恒等变形后的表达式已经直接满足当前设问]
      role: primary
      applies_when: [存在可约因子或可有理化的根式差]
    - action_id: A7a
      description: 对正的幂指式取对数，将乘方极限化为指数中的乘积极限
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions: [求对数后的极限, 用指数连续性还原原极限]
      terminal_when: [当前设问只要求对数后的渐近表示]
      role: primary
      applies_when: [原式在去心邻域为正, 取对数后确实把幂指结构化为可处理的乘积]
    - action_id: A7b
      description: 变量代换，把趋近方式或尺度改写为已知标准极限
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions: [求换元后的极限, 核对换元覆盖的单侧、分支与定义域]
      terminal_when: [换元后的标准极限及趋近方向已经直接给出当前要求]
      role: primary
      applies_when: [存在保持目标趋近关系的合法换元]
    - action_id: A8
      description: 整理为定积分的 Riemann 和
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: optional_any_of, actions: [计算所得定积分] }
      terminal_when: [设问允许以该定积分表示答案，或该定积分已经算出]
      role: alternative
      applies_when: [可整理为固定区间上、网格直径趋零的 Riemann 和]

  selection_rule:
    guards:
      - condition: >-
          A1 在乘除上下文中的必要定义域条件：若 E = u·h、Ẽ = v·h，或
          E = u/h、Ẽ = v/h，则相关比值必须在某个去心邻域内有定义；特别是除法中的
          h 必须非零。若还要把 E 与 Ẽ 写成渐近等价，须能在相应定义域上写
          Ẽ/E = v/u → 1。这里**不需要** E 与 u 同阶。
        logical_role: necessary
        check: 先判乘法或除法，并检查相关因子在去心邻域内使比值有定义
        explanation: >
          乘除稳定性来自同一因子的约去，而不是 E 与 u 的量级关系。
      - condition: >-
          A1 在加减上下文中的一个**充分条件**：设 E = u+w、Ẽ = v+w。
          若目标是保证 Ẽ ~ E，则可要求 u-v = o(E)。u ~ v 只给 u-v = o(u)，
          在主阶相消使 E ≪ u 时并不足够。该条件不声称是保持数值极限的普遍必要条件。
        logical_role: sufficient
        check: 仅在加减上下文、且需要保持整体主阶等价时，比较替换误差 u-v 与 E 的阶
        explanation: >
          充分条件失败只表示当前等价替换的保证不足；不推出替换后的数值极限必然错误，
          也不推出所有更高精度替换都失败。
      - condition: "A3 要求当前确为 0/0 或 ∞/∞；分子分母在去心邻域可导；**分母导数不为零**；且求导后极限确实存在（或为 ∞）"
        logical_role: necessary
        check: 四条逐条验；求导后振荡则该次调用无结论
      - condition: A3 不能直接作用于数列（n 不可导），须先换成连续变量的函数极限
        logical_role: necessary
        check: 读 F3
      - condition: >-
          A5 只覆盖递推数列：先由单调有界得到收敛；再把极限通过递推关系时，
          递推函数须在候选极限处连续；若不动点不唯一，还须用已证范围或单调性选定分支。
        logical_role: necessary
        check: 读 F3、F4；分别核对收敛性、连续性与不动点选择
      - condition: >-
          A8 不能由「有 n 项且出现 k/n」单独触发。必须能写成
          Σ f(ξ_{k,n})Δx_{k,n}，其中分割覆盖同一固定闭区间、网格直径趋于 0、
          ξ_{k,n} 落在对应小区间，且 f 在该区间 Riemann 可积。
        logical_role: necessary
        check: 明确写出固定区间、分割、取样点、网格直径与被积函数
      - condition: "A2 应展开到**足以确定比较双方各自的第一个非零主项**为止"
        logical_role: supporting_heuristic
        check: 展开后看是否已能读出双方主项；不够就再加一阶

    minimal_probe:
      action: |
        三段，按序执行，随时可提前结束：
        ① 直接代入（A0）——不是待定型就结束；
        ② 按 F1 与 F4 做结构归一化（A6 / A7a / A7b：恒等化型、取对数或换元；另检查递推与 Riemann-sum 结构）；
        ③ 只有当问题落到「两个小量比较」这一分支时，才按 F2 的口径检查是否发生主阶相消。
      interpretation: >
        ③ 处在满足定义域 guard 的乘除上下文，A1 的比值机制直接传递；处在加减上下文且发生相消时，
        A1 的**一阶等价关系不再提供保证**，
        但 A2、A3、A6 乃至「保留更高阶的等价式」都仍可能合法——不是「必须 A2」。
    preference_rule: >
      在有限阶初等渐近结构中 A2 通常比 A3 更直接。**这是该情形下的效率排序，
      不是 A2 的一般优势，也不是对 A3 合法性的判断。**

  mechanism:
    statement: |
      u ~ v 的内容是**乘法稳定性**：lim u/v = 1。
      · 在相关比值有定义的乘除上下文 E = u·h 或 u/h：Ẽ/E = v/u → 1，
        其依据与 E、u 的量级关系无关。
      · 加减上下文 E = u + w：乘法稳定性不再自动传递。u ~ v 只给 u − v = o(u)，
        而保持整体主阶等价需要 u − v = o(E)。相消使 E ≪ u 时两者分离。
      泰勒展开保留有限阶多项式 + 可控余项，因此在相消后仍能读出下一阶主项系数。
    why_action_follows: |
      所以选择的实质不是「因子位还是加数位」，而是「误差相对于 E 是否仍可忽略」。
      probe 的第 ③ 步就是在最低成本下回答这一问。

  failure_boundaries:
    - boundary_id: B1
      changed_condition: 表达式进入会发生**决定性主阶相消**的结构
      effect: becomes_inconclusive
      explanation: >
        **仅凭 u ~ v 这一阶等价关系已不足以保证替换合法**，必须额外控制替换误差。
        这不是「等价替换绝对失效」——保留足够精度（如 sin x ↦ x − x³/6）仍可正确处理。
      witness:
        kind: constructed_counterexample
        ref: |
          lim_{x→0} (sin x − x)/x³：
            用 sin x ~ x（一阶）得 0            ← 保证失效
            用 sin x = x − x³/6 + o(x³)  得 −1/6 ← 同为「替换」，精度足够即正确
          对照（说明加减位置本身无罪）：lim_{x→0}(sin x + x)/x = 2，
          用 sin x ~ x 得 2，正确。此处 E ≍ u，无相消。
        verification: verified
    - { boundary_id: B2, changed_condition: 洛必达求导后的极限不存在（振荡）, effect: becomes_inconclusive,
        explanation: 洛必达是充分条件；求导后极限不存在时不能反推原极限不存在,
        witness: { kind: constructed_counterexample, ref: "lim_{x→∞}(x+sin x)/x = 1；洛必达后为 1+cos x，不存在。", verification: verified } }
    - { boundary_id: B3, changed_condition: 对象从函数换成数列, effect: invalidates,
        explanation: n 不可导，A3 的可导前提不满足,
        witness: { kind: proof, ref: 洛必达法则的前提是可导, verification: verified } }
    - { boundary_id: B4, changed_condition: 泰勒展开阶数不足以覆盖相消, effect: becomes_inconclusive,
        explanation: 得到 0/0 的假象，需提高阶数重展；不是方法错误,
        witness: { kind: proof, ref: 余项阶数不足时主项未被解析出来, verification: verified } }

  revision_history:
    retired_rule_counterexamples:
      - retired_rule: "变限积分的阶 = 被积函数的阶 + 1（v1 规则，已删除）"
        why_wrong: 该规则只在上限为 x 本身时偶然成立
        correct_form: |
          若 g(t) ~ c·t^m（m > −1，保证积分收敛）且 φ(x) → 0⁺，
          在 g 于 (0, δ) 上可积、φ 在极限点单侧取值的条件下：
            ∫₀^{φ(x)} g(t) dt  ~  [c/(m+1)] · φ(x)^{m+1}
          最终关于 x 的阶还要再读 φ(x) 自身的阶。
        counterexample: |
          g(t) = t（关于 t 是 1 阶），φ(x) = x²：
            ∫₀^{x²} t dt = x⁴/2，关于 x 是 4 阶，旧规则给 2 阶。
          代入正确形式：c=1, m=1 → (1/2)(x²)² = x⁴/2 ✓
        verification: verified
        note: >
          按验收意见移出 failure_boundaries——它纠正的是已删除的旧规则，
          不是当前任何 action 的失效边界。

  fallback_policy:
    when_guard_fails:
      - A1 的误差阶守卫失败 → 提高等价式精度，或转 A2 / A3 / A6
      - A3 的四条前提任一失败 → A2 或 A4
      - A5 不适用（非递推数列）→ A4 或 A8
    candidate_actions: [A2, A4, A6]

  evidence:
    positive_problem_ids:
      解答: [2006-16, 2008-15, 2010-17, 2011-15, 2011-18, 2014-15, 2014-19,
             2015-15, 2016-19, 2017-16, 2018-19, 2019-18, 2021-17]
      选择填空: [2004-7, 2006-1, 2007-1, 2008-4, 2009-1, 2010-1, 2013-1, 2015-9,
                2016-9, 2016-12, 2017-1, 2017-9, 2018-9, 2019-1, 2019-3, 2020-1,
                2020-9, 2021-3, 2021-4, 2023-1, 2023-11, 2024-11, 2025-11, 2026-12]
      count: 37
    candidate_set_completeness:
      global_exhaustiveness: not_established
      route_scan_status: open
      open_gaps:
        - 单侧极限的分支讨论、含绝对值/取整的分段讨论——**已移出 scope**，不再算缺口
        - 用中值定理转化差式，尚未系统检查
      alternative_checklist_applied:
        [直接代入, 代数恒等变形, 等价无穷小, 泰勒, 洛必达, 夹逼, 单调有界,
         取对数/换元, 黎曼和, 中值定理转化]
      adversarial_pass: 第三轮（v3）。本轮无新增结构性路线；缺口改由收窄 scope 消解
    scan_basis:
      - { source: 解法.md, status: unverified_inference, routes_scanned: 37 }
      - { source: 高数方法速查.md §1, status: human_synthesis, routes_scanned: 10 }
      - { source: independent_route_generation, status: human_synthesis }
    derivation: human_synthesis
    annotation_dependence: material
    constructed_counterexamples: [B1, B2]

  teaching_policy:
    first_encoding_visible: true
    unprompted_selection_hidden: true
    transfer_probe_must_change_decisive_condition: true   # 改 F2：在同一加减结构上制造/消除主阶相消
```

---

## 2. `calc.vector-integral.route-selection`（v3.2：两级 router）

第一轮判定「填法错了要重写」，理由是把四类积分与三个定理混进同一个 router。确认成立：`F1 = 是否封闭 → A3` 作为统一路由从根上不成立——Stokes 的曲面**本身不需要封闭**，第一类积分也不因载体封闭就能套第二类公式。

**重写方案**：一级 router 只做对象分类，不产生任何 solver；二级才按分类生成合法候选。

```yaml
method_family_rule:
  schema_version: CALC-METHOD-FAMILY-v1.3
  family_id: calc.vector-integral.route-selection
  name: 曲线/曲面积分的路线选择（两级 router）
  status: challenged
  teaching_use: quarantine
  pedagogical_validation: { status: untested }
  status_history:
    - from: null
      to: candidate
      review_kind: author_creation
      artifact_version: 1.0.0
      reviewer: Claude Code
      reason: 首次按 method-family schema 建档，作者自主产出上限为 candidate
      open_items: [一级对象分类与二级 route 均待独立审核]
    - from: candidate
      to: candidate
      review_kind: full-file_audit
      artifact_version: 3.1.0
      reviewer: GPT
      reason: 两级 router 保留，但 action 调用语义、对称映射、闭合优先级、奇点 guard 与 evidence index 尚未关闭
      open_items: [CALC-METHOD-FAMILY-v1.3, surface_second_kind route scan, planar_curve_second_kind route scan, spatial_curve_second_kind route scan]

    - from: candidate
      to: challenged
      review_kind: full_file_audit
      artifact_version: 3.2.0
      reviewer: GPT
      reason:
        - "补域 mechanism 写「合法性只要求闭合与定向一致」，被本 family 自己的 B1 反例推翻"
        - "Stokes 写「任一以 L 为边界的曲面」过强，未要求 admissible spanning surface 的正则性/可定向/边界对应/定向相容"
        - "「第一类积分下 Green/Gauss/Stokes 全部退出候选集」过强：特殊结构可先构造成第二类再进入 theorem route"
      teaching_use: quarantine
      landed_by: Claude Code（v3.2.1，基于远端基线重新落地）
  scope:
    objects: [第一类曲线积分, 第二类曲线积分, 第一类曲面积分, 第二类曲面积分]
    target_tasks: [求积分值, 判断积分的符号或是否为零]
    exclusions: [纯重积分, 场论恒等式的证明, 曲面方程本身的求取]

  rule_kind: structural_rule

  # ── 一级：对象分类，不产生 solver ──
  level_1_router:
    axes:
      - { axis: carrier,   values: [曲线, 曲面] }
      - { axis: kind,      values: [第一类（对弧长/面积，与方向无关）, 第二类（对坐标，与方向有关）] }
      - { axis: ambient,   values: [平面, 空间] }
    acquisition: 读微元记号（ds/dS vs dx,dy,dz/dydz,dzdx,dxdy）与载体所在维数
    rule: >
      必须先定这三个轴，再进入二级。**一级不选方法。**
      v1 的错误正是跳过一级，直接用「是否封闭」路由到「Green/Gauss/Stokes」。

  level_2_candidates:
    - cell: 平面 · 曲线 · 第二类
      actions: [A1, A2, A3g, A4l, A5l, A6]
      note: 只有这一格产生 Green / 补线 / 挖奇点 / 路径无关
    - cell: 空间 · 曲线 · 第二类
      actions: [A1, A2, A3s, A6]
      note: >
        产生 Stokes；**Stokes 要求 L 闭合，但所张曲面 S 不必封闭**。
        **A6（路径无关）同样属于这一格**：三维中 F = ∇φ 时 ∫_L F·dr = φ(B) − φ(A)。
        v2 只把 A6 放进平面格，是遗漏。
    - cell: 曲面 · 第二类
      actions: [A1, A2, A3G, A4s, A5s, A8]
      note: 产生 Gauss / 补面 / 挖奇点 / 合一投影
    - cell: 曲线或曲面 · 第一类
      actions: [A1, A1p, A2, A9]
      note: >
        与方向无关。**不产生 Green/Gauss/Stokes**（这三个定理的对象是第二类积分），
        且 **A7 是单向的（第二类 → 第一类），不能反过来把任意第一类积分转成第二类**。
        v3 补入：A1p（投影公式，A1 的 subroute）、A9（被积为常数时退化为弧长/面积）；
        轮换对称并入 A2。

  candidate_actions:
    - action_id: A1
      description: 直接参数化，化为定积分/二重积分
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [计算参数域上的积分并核对覆盖与方向] }
      terminal_when: [参数化后的积分已经算出，或设问允许保留该积分表示]
      role: primary
      applies_when: [载体可由参数化完整覆盖且参数方向/法向可核对]
    - action_id: A2
      description: 在选定对称映射 T 下化简（含反射、中心对称与轮换对称）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions: { mode: optional_any_of, actions: [A1, A1p] }
      terminal_when: [pullback 关系已经强制积分为零或直接给出所求比例]
      role: primary
      applies_when: [存在可验证的载体自映射 T，且测度、定向与被积对象的变换均已确定]
    - action_id: A3g
      description: Green 公式（平面闭曲线 → 平面区域）
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [计算所得二重积分] }
      terminal_when: [区域积分已算出，或设问只要求 Green 变换后的表示]
      role: primary
      applies_when: [平面, 曲线, 第二类, 闭合]
    - action_id: A3s
      description: Stokes 公式（空间闭曲线 → 任一以它为边界的曲面）
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [选择合法且成本较低的跨接曲面, A1] }
      terminal_when: [所选曲面上的通量已经算出]
      role: primary
      applies_when: [空间, 曲线, 第二类, 闭合]
    - action_id: A3G
      description: Gauss 公式（闭曲面 → 体域）
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [计算所得三重积分] }
      terminal_when: [体积分已经算出，或设问只要求散度形式]
      role: primary
      applies_when: [曲面, 第二类, 闭合]
    - action_id: A4l
      description: 补线：补一段线凑闭，用 Green 后减补线
      action_kind: framework
      produces: subproblems
      followup_actions:
        mode: all_of
        actions:
          - { action: A3g, target: 补线后得到的闭曲线及其平面区域 }
          - { action: A1, target: 新增补线上的第二类曲线积分 }
      terminal_when: [闭合后的 Green 路线与补线积分均已完成并按方向合并]
      role: primary
      applies_when: [平面, 曲线, 第二类, 不闭合]
    - action_id: A4s
      description: 补面：补一块面凑闭，用 Gauss 后减补面
      action_kind: framework
      produces: subproblems
      followup_actions:
        mode: all_of
        actions:
          - { action: A3G, target: 补面后得到的闭曲面及其体域 }
          - { action: A1, target: 新增补面上的第二类曲面积分 }
      terminal_when: [闭合后的 Gauss 路线与补面积分均已完成并按法向合并]
      role: primary
      applies_when: [曲面, 第二类, 不闭合]
    - action_id: A5l
      description: 挖奇点（平面：按 singular set 形态选择小边界）
      action_kind: framework
      produces: subproblems
      followup_actions: { mode: all_of, actions: [A3g, 计算新增小边界积分] }
      terminal_when: [去奇点区域与全部新增小边界均已处理并按诱导方向合并]
      role: primary
      applies_when: [平面, 曲线, 第二类, 奇点可按当前挖洞结构隔离]
    - action_id: A5s
      description: 挖奇点（空间：按 singular set 形态选择小闭曲面）
      action_kind: framework
      produces: subproblems
      followup_actions: { mode: all_of, actions: [A3G, 计算新增小边界通量] }
      terminal_when: [去奇点体域与全部新增小边界均已处理并按诱导法向合并]
      role: primary
      applies_when: [曲面, 第二类, 奇点可按当前挖洞结构隔离]
    - action_id: A6
      description: 路径无关 → 换路径或求势函数作差
      action_kind: solver
      produces: result_object
      terminal_when: [势函数已确定且端点代入已经给出当前要求]
      role: alternative
      applies_when: [曲线, 第二类, 场为某全局势函数的梯度, 平面或空间均可]
      eligible_cells: [planar_curve_second_kind, spatial_curve_second_kind]
    - action_id: A7
      description: "**第二类 → 第一类**表示转换（方向余弦），单向"
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions: { mode: any_of, actions: [A1, A2] }
      terminal_when: [设问只要求完成第一类表示转换]
      role: alternative
      applies_when: [第二类积分, 转成第一类后更易算]
      direction_note: >
        标准关系 ∬_S P dydz + Q dzdx + R dxdy = ∬_S (P cosα + Q cosβ + R cosγ) dS
        的方向是**第二类 → 第一类**。反方向「把任意第一类积分转成第二类再套
        Gauss/Stokes」**不存在同样普遍自然的规则**；个别第一类积分若有特殊结构
        可构造成第二类，只作特殊 subroute 记录，不作一般候选。
    - action_id: A1p
      description: 投影公式（投到坐标面化为重积分/定积分）
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [计算投影域上的积分] }
      terminal_when: [投影积分已经算出，或设问允许保留该积分表示]
      subroute_of: A1
      role: primary
      applies_when: [第一类, 载体可在所选投影上写成显式图形且覆盖重数已核对]
    - action_id: A9
      description: 被积函数在载体上恒为常数时，退化为「常数 × 弧长/面积」
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [求载体的弧长或面积] }
      terminal_when: [载体弧长或面积已知或已算出]
      role: primary
      applies_when: [第一类, 被积在载体上恒为常数]
    - action_id: A8
      description: 合一投影法（三项统一到 dxdy 后再算）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [A1] }
      terminal_when: [统一投影后的积分已经算出]
      role: alternative
      applies_when: [曲面, 第二类, "可写成 z=z(x,y)"]

  selection_rule:
    guards:
      - condition: "**必须先完成一级分类**，再选二级候选"
        logical_role: necessary
        check: 三个轴各定一个值
      - condition: "A3g / A3G 要求载体闭合；**A3s 只要求 L 闭合，所张曲面 S 不必闭合**"
        logical_role: necessary
        check: 分别核对，不要把三个定理的闭合要求当成同一条
      - condition: >-
          A4l / A4s / A5l / A5s 要求原载体与补/挖的部分合起来构成同一区域的**完整闭边界**，
          且**每一个边界分量都按同一个「剩余区域」的诱导正向取向**。
        logical_role: necessary
        check: |
          逐分量核对。不要只说「统一取外侧」——对有洞区域那句话会误导：
          · Green：剩余区域的外边界取**逆时针**，内洞边界取**顺时针**；
          · Gauss：外表面法向朝外，但**挖掉的小球面，其「剩余区域外法向」指向小球中心**。
        explanation: >
          这正是挖奇点题最容易差一个负号的位置。v2 的「方向统一取正向（外侧）」
          对补域情形成立，对挖奇点情形会把内边界定向搞反。
      - condition: A3g / A3G / A3s 要求场在所需区域内满足各自定理的光滑性条件（分量有连续偏导，无奇点）
        logical_role: necessary
        check: 解分母零点集，判断是否落入区域
      - condition: >-
          A5l / A5s 不是「发现奇点就挖」：singular set 必须能由所选小边界隔离；
          挖去后剩余区域须满足对应定理条件；全部新增小边界积分/通量须存在且可控制；
          点奇点可对应小圆/小球，但曲线、曲面或非隔离奇点不能未经论证套用同一结构。
        logical_role: necessary
        check: 依次记录 singular set 形态、隔离邻域、剩余区域条件与小边界极限
        explanation: >
          任一项未知时，A5 只能记为 pending candidate；不能从「原区域内有奇点」直接推出
          当前挖洞 framework 合法或更省事。
      - condition: "A6 的成立条件是**场为某个全局势函数的梯度**"
        logical_role: necessary
        check: 找势函数，或在目标区域上验证沿任一闭路径的积分为零
      - condition: "「∂P/∂y = ∂Q/∂x **且区域单连通**」是 A6 的**充分条件**之一"
        logical_role: sufficient
        check: "满足即可用；**不满足不等于 A6 不可用**"
        explanation: >
          v1 把它标成 necessary 是错的。反例：F = (2x, 2y) = ∇(x²+y²)
          在环域 1 < x²+y² < 4 上，区域非单连通，但 F 处处是同一个全局势函数的梯度，
          积分路径无关。单连通只是从「旋度为零」推出「有势函数」的常用充分条件。
      - condition: >-
          A2 必须先选定一个对称映射 T，再分别核对：T 是否把载体（或待配对的两部分）
          映回载体；弧长/面积测度在 T 下怎样变；第二类积分的定向或法向怎样变；
          标量被积函数或微分形式的 pullback T*ω 与原对象是相同、相反还是轮换到另一项。
          只有这些变换合成后的总因子已经确定，才可作倍增、抵消或轮换合并。
        logical_role: necessary
        check: 逐项写 carrier、measure、orientation、integrand/form 在 T 下的像
        explanation: 奇偶性只是 T 为反射时的一个特例，不能覆盖轮换对称
      - condition: 补面尽量选垂直于坐标轴的平面
        logical_role: supporting_heuristic
        check: 看能否找到这样的平面
        explanation: 效率偏好，不是合法性条件

    minimal_probe:
      action: |
        **必须在一级分类之后执行，且按格子分派**：
        · 平面·曲线·第二类 → 算 ∂Q/∂x − ∂P/∂y
        · 曲面·第二类       → 算 div F
        · 空间·曲线·第二类   → 算 curl F
        · 第一类（任意）     → **不算上述任何一个**，改查对称性与参数化难度
      interpretation: >
        前三格中若结果为常数或零，说明对应的定理路线**获得了明显的简化信号，应进入
        高优先级候选**——但不能据此断言「收益最大」：补线/补面本身仍可能很难，需与 A1
        比较后再定。结果仍复杂则回到 A1。
        第四格中这些量与题目无关，强行计算即是路由错误的信号。
    preference_rule: >
      在一个格子内部，闭合只使对应定理路线成为**高优先级合法候选**；仍须核对光滑性、
      定向和变换后的积分成本，并与 A1 直接参数化、A2 对称性比较。不存在
      「闭合 → 无条件直接用定理」的规则。不闭合且补域合法时 framework 才进入候选；
      含奇点时先执行 A5 的四项 guard。以上只是在合法候选之间比较执行成本。

  mechanism:
    statement: |
      补面（曲面·第二类）：设 S ∪ Σ₀ = ∂V 且方向统一取外侧，由 Gauss 公式
        ∬_S F·n dS + ∬_{Σ₀} F·n dS = ∭_V (∇·F) dV
      故
        ∬_S F·n dS = ∭_V (∇·F) dV − ∬_{Σ₀} F·n dS。
      补线（平面·曲线·第二类）把 Gauss 换成 Green、体域换成平面区域，结构相同。
      Stokes 的结构不同：∮_L F·dr = ∬_S (∇×F)·n dS，**S 是自由选择的、以 L 为边界的曲面**，
      因此它不是「把不封闭补成封闭」，而是「把线积分换到任一张方便的曲面上」。
    why_action_follows: |
      补域的全部作用是去掉「不封闭」这一个障碍，代价是多算一块 Σ₀，
      所以选补面的标准是「Σ₀ 上好算」，合法性只要求闭合与定向一致。
      Stokes 的自由度在选 S，不在补 S——把两者混为一谈正是 v1 的结构错误。

  failure_boundaries:
    - boundary_id: B1
      changed_condition: 闭合区域内出现被积场的奇点
      effect: invalidates
      explanation: Green/Gauss 要求场在**整个**区域内有连续偏导；奇点处不满足
      witness:
        kind: constructed_counterexample
        ref: |
          F = ( −y/(x²+y²),  x/(x²+y²) )，L 为单位圆周取正向。
          在去心平面上 ∂Q/∂x = ∂P/∂y = (y²−x²)/(x²+y²)²，即"旋度"为零；
          若无视原点奇点、直接对整个圆盘用 Green，会得到 ∬ 0 dxdy = 0。
          而实际 ∮_L F·dr = ∫₀^{2π} 1 dθ = 2π ≠ 0。
          **这直接证明「区域内光滑性」这条 guard 不是装饰。**
        verification: verified
      candidate_source_references:
        - { ref: 2020-16, intended_role: possible_real_instance, verification: pending, evidentiary_weight: none }
      recovery: 转 A5l / A5s
    - boundary_id: B2
      changed_condition: 补面的定向与闭曲面的外向不一致
      effect: invalidates
      explanation: 通量可加式中该项符号反转
      witness: { kind: proof, ref: 由 mechanism 的等式直接可见, verification: verified }
    - boundary_id: B4
      changed_condition: 补面存在且合法，但补面上的积分比原积分更难算
      effect: loses_advantage
      explanation: >
        **不是 Gauss 公式失效**，只是 A4s 不再是优先候选，应回到 A1 或 A2。
        把「不好算」误判为「不能用」是本族最容易串位的地方。
      witness:
        kind: constructed_counterexample
        ref: |
          取 F = (0, 0, z)，S 为平面 z = 0 上的单位圆盘 x²+y² ≤ 1，**取下侧**
          （即取上半单位球体 V 的外法向 n = (0,0,−1)，这样 S 与补面能直接凑成 ∂V）。

          直接算（A1）：S 上 z ≡ 0，故 ∬_S F·n dS = ∬_S (−z) dS = 0。**一步完成。**

          改走 A4s：补上半球面 Σ₀: x²+y²+z²=1 (z ≥ 0) 取外侧，则 S ∪ Σ₀ = ∂V。
            div F = ∂z/∂z = 1，∭_V div F dV = (1/2)·(4/3)π = (2/3)π；
            Σ₀ 上：∬_{Σ₀} F·n dS = ∬_{x²+y²≤1} √(1−x²−y²) dxdy
                 = ∫₀^{2π}∫₀^1 √(1−r²)·r dr dθ = 2π·(1/3) = (2/3)π。
            故 ∬_S F·n dS = (2/3)π − (2/3)π = 0 ✓ 与直接算一致。

          两条路线都合法、结果相同，但 A4s 多算了一个三重积分与一个曲面积分。
          **这正是 loses_advantage 而非 invalidates。**

          ⚠️ 附注：本反例的初稿把 S 写成「取上侧」，那样 S ∪ Σ₀ 就不是 ∂V 的
          一致外向定向，等式会差一个符号——**恰好是本族 guard「方向统一」与 B2
          所警告的那类错误**。此处保留这条记录，因为它本身就是该 guard 有效性的证据。
        verification: verified
    - boundary_id: B5
      changed_condition: 已选 T，但 carrier / measure / orientation / integrand-form 中至少一项的变换与声称的配对关系不兼容
      effect: invalidates
      explanation: A2 的倍增、抵消或轮换合并依赖四项 pullback 关系的合成，缺一项就不能推出所声称的化简
      witness: { kind: proof, ref: 由换元公式或微分形式的 pullback 等式直接可见, verification: verified }
    - boundary_id: B6
      changed_condition: 积分从第二类换成第一类
      effect: changes_branch
      explanation: >
        整条路由改变：Green/Gauss/Stokes 全部退出候选集（其陈述对象是第二类积分），
        A7 只能单向使用，方向不再有意义。
        **对称性判据不是简单的「不变号即取两倍」**：第一类积分没有第二类积分的定向因子，
        对称性由**载体测度与标量被积函数在选定 T 下的变换**决定——
        不变则取半边两倍，变号则成对抵消为零。**两种情形都存在。**
      witness: { kind: proof, ref: 三个定理的陈述对象都是第二类积分；第一类的对称性只依赖标量函数的变换性质, verification: verified }

  revision_history:
    retired_rule_counterexamples:
      - retired_rule: "∂P/∂y = ∂Q/∂x 且区域单连通是路径无关的必要条件（v1 规则，已删除）"
        why_wrong: 单连通是由旋度为零推出全局势函数的常用充分条件，不是路径无关的必要条件
        counterexample: |
          F = (2x, 2y) 在环域 D = {1 < x²+y² < 4} 上。D 非单连通，
          但 F = ∇(x²+y²)，故对 D 内任意路径积分只取决于端点。
        verification: verified
        note: 这是旧规则的反例，不属于当前规则的 failure boundary

  fallback_policy:
    when_guard_fails:
      - 闭合/定向守卫失败 → 重新划分区域，或转 A1
      - 奇点使 theorem 的光滑性 guard 失败 → 只有 A5 的隔离、剩余区域、小边界可控性与 singular-set 形态四项 guard 全部通过时，才把 A5l / A5s 放入候选；否则转 A1 / A2 或记 pending
      - A6 的势函数守卫失败 → 不能用 A6；转该格的 framework 或 A1
    candidate_actions: [A1, A2, A7]

  evidence:
    positive_problem_ids:
      解答: [2004-17, 2005-19, 2006-19, 2007-18, 2008-16, 2009-19, 2010-19,
             2012-19, 2014-18, 2015-19, 2016-17, 2016-18, 2017-19, 2018-17,
             2020-16, 2020-18, 2021-20, 2023-19, 2024-20, 2025-20, 2026-19]
      选择填空: [2004-3, 2005-4, 2006-3, 2007-6, 2007-14, 2008-12, 2009-11,
                2010-11, 2011-12, 2012-12, 2013-4, 2014-12, 2017-11, 2018-12,
                2019-4, 2019-12, 2021-14, 2024-2, 2025-14]
      count: 40
    candidate_set_completeness:
      global_exhaustiveness: not_established
      route_scan_status: open        # 四格全部完成前，整族保持 open
      route_scan_by_cell:
        first_kind:                 { status: scanned_v3, note: "产出 A1p、A9，并更正 A7 为单向" }
        surface_second_kind:        { status: pending, priority: 1, note: "验收指定优先：该格几乎覆盖 v1.2 的全部 action 关系（参数化/投影/对称/Gauss/补面/挖奇点/定向/合一投影/invalidates 与 loses_advantage 之别）" }
        planar_curve_second_kind:   { status: pending, priority: 2, note: "复用直接法+Green+补域+挖奇点，另加势函数路线，用于检查『定理路线』与『potential route』是否被错误统合" }
        spatial_curve_second_kind:  { status: pending, priority: 3, note: "最薄；核心竞争只有 直接参数化 / Stokes / 路径无关 / 对称性" }
      open_gaps:
        - "「第一类」格已按验收意见补扫：新增 A1p（投影公式，A1 的 subroute）与 A9（被积为常数时退化为弧长/面积），轮换对称并入 A2"
        - 平面第二类 / 空间第二类 / 曲面第二类 三格尚未逐格做 adversarial pass
      adversarial_pass: >
        第三轮（v3）。本轮只完成了「第一类」这一格的定向扫描（验收指定优先），
        产出三条：A1p、A9、以及「A7 是单向的」这一结构性更正。其余三格待扫。
    scan_basis:
      - { source: 解法.md, status: unverified_inference, routes_scanned: 40 }
      - { source: 高数方法速查.md §8, status: human_synthesis, routes_scanned: 16 }
      - { source: 同题识别.md 组 5 / 组 26 / 组 33, status: human_synthesis, routes_scanned: 3 }
      - { source: independent_route_generation, status: human_synthesis }
    derivation: human_synthesis
    annotation_dependence: material
    constructed_counterexamples: [B1, B4]

  teaching_policy:
    first_encoding_visible: true
    unprompted_selection_hidden: true
    transfer_probe_must_change_decisive_condition: true   # 改一级分类的任一轴（如第二类→第一类），看学生是否整条路由重走
```

---

## 3. `calc.extrema.constraint-selection`（v3.2）

```yaml
method_family_rule:
  schema_version: CALC-METHOD-FAMILY-v1.3
  family_id: calc.extrema.constraint-selection
  name: 多元极值/最值的约束类型选择
  status: challenged
  teaching_use: quarantine
  pedagogical_validation: { status: untested }
  status_history:
    - from: null
      to: candidate
      review_kind: author_creation
      artifact_version: 1.0.0
      reviewer: Claude Code
      reason: 首次按 method-family schema 建档，作者自主产出上限为 candidate
      open_items: [数学 guard、route scan 与 schema 均待独立审核]
    - from: partially_verified
      to: candidate
      review_kind: full_file_audit
      artifact_version: 3.1.0
      reviewer: GPT
      reason:
        - F4 continuity acquisition conflates nondifferentiability with discontinuity
        - A2 local/global followup conflicts with stale guard/B5
        - compact equality-constrained feasible sets are misrouted into A3
    - from: candidate
      to: challenged
      review_kind: full_file_audit
      artifact_version: 3.2.0
      reviewer: GPT
      reason:
        - "F7 已检测不可导点，但 local+unconstrained 仍路由到 A1（驻点+Hessian），漏掉 f=|x|+y² 这类梯度不存在的严格局部极小"
        - "A2 的 applies_when 只有「有等式约束」，缺可微/正则性；f=|x| 在 x²+y²=1 上的约束最小点 (0,±1) 处 f 不可微"
        - "B6 与 F6 不一致：local→global 后不应无条件进入 A3；紧的等式约束流形应走 A2/A4/参数化"
        - "A4 消元未要求保留全部代数分支（y=±√(1−x²) 只取正支会漏 f=y 的全局最小）"
      teaching_use: quarantine
      landed_by: Claude Code（v3.2.1，基于远端基线重新落地）
  scope:
    objects: [二元实值函数]
    constraints: [至多一个常见等式约束]
    scope_note: >
      v2 写「二元及以上」，但 A1 用的是 AC−B²——那只是二维 Hessian 判别的写法。
      三元及以上必须讨论 Hessian 的正定/负定/不定（Sylvester 判据或特征值）。
      按验收意见**收窄而非扩大**，这也与当前正例集合一致。
    target_tasks: [求局部极值, 求全局最大最小值, 求梯度模的约束最值]
    domain_restriction: >
      涉及 A3 时，定义域限于**有界闭且分段光滑**的区域（考研常见形态）。
      一般有界闭集上「内部 + 边界 + 角点」不构成 structural rule。
    exclusions: [一元函数的极值, 极值存在性的抽象证明, 条件极值的几何解释]

  rule_kind: structural_rule

  pre_solution_features:
    - { feature_id: F1, description: 问的是**局部极值**还是**全局最大最小值**, acquisition: 读设问词, role: decisive }
    - { feature_id: F2, description: 有无等式约束 g(x,y)=0, acquisition: 读是否给出附加方程, role: decisive }
    - { feature_id: F3, description: 定义域是否为**有界闭且分段光滑**的集合, acquisition: 读定义域描述, role: decisive }
    - feature_id: F4
      description: 目标函数在该定义域上是否连续
      acquisition: 检查未定义点、分母奇点、发散奇点，以及分段拼接处的极限与函数值是否一致
      role: decisive
    - { feature_id: F5, description: 约束是否可显式解出一个变量, acquisition: 尝试从 g=0 解出某变量, role: supporting }
    - feature_id: F6
      description: "feasible_set_type：full_dimensional_region 还是 equality_constrained_manifold"
      acquisition: 判断可行集在 R² 中是否含二维内部；单个等式通常给一维约束曲线，但退化情形需另查
      role: decisive
    - feature_id: F7
      description: 可行集内有哪些不可导点或分段转折点
      acquisition: 在连续性检查之后单独求不可导集合；不可导不等于不连续
      role: decisive

  candidate_actions:
    - action_id: A1
      description: 无约束驻点 + 二维 Hessian 判别（AC−B²）
      action_kind: solver
      produces: candidate_set
      terminal_when: [目标是无约束局部极值，F7 为空，且全部驻点均已分类]
      role: primary
      applies_when: [二元, 无约束, 求局部极值 或 作为 A3 的内部步骤]
    - action_id: A2
      description: 拉格朗日乘数法
      action_kind: solver
      produces: candidate_set
      followup_actions:
        mode: all_of
        actions:
          - { action: 在约束内逐点做局部分类, when: F1 为局部极值 }
          - { action: 与全部其他合法候选比较函数值, when: F1 为全局最值 }
      terminal_when: [与 F1 对应的 followup 已完成，且正则点、非正则点及其他可行分支均未遗漏]
      role: primary
      applies_when: [有等式约束]
      output_note: >
        A2 求解 ∇f = λ∇g 得到的是**候选点集合**，不是解。
        局部任务要求约束内分类；全局任务要求完整候选比较，二者不能用同一句
        「每组驻点代回比较」覆盖。
    - action_id: A3
      description: 二维有界闭区域的全局最值 framework
      action_kind: framework
      produces: subproblems
      followup_actions:
        mode: sequence
        actions: [搜索内部临界点与不可导点, 完整分析每段边界及其非光滑点, 比较全部候选函数值]
      terminal_when: [三步均完成且给出全局最大值与最小值及其取到点]
      role: primary
      applies_when: [F1 为全局最值, F3 成立, F4 成立, F6 为 full_dimensional_region]
    - action_id: A4
      description: 用约束消元，降为低一维问题
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [在保留后的定义域上求低维极值, 检查端点并映回原约束] }
      terminal_when: [降维问题的全部候选已分类或比较并映回原可行集]
      role: alternative
      applies_when: [F5 成立]
    - action_id: A5
      description: 对 |∇f|² 用拉格朗日（避开根号）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [A2, 把平方后的比较结果还原为梯度模结论] }
      terminal_when: [平方目标的候选已完整处理且利用非负性合法还原]
      role: alternative
      applies_when: [目标是梯度模的约束最值]
    - action_id: A6
      description: 不等式估计（均值 / 柯西 / 配方）给出界并验证可取到
      action_kind: solver
      produces: result_object
      terminal_when: [目标所需上下界均已证明且取等条件在可行集内实现]
      role: alternative
      applies_when: [目标与约束具备可套用的经典不等式结构]
    - action_id: A7
      description: 参数化约束曲线或二维区域的边界，降为一元问题
      action_kind: solver
      produces: transformed_problem
      followup_actions: { mode: sequence, actions: [求参数区间上的一元极值, 检查端点与周期重复, 映回原可行集] }
      terminal_when: [参数域全部候选已分类或比较并映回原可行集]
      role: primary
      applies_when: [F6 为 equality_constrained_manifold 或 A3 的边界步骤, 相关曲线可被参数化覆盖]

  selection_rule:
    guards:
      - condition: A1 的判别法要求 f 在驻点邻域内二阶偏导连续
        logical_role: necessary
        check: 读函数的光滑性
      - condition: "A1 在 AC−B² = 0 时**无结论**"
        logical_role: necessary
        check: 算出判别式后先看是否为零
      - condition: >-
          A2 在正则点只给必要候选。若 F1 为局部极值，须在约束集合内逐点分类；
          若 F1 为全局最值，须把这些点与非正则点、其他约束分支及端点等全部合法候选比较函数值。
        logical_role: necessary
        check: 先读 F1，再核对对应 followup；不能用「全部代回比较」替代局部分类
      - condition: "A2 的推导要求候选点处**∇g ≠ 0**（正则性）；非正则点须作为额外候选单独讨论"
        logical_role: necessary
        check: 验 ∇g ≠ 0
      - condition: "A3 要求**目标函数在该有界闭集上连续**（Weierstrass 的前提）"
        logical_role: necessary
        check: 读 F4
        explanation: >
          v1 只写了「有界闭集」。有界闭集本身不足以保证最值取得——
          还需要 f 连续。这是 v1 漏掉的最重要一条 guard。
      - condition: "A3 要求定义域**有界且闭**；无界或开区域上最值可能不存在"
        logical_role: necessary
        check: 读 F3
      - condition: >-
          A3 只路由二维 full_dimensional_region。紧的 equality_constrained_manifold
          （如 x²+y²=1）虽然闭且有界，也不能当成「二维内部 + 二维边界」区域处理；
          应走 A2 / A4 / A7，并对完整候选集分类或比较。
        logical_role: necessary
        check: 读 F6；检查可行集在 R² 中是否含二维内部
      - condition: >
          A3 的完整搜索是：**内部临界点与不可导点 + 完整边界分析 + 边界的非光滑点（角点）**。
          三部分一处都不能少；边界含多段要逐段算。
        logical_role: necessary
        check: 动笔前先在草稿写下这三行
        explanation: v1 写的「内部 + 边界 + 顶点」中，「顶点」只是「边界非光滑点」的特例
      - condition: A4 消元后要保留原约束对新变量的取值范围
        logical_role: necessary
        check: 从 g=0 反推新变量的定义域
      - condition: "A6 给出的界必须验证**可以取到**，否则只是估计不是最值"
        logical_role: necessary
        check: 找出取等条件并验证它在可行集内
      - condition: F5 成立时 A4 通常比 A2 快
        logical_role: supporting_heuristic
        check: 看 F5
        explanation: 效率偏好；A2 在能消元时同样合法

    minimal_probe:
      action: |
        四段，按序：
        ① 读 F1——局部极值 还是 全局最值？（零成本，且决定后面走不走 A3）
        ② 读 F2——有无约束？
        ③ 读 F6——二维 full-dimensional region 还是 equality-constrained manifold？
        ④ 仅当 ① 为全局最值且 ③ 为二维区域时，才读 F3 与 F4（有界闭 + 连续）。
      interpretation: >
        (局部, 无约束) → A1；(局部, 有约束) → A2 或 A4；
        (全局, full_dimensional_region, F3∧F4 成立) → A3；
        (全局, equality_constrained_manifold) → A2 / A4 / A7，再做完整 candidate comparison；
        (全局二维区域, F3 或 F4 不成立) → A3 不可用，须先讨论最值是否存在。
    preference_rule: >
      A3 是**外层 framework，不是与 A1/A2 并列的 solver**；同时它只属于二维区域 branch。
      framework 的内部顺序与完成条件由 `followup_actions` + `terminal_when` 表达。

  mechanism:
    statement: |
      无约束（**在内点且 f 可微时**）：极值点必为驻点。
      ⚠️ 前提不可省：f = |x| + y² 在原点有极小值，但梯度不存在，该点不是驻点。
      二阶泰勒的二次型 A dx² + 2B dxdy + C dy² 的定号性
      由 AC−B² 与 A 的符号决定——正定/负定给极小/极大，不定给鞍点，半定（=0）无结论。
      有约束（正则点处）：约束流形上的极值点满足 ∇f = λ∇g，即目标梯度与约束梯度线性相关。
      有界闭集：f 连续 + 定义域有界闭 ⇒ 最值必被取到（Weierstrass），
      且最值点只能落在内部临界点、内部不可导点、或边界上。
      但「内部 + 边界」搜索是 full-dimensional region 的几何分解；紧等式约束曲线
      虽同样可由 Weierstrass 保证取得最值，却应在约束流形上搜索候选，而不是调用二维区域 A3。
    why_action_follows: |
      三条 mechanism 分别对应 A1、A2、A3，并解释了为什么 A3 是 framework：
      Weierstrass 只保证最值存在并限定其位置，**不提供求法**；
      求的过程仍要在内部调 A1、在边界调 A2 或 A4。

  failure_boundaries:
    - boundary_id: B1
      changed_condition: AC−B² 从非零变为零
      effect: becomes_inconclusive
      explanation: 判别法无结论，**不等于不是极值**
      witness:
        kind: constructed_counterexample
        ref: |
          f = x⁴ + y⁴ 在原点：A=B=C=0，判别式为零，但原点是严格极小值点。
          f = x⁴ − y⁴ 在原点：判别式同样为零，却是鞍点。
          判别式相同、结论相反 ⇒ 该情形下判别法确实无信息。
        verification: verified
    - boundary_id: B2a
      changed_condition: "**只改闭性**：定义域从闭圆盘 x²+y²≤1 变为开圆盘 x²+y²<1（仍有界）"
      effect: invalidates
      explanation: Weierstrass 需要闭；失去闭性后最值可能不存在
      witness:
        kind: constructed_counterexample
        ref: "f(x,y)=x 在开圆盘 x²+y²<1 上：sup=1、inf=−1 均不可取到，最值不存在。"
        verification: verified
    - boundary_id: B2b
      changed_condition: "**只改有界性**：定义域从紧集变为无界闭集（仍闭）"
      effect: invalidates
      explanation: Weierstrass 需要有界；失去有界性后最值可能不存在
      witness:
        kind: constructed_counterexample
        ref: "f(x,y)=x 在闭半平面 {x≥0} 上：该集合是闭的但无界，f 无最大值。"
        verification: verified
      note: >
        v2 的 B2 一次同时改了闭性与有界性两个条件，无法直接用于「只改一个决定性
        条件」的迁移题。按验收意见拆为 B2a / B2b。
    - boundary_id: B2c
      changed_condition: 定义域有界闭，但目标函数在其上不连续
      effect: invalidates
      explanation: >
        Weierstrass 的两个前提缺一不可。这是 v1 完全漏掉的边界。
      witness:
        kind: constructed_counterexample
        ref: |
          在闭圆盘 x²+y² ≤ 1 上定义 f(0,0)=0，其余点 f = 1/(x²+y²)。
          定义域有界闭，但 f 在原点不连续，f 在该集上无上界，最大值不存在。
        verification: verified
    - boundary_id: B3
      changed_condition: 约束在候选点处 ∇g = 0（非正则点）
      effect: invalidates
      explanation: ∇f = λ∇g 的推导依赖 ∇g ≠ 0；非正则点须单独检查
      witness:
        kind: constructed_counterexample
        ref: |
          取 g(x,y) = x² + y² = 0，f(x,y) = x。
          可行集只有原点，故原点同时是约束最大值点与最小值点。
          但 ∇g(0,0) = (0,0)，∇f(0,0) = (1,0)，
          **不存在 λ 使 ∇f = λ∇g** —— 标准拉格朗日条件把唯一的极值点漏掉了。
        verification: verified
    - boundary_id: B4
      changed_condition: 闭区域上只算了内部驻点、跳过边界
      effect: invalidates
      explanation: 最值经常恰在边界取到；极端情形下内部根本没有驻点
      witness:
        kind: constructed_counterexample
        ref: |
          f(x,y) = x 在闭圆盘 x² + y² ≤ 1 上。
          ∇f = (1, 0) ≠ 0 处处成立 ⇒ **内部没有任何驻点**；
          若只搜内部，会得出"无最值"的错误结论。
          实际全局最大在 (1,0) 取 1、全局最小在 (−1,0) 取 −1，两者都在边界上。
        verification: verified
      candidate_source_references:
        - { ref: 2007-17, intended_role: possible_real_instance, verification: pending, evidentiary_weight: none }
        - { ref: 2018-16, intended_role: possible_real_instance, verification: pending, evidentiary_weight: none }
        - { ref: 2024-18, intended_role: possible_real_instance, verification: pending, evidentiary_weight: none }
    - boundary_id: B5a
      changed_condition: 局部约束极值任务中，把 Lagrange 候选点直接当成已分类的极值点
      effect: becomes_inconclusive
      explanation: Lagrange 只给必要候选，候选可能既非约束局部极大也非约束局部极小
      witness:
        kind: constructed_counterexample
        ref: |
          在 x²+y²=1 上考察 f(x,y)=y³。
          ∇f=(0,3y²)，在 (1,0)、(−1,0) 处 ∇f=(0,0)=0·∇g（λ=0），因而被 Lagrange
          方程收入候选；沿圆周参数化 (cos t, sin t) 得 f=sin³t，
          在 t=0、t=π 附近 sin³t 变号，故这两点都**不是**约束局部极值。
          ⇒ Lagrange candidate ⇏ classified constrained extremum。
        verification: verified
        reviewer: GPT
        authority: GPT v3.2 full-file audit
        verification_lineage: >
          v3.2 中记为 candidate_counterexample / pending / evidentiary_weight: none；
          经 GPT 对完整 v3.2 的 full-file audit 独立数学核验后晋升为 verified witness。
    - boundary_id: B5b
      changed_condition: 全局最值任务中，只比较 Lagrange 候选集的一部分或漏掉其他合法候选
      effect: invalidates
      explanation: 未完成完整 candidate comparison 时，声称的全局最大/最小可能被遗漏候选改变
      witness: { kind: proof, ref: 必要条件只筛出候选；全局结论要求覆盖全部可行候选并比较其值, verification: verified }
      # v1 曾引用 2005-21 作为 witness —— 核查确认该题主考点为「AB=O 的秩关系与线性相关性」，
      # 是线代题，属跨题串位，已删除。这正是 source witness 标 pending 的价值。
    - boundary_id: B6
      changed_condition: 设问从「极值」变为「最值」（F1 翻转）
      effect: changes_branch
      explanation: 局部概念变全局概念，整条路由改变——A3 从不参与变为外层框架
      witness: { kind: proof, ref: 局部极值点未必是全局最值点, verification: verified }
    - boundary_id: B7
      changed_condition: 可行集从二维紧区域改为紧的等式约束曲线（F6 翻转）
      effect: changes_branch
      explanation: Weierstrass 仍保证最值取得，但二维「内部 + 边界」A3 不再是该对象的路由；应在约束流形上找完整候选
      witness:
        kind: constructed_counterexample
        ref: |
          在 x²+y²=1 上求 f(x,y)=x 的最大最小值。可行集闭且有界，f 连续，
          但它是一维约束曲线，不是二维闭区域；正确候选由 A2、A4 或 A7 得到，
          最大值 1 在 (1,0)，最小值 −1 在 (−1,0)。把它送入二维区域 A3 会错误描述对象层级。
        verification: verified

  fallback_policy:
    when_guard_fails:
      - AC−B²=0 → 回定义 / 沿路径讨论 / 换 A6
      - ∇g=0 → 把该点作为额外候选单独讨论
      - F3 或 F4 不成立 → 先讨论最值是否存在（边界行为、无穷远行为）
      - F6 为 equality_constrained_manifold → 转 A2 / A4 / A7，不调用二维区域 A3
    candidate_actions: [A4, A6]

  evidence:
    positive_problem_ids:
      解答: [2004-19, 2007-17, 2008-17, 2009-15, 2012-16, 2013-17, 2015-17,
             2018-16, 2019-16, 2020-15, 2021-19, 2023-18, 2024-18, 2026-17]
      选择: [2006-10, 2011-3]
      count: 16
    candidate_set_completeness:
      global_exhaustiveness: not_established
      route_scan_status: open        # v1 误标 locally_saturated
      open_gaps:
        - A6（不等式估计）与 A7（参数化边界）均已收入候选集，但尚未做针对二者的 adversarial pass
        - 尚未系统检查：对称性归约、三元及以上的 Hessian 判别（**已移出 scope**，不再算缺口）
      alternative_checklist_applied:
        [无约束驻点+Hessian, 拉格朗日, 闭区域框架, 消元降维, 梯度模的约束最值,
         不等式估计, 参数化边界]
      adversarial_pass: >
        第二轮（v2）。第一轮把 A6 以「22 年真题未作为主路线出现」为由排除，
        这是把 route scan 的目标（scope 内有哪些合法结构路线）
        错当成了历史统计（哪些路线当过主解）。已纠正。
    scan_basis:
      - { source: 解法.md, status: unverified_inference, routes_scanned: 16 }
      - { source: 高数方法速查.md §5, status: human_synthesis, routes_scanned: 15 }
      - { source: independent_route_generation, status: human_synthesis }
    derivation: human_synthesis
    annotation_dependence: material
    constructed_counterexamples: [B1, B2a, B2b, B2c, B3, B4, B5a, B7]

  teaching_policy:
    first_encoding_visible: true
    unprompted_selection_hidden: true
    transfer_probe_must_change_decisive_condition: true   # 改 F1（局部↔全局）、F4（连续性）或 F6（二维区域↔约束流形）
```

---

## 4. 当前仍未解决的

1. **P0 等待 GPT 重审。**本版已按 full-file audit 执行 schema v1.3、limit、vector-integral 与 extrema 的指定修复，但三族仍全部是 `candidate`；作者没有升级权限。GPT 需要基于完整 v3.2 文件裁定哪些可升 `partially_verified`。

2. **第二族的三格扫描尚未启动。**按停止规则，只有 P0 schema/limit/extrema 重审通过后，才按曲面·第二类（priority 1）→ 平面·曲线·第二类（2）→ 空间·曲线·第二类（3）继续。四格全完成前整族 `route_scan_status` 保持 `open`。

3. **`candidate_source_references` 的后续。**四条真题引用（2020-16、2007-17、2018-16、2024-18）现在 `evidentiary_weight: none`。晋升为 `witness` 需要独立核验题面与路线，而我核验的依据仍是 `解法.md`。这条路要通，得等题面库从 `ocr_uncertain` 升到 `verified_from_paper`——也就是等原卷。

4. **B5a 的新构造反例待独立复核。**`f=y³` 在单位圆上的例子已给出完整计算，但它是本轮新构造，当前只记 `candidate_counterexample / pending / evidentiary_weight: none`，不是 witness。

5. **教学效果没有升级。**三族 `pedagogical_validation` 均为 `untested`；schema 与数学规则写得更完整不构成教学效果证据。
