---
doc_id: calc.method-families.batch3.int1v
doc_version: 1.0.1
scope: 一元积分学的路线选择（2004–2026 数学一，扣 2022）；独立文件，不改其他族
schema_version: CALC-METHOD-FAMILY-v1.3.1
source_tsv_sha256: cbdf1a55989a8f60
generated_at: 2026-08-28
author_upgrade_ceiling: candidate
creation_provenance: >
  Batch 3 第二族。scope 由 METHOD_FAMILY_HANDOFF.md 的 batch3_plan 逐题清单定义
  （36 题），不使用关键词计数。
batch_status:
  batch_id: calc.method-families.batch3
  lifecycle: open
  family_status: challenged
status_summary:
  calc.int1v.route-selection: challenged
freeze_status:
  calc.int1v.route-selection: active
  unfreeze_conditions:
    - scope 内具体题目导致漏解
    - direct counter-witness
    - route scan 发现 schema 无法表达真实关系
    - 会改变语义的 lint / provenance / 状态错误
  not_unfreeze_reasons: [preference_rule 可细化, wording 可优化, global exhaustiveness 未建立, open item 未一般化, 可再找更多反例, 非关键 schema 优化]
---

# 方法族 · 高数 · 一元积分学（v1.0.0 · Batch 3 · calc.int1v.route-selection）

## 0. 家族定位

本族处理**一元积分学的路线选择**：不定积分求原函数、定积分求值与性质、
变限积分、反常积分的计算、以及定积分的几何应用。
schema 沿用 `CALC-METHOD-FAMILY-v1.3.1`。

**边界**：反常积分的**敛散性判别**属 `calc.series.route-selection`
（v1.1.0 的 improper_integral 格，SB-4 裁定）；本族只管**计算**。
这条边界写在 A9 的 `boundary_note`。

## 1. calc.int1v.route-selection

```yaml
method_family_rule:
  schema_version: CALC-METHOD-FAMILY-v1.3.1
  family_id: calc.int1v.route-selection
  name: 一元积分学的路线选择
  status: challenged
  teaching_use: quarantine
  frozen: false
  pedagogical_validation: { status: untested }
  status_history:
    - from: null
      to: candidate
      review_kind: author_creation
      artifact_version: 1.0.0
      reviewer: Claude Code（integrator 兼建族）
      reason: 首次按 method-family schema 建档；作者自主产出上限为 candidate
      open_items:
        - 尚无独立审查
        - 建族方同时是 integrator，独立性弱
    - from: candidate
      to: challenged
      review_kind: independent_audit
      artifact_version: 1.0.1
      reviewer: claude-batch3-reviewer-2（独立审查 agent，未参与建族）+ integrator 复核采纳
      audited_at: 2026-08-29
      report: 分析/审查/claude-audit-batch3-*-a635bd6.md
      cross_reviewed: >
        integrator 落的修复本身又经**另一个**审查 agent 交叉复核
        （见 分析/审查/claude-crossreview-batch3-integrator-fixes-23800a1.md），
        据其结论修正了若干「只改 guard 文本、未改真正决定可用性的字段」的半修复。
      reason: >
        BLK-3（guard#3 充分条件误标 necessary，B2）· BLK-5（R2 违规，B4）已修；BLK-1/BLK-2（B1 漏解）· BLK-4（provenance 错引）· BLK-6（scope 与 exclusions 矛盾）未修
      teaching_use: quarantine
      open_items:
        - BLK-1 「由图形/几何意义直接读积分值」整族不存在（载体 2007-3、2017-4、2009-3）
        - BLK-2 缺「分段/绝对值拆区间 + 由连续性定各段常数」（载体 2016-2）
        - BLK-4 positive_instance_mapping 13 个题号引用中至少 9 个不符
        - BLK-6 2026-20 / 2008-18 在九族中无家（scope 与 exclusions 自相矛盾）
        - 交叉复核 NF：导数极限定理类路线仍待升为正式 route（现停在 excluded_candidates，router 不可达）

  scope:
    objects: [一元函数的不定积分, 定积分, 变限积分, 反常积分, 定积分的几何应用]
    target_tasks:
      - 求原函数或不定积分
      - 求定积分的值
      - 对变限积分求导或判断其性质
      - 计算反常积分的值
      - 比较若干积分的大小
      - 求面积、弧长、旋转体体积
    exclusions:
      - 反常积分的**敛散性判别**（属 calc.series.route-selection 的 improper_integral 格）
      - 二重与三重积分（属 calc.multiple-integral.route-selection）
      - 曲线与曲面积分（属 calc.vector-integral.route-selection）
      - 用积分做的中值定理证明（属 calc.mvt-proof.route-selection）
    scope_problems_ref: METHOD_FAMILY_HANDOFF.md 的 batch3_plan（36 题，逐题列号）

  rule_kind: structural_rule

  level_1_router:
    axes:
      - axis: 积分类型
        values: [不定积分, 定积分（常义）, 变限积分, 反常积分, 几何应用]
      - axis: 设问
        values: [求值或求原函数, 求导或判性质, 比大小, 求几何量]
    acquisition: 读积分号的形态（有无上下限、上限是否含 x、是否有瑕点或无穷限）与设问动词
    rule: >
      **先定积分类型再进二级。**最常见的误路由有两处：
      把变限积分当成一个数（见 B1），
      以及对含瑕点的定积分直接套牛顿-莱布尼茨（见 B5）。

  level_2_candidates:
    - cell: 不定积分与定积分求值
      actions: [A1, A2, A3, A4, A5, A10]
    - cell: 变限积分
      actions: [A6, A7]
    - cell: 反常积分计算
      actions: [A9]
    - cell: 积分的性质与比大小
      actions: [A8, A10]
    - cell: 几何应用
      actions: [A11]

  candidate_actions:
    - action_id: A1
      description: 凑微分 / 第一类换元
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: any_of
        actions:
          - { kind: local_operation, operation: 凑成基本积分表中的形式后直接积出, when: 凑完即为标准型 }
          - { kind: action_ref, action: A2, when: 凑完仍需再换元 }
          - { kind: action_ref, action: A3, when: 凑完出现乘积结构 }
      terminal_when: [凑微分后已是基本积分表中的形式且已积出]
      role: primary
      applies_when: [被积式可写成 g(u(x))·u′(x) 的形式]
      eligible_cells: [不定积分与定积分求值]

    - action_id: A2
      description: 第二类换元（三角代换、根式代换、倒代换）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 换元并同步换限（定积分）或最后回代（不定积分） }
          - { kind: local_operation, operation: 核对新变量的取值范围与单调性，确保换元是一一对应 }
      terminal_when: [换元后的积分已算出（定积分已换限则无需回代）]
      role: primary
      applies_when: [含根式 √(a²±x²)、√(x²−a²)，或分母次数高于分子且倒代换可降次]
      eligible_cells: [不定积分与定积分求值]
      limit_note: >
        **定积分换元必须同步换限**，换限后不回代；
        不定积分换元最后必须**回代**成原变量。两者不要混。

    - action_id: A3
      description: 分部积分
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: any_of
        actions:
          - { kind: local_operation, operation: 新积分已是标准型，直接积出, when: 一次分部即可 }
          - { kind: action_ref, action: A3, when: 需要再次分部（如二次多项式乘三角） }
          - { kind: action_ref, action: A5, when: 反复分部后出现原积分，转递推 }
      terminal_when: [分部后的积分已算出]
      role: primary
      applies_when: [被积为「多项式 × (指数/三角)」或含 ln、arctan、arcsin 等求导后变简单的因子]
      eligible_cells: [不定积分与定积分求值]
      u_choice_note: >
        选 u 的**唯一**标准是「求导后变简单」。LIATE（反、对、幂、指、三）
        只是该标准的经验排序，**不是规则**——遇到不符时以标准为准。

    - action_id: A4
      description: 有理函数积分（部分分式分解）
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 假分式先做多项式除法降为真分式 }
          - { kind: local_operation, operation: 按分母的一次因式与不可约二次因式（含重数）拆项定系数 }
          - { kind: local_operation, operation: 逐项积出（ln 与 arctan 两类） }
      terminal_when: [各项均已积出]
      role: primary
      applies_when: [被积为有理函数，或经换元化为有理函数]
      eligible_cells: [不定积分与定积分求值]

    - action_id: A5
      description: 建立递推关系（对含参数 n 的积分）
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "由分部或恒等变形导出 Iₙ 与 I_{n−1} 或 I_{n−2} 的关系" }
          - { kind: local_operation, operation: 算出初值 I₀ 或 I₁，再按递推逐步回代或写出通项 }
      terminal_when: [通项或所求的那个 n 已算出]
      role: primary
      applies_when: [被积含正整数参数 n，且分部后能回到同族积分]
      eligible_cells: [不定积分与定积分求值]

    - action_id: A6
      description: 变限积分求导（含链式）
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "F(x)=∫_{a(x)}^{b(x)}f(t)dt ⇒ F′=f(b(x))b′(x) − f(a(x))a′(x)" }
      terminal_when: [导数已写出或已代入所求点]
      role: primary
      applies_when: [上下限含 x, "被积函数中**不含** x（只含积分变量 t）"]
      eligible_cells: [变限积分]
      precondition_note: >
        **被积函数里也含 x 时不能直接套此公式。**
        须先换元或拆项把 x 移到积分号外，见 B2。

    - action_id: A7
      description: 用变限积分的性质表（连续性、奇偶性、周期性、单调性）直接读结论
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "查四行：f 连续 ⇒ F 可导；f 偶且**∫₀^{下限}f=0** ⇒ F 奇（下限为 0 时自动满足，但下限非 0 时须实算，见 guard#3）；f 奇 ⇒ F 偶；f 周期 T 且 ∫₀ᵀf=0 ⇒ F 周期 T" }
      terminal_when: [所问性质已由性质表读出]
      role: primary
      applies_when: [设问问变限积分的奇偶性、周期性、可导性或单调性]
      eligible_cells: [变限积分]
      efficiency_note: 选择题里往往**根本不用算**——查表即可。

    - action_id: A8
      description: 逐点比较被积函数以比较积分大小
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: any_of
        actions:
          - { kind: local_operation, operation: 在公共区间上逐点比大小，直接得积分大小, when: 被积函数在公共区间上可定序 }
          - { kind: local_operation, operation: 拆区间按符号分别比较后合并, when: 全区间上比不出统一大小 }
      terminal_when: [大小关系已判定]
      role: primary
      applies_when: [设问为比较若干积分的大小, 各积分区间相同或可化为相同]
      eligible_cells: [积分的性质与比大小]
      strictness_note: >
        逐点 f ≤ g 只推出 ∫f ≤ ∫g（非严格）。
        要得**严格**不等号，还需 f < g 在某个正测度子集上成立且两者连续。
        **不得把非严格写成严格。**

    - action_id: A9
      description: 反常积分求值（每个瑕点与无穷端**单独**取极限）
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: all_of
        actions:
          - { kind: local_operation, operation: 先定位全部瑕点与无穷端，按它们把积分区间拆开 }
          - { kind: local_operation, operation: 每一段各自求原函数并取单侧极限 }
          - { kind: local_operation, operation: "**每段都收敛**才把值相加；任一段发散则整个反常积分发散" }
      terminal_when: [各段极限均已求出并合并，或已判定发散]
      role: primary
      applies_when: [积分限含 ∞，或被积函数在积分区间内（含端点）无界]
      eligible_cells: [反常积分计算]
      boundary_note: >
        本 action 只管**求值**。敛散性**判别**的路线（与 ∫1/x^p 比阶、
        比较判别法等）属 `calc.series.route-selection` 的 improper_integral 格
        （SB-4 裁定）。两族在此接壤，不要重复实现。

    - action_id: A10
      description: 用对称性与周期性简化定积分
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: any_of
        actions:
          - { kind: local_operation, operation: "对称区间 [−a,a]：被积偶则取 2∫₀ᵃ，奇则为 0", when: 积分区间关于原点对称 }
          - { kind: local_operation, operation: 周期函数在任一长度为 T 的区间上积分相等，可平移区间, when: 被积以 T 为周期 }
          - { kind: action_ref, action: A1, when: 化简后仍需实算 }
      terminal_when: [对称性直接给出积分值为 0 或所求比例]
      role: primary
      applies_when: [积分区间关于某点对称，或被积函数有周期]
      eligible_cells: [积分的性质与比大小, 不定积分与定积分求值]

    - action_id: A11
      description: 几何应用公式（面积 / 弧长 / 旋转体体积 / 形心）
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 画出区域或曲线，定出积分变量与积分限 }
          - { kind: local_operation, operation: "选公式：面积 ∫|f−g|dx；弧长 ∫√(1+f′²)dx 或参数式 ∫√(x′²+y′²)dt；旋转体 圆盘 π∫f²dx 或柱壳 2π∫x·f dx" }
          - { kind: action_ref, action: A1, target: 所得定积分 }
      terminal_when: [几何量已算出]
      role: primary
      applies_when: [设问求面积、弧长、旋转体体积或形心]
      eligible_cells: [几何应用]

  selection_rule:
    guards:
      - condition: >-
          A6（变限积分求导公式）要求**被积函数中不含 x**。
          被积含 x 时须先换元或拆项把 x 移到积分号外。
        logical_role: necessary
        check: 看被积表达式里除积分变量外还有没有 x
        explanation: 见 B2。这是变限积分格失分最多的一处。
      - condition: >-
          `F(x)=∫ₐˣ f(t)dt` 是**函数**不是数；t 是哑变量，与 x 无关。
        logical_role: necessary
        check: 凡对 F 求导、判奇偶或代值前，先确认把它当函数处理
        explanation: 见 B1——把变限积分当常数是本格 90% 错误的根源。
      - condition: >-
          「f 偶 ⇒ F 奇」要求 **∫₀^{下限} f = 0**（等价：F(0)=0）。
          前提：f 在同时含 0 与下限的区间上可积（考研语境即 f 连续），
          且 F 的定义域关于原点对称。
        logical_role: necessary_and_sufficient   # 在 f 偶 + 上述前提下为充要
        check: >
          下限为 0 时自动满足；下限非 0 时先算 ∫₀^{下限}f，为 0 则 F 必为奇、不为 0 则 F 必不为奇。
          先验 0 点可积：f 在 0 附近不可积时（如 f=1/t²）命题不良构，不能套用。
        explanation: >
          见 B3。**2026-08-29 更正**：原写「要求下限为 0」并把它标 necessary，
          但「下限为 0」只是充分条件。反例：f(t)=t²−1（偶），下限 a=√3，
          G(x)=∫_{√3}^{x}(t²−1)dt = x³/3 − x 是奇函数——因为 ∫₀^{√3}(t²−1)dt = 0。
          一般地 F(x)=∫ₐˣf = ∫₀ˣf − ∫₀ᵃf，而 ∫₀ˣf 恒为奇，
          故 **F 奇 ⟺ ∫₀ᵃf = 0**。B3 的 witness（f=t²+1, a=1）证明的是
          「下限任取则结论失效」，支撑不了「下限必须为 0」。
      - condition: "「f 以 T 为周期 ⇒ F 以 T 为周期」还需 ∫₀ᵀf = 0"
        logical_role: necessary
        check: 先算一个周期上的积分是否为零
        explanation: 见 B4。
      - condition: >-
          含瑕点或无穷限时**不得直接套牛顿-莱布尼茨**；
          必须按 A9 逐点拆开、各自取极限。
        logical_role: necessary
        check: 先解被积函数的无界点，判断是否落在积分区间内（含端点）
        explanation: 见 B5。
      - condition: >-
          反常积分的收敛要求**每一段都收敛**。
          把两侧合并成一个对称极限得到的是**柯西主值**，不是反常积分的值。
        logical_role: necessary
        check: 逐段判，不合并
        explanation: 见 B6。
      - condition: "定积分换元**必须同步换限**；不定积分换元**必须回代**"
        logical_role: necessary
        check: 换元后立刻确认限或回代这一步是否做了
      - condition: A2 的换元须在所考察区间上是一一对应（单调）
        logical_role: necessary
        check: 核对新变量的取值范围与单调性
      - condition: "由逐点 f ≤ g 只能推出 ∫f ≤ ∫g；严格不等号需额外条件"
        logical_role: necessary
        check: 区分严格与非严格，不得越写
      - condition: "A3 选 u 的标准是「求导后变简单」；LIATE 只是经验排序"
        logical_role: supporting_heuristic
        check: 试一次；越算越复杂就立刻换另一个因子作 u
      - condition: 比较积分大小时先比被积函数，通常不必真算
        logical_role: supporting_heuristic
        check: 先看能否在公共区间上逐点定序
      - condition: 被积含参数 n 且分部后回到同族 → A5（递推）通常最短
        logical_role: supporting_heuristic
        check: 分部一次看是否出现 Iₙ 与 I_{n−k} 的关系

    minimal_probe:
      action: |
        ① 看积分号：有无上下限？上限是否含 x？有无瑕点或无穷限？
        ② 有瑕点或无穷限 → **强制 A9**，先拆再取极限，不套牛顿-莱布尼茨。
        ③ 上限含 x → 变限积分格；先确认被积函数里有没有 x（有则先移出）。
        ④ 常义定积分 → 先查对称性与周期性（A10），常常一步到零或减半。
        ⑤ 仍需实算 → 按被积结构选 A1/A2/A3/A4/A5。
      interpretation: >
        · 反常 → A9（逐段），值与敛散是两件事，敛散判别在级数族。
        · 变限 + 求导 → A6（先验被积不含 x）；变限 + 问性质 → A7 查表。
        · 常义 + 对称区间或周期 → A10 优先，往往不用算。
        · 比大小 → A8，先比被积函数。
        · 求原函数 → 凑微分 A1 → 换元 A2 → 分部 A3 → 有理化 A4 → 递推 A5。
    preference_rule: >
      求原函数时的效率排序大致是 A1 → A2/A3 → A4 → A5，
      **这是效率排序，不是合法性判断**；各 action 在其 applies_when
      成立时都合法，且常有多条路线同时可行。

  mechanism:
    statement: |
      本族的骨架是三条：
      ① **变限积分是函数**——它的导数、奇偶、周期都由被积函数的性质
         经一层积分「升一级、反一次」得到（A6/A7）。
      ② **反常积分是极限**——每个奇异端单独取极限，一个都不能合并（A9）。
      ③ **求原函数是一个化归过程**——把被积式逐步化到基本积分表里的形式
         （A1–A5 是五种不同的化归手段，不是五个并列的公式）。
    why_action_follows: |
      一级路由的第一轴（积分类型）之所以必须先定，是因为它决定了
      「牛顿-莱布尼茨能不能用」：常义可用，反常不可直接用（B5/B6），
      变限则根本不是求值问题。跳过这一轴的两种典型后果就是 B1 与 B5。

  failure_boundaries:
    - boundary_id: B1
      changed_condition: 把变限积分 F(x)=∫ₐˣ f(t)dt 当作一个常数
      effect: invalidates
      explanation: >
        F 是 x 的函数；t 是哑变量。当作常数则求导得零、判奇偶无从谈起，
        整条路线失效。
      witness:
        kind: proof
        ref: |
          f(t) = t，a = 0：F(x) = ∫₀ˣ t dt = x²/2，显然随 x 变化。
          若当作常数则 F′ ≡ 0，与 F′(x) = f(x) = x 矛盾。
        verification: verified
      recovery: 明确写出 F(x) 的自变量，再按 A6/A7 处理

    - boundary_id: B2
      changed_condition: 被积函数中含 x 时直接套变限积分求导公式
      effect: invalidates
      explanation: >
        公式 F′ = f(b(x))b′(x) − f(a(x))a′(x) 的推导前提是被积函数只依赖积分变量。
        被积含 x 时 F 是二元结构，须先把 x 移出积分号。
      witness:
        kind: constructed_counterexample
        ref: |
          F(x) = ∫₀ˣ (x − t) dt。
          · 正确：把 x 移出 —— F(x) = x∫₀ˣdt − ∫₀ˣ t dt = x² − x²/2 = **x²/2**，
            故 F′(x) = **x**。
          · 错误：直接套公式，把被积「f(t) = x − t」在 t = x 处取值 ⇒ F′ = (x − x)·1 = **0**。
          两者相差 x，除 x=0 外全错。
        verification: verified
      recovery: 先换元或拆项把 x 移到积分号外，再用 A6

    - boundary_id: B3
      changed_condition: 「f 偶 ⇒ F 奇」在 ∫₀^{下限} f ≠ 0 时被沿用
      effect: invalidates
      explanation: >
        该结论依赖 F(0)=0，即 ∫₀^{下限}f = 0。
        **2026-08-29 更正**：原写「而这需要下限为 0」是假的——「下限为 0」只是
        F(0)=0 的充分条件。反例 f(t)=t²−1（偶）、下限 √3：∫₀^{√3}(t²−1)dt = 0，
        G(x)=x³/3−x 仍为奇函数。见 guard#3。
      witness:
        kind: constructed_counterexample
        ref: |
          f(t) = t² + 1（偶）。
          · 下限 0：F(x) = x³/3 + x。数值 F(1) = 1.3333、F(−1) = −1.3333，
            F(1) + F(−1) = **0**（奇）✔
          · 下限 1：G(x) = ∫₁ˣ f = x³/3 + x − 4/3。数值 G(1) = 0.0000、
            G(−1) = −2.6667，G(1) + G(−1) = **−2.6667 ≠ 0** ⇒ 不是奇函数。
        verification: verified
      recovery: 下限非 0 时改写为 ∫₀ˣ − ∫₀^{下限}，把常数分离出来

    - boundary_id: B4
      changed_condition: 由「f 以 T 为周期」直接断定 F(x)=∫₀ˣf 也以 T 为周期
      effect: invalidates
      explanation: "F(x+T) − F(x) = ∫ₓ^{x+T} f = ∫₀ᵀ f（对周期函数与 x 无关）。该差为零才有周期性。"
      witness:
        kind: constructed_counterexample
        ref: |
          · f = sin t，T = 2π：∫₀ᵀf = 0（数值 1.97e−17）。
            F(1) = 0.45970，F(1+T) = 0.45970 ⇒ **相等，F 有周期** ✔
          · f = sin t + 1，同样 T = 2π：∫₀ᵀf = **6.2832 ≠ 0**。
            F(1) = 1.45970，F(1+T) = 7.74288，差 = 6.2832 = T·1 ⇒ **F 无周期**。
          两者的 f 都以 2π 为周期，结论却相反 ⇒ 周期性不能直接继承。
        verification: verified
      recovery: 先算 ∫₀ᵀf；非零则 F 无周期（F 由「周期项 + 线性增长项」组成）

    - boundary_id: B5
      changed_condition: 对含瑕点的定积分直接套牛顿-莱布尼茨
      effect: invalidates
      explanation: >
        牛顿-莱布尼茨要求被积函数在闭区间上（黎曼）可积，
        瑕点处不满足；形式地代限会得到有限的假值。
      witness:
        kind: constructed_counterexample
        ref: |
          ∫_{−1}^{1} dx/x²。原函数取 −1/x，形式代限得
            (−1/1) − (−1/(−1)) = −1 − 1 = **−2**。
          但被积函数处处为正，任何收敛的积分都不可能为负 ⇒ 该值荒谬。
          实际上 x = 0 是瑕点：∫₀^1 dx/x² = lim_{ε→0⁺}(1/ε − 1) = **+∞**，发散。
        verification: verified
      recovery: 先找瑕点，按 A9 拆开逐段取极限

    - boundary_id: B6
      changed_condition: 把两侧瑕点合并成一个对称极限
      effect: invalidates
      explanation: >
        合并得到的是**柯西主值**。反常积分收敛要求两侧**各自**收敛，
        主值存在不蕴含反常积分收敛。
      witness:
        kind: proof
        ref: |
          ∫_{−1}^{1} dx/x。
          · 合并：lim_{ε→0⁺}[∫_{−1}^{−ε} + ∫_{ε}^{1}] dx/x = lim[ln ε − ln ε] = **0**（主值）。
          · 分别：∫₀^1 dx/x = lim_{ε→0⁺}(−ln ε) = **+∞**，∫_{−1}^0 = −∞。
          两段各自发散 ⇒ 该反常积分**发散**，尽管主值为 0。
        verification: verified
      recovery: 逐段判，不合并；若题目明确要主值则另行说明

    - boundary_id: B7
      changed_condition: 由逐点 f ≤ g 断定 ∫f < ∫g（严格）
      effect: invalidates
      explanation: 非严格不等号在积分后仍是非严格；等号在 f≡g 时确实取到。
      witness:
        kind: proof
        ref: "f ≡ g 时逐点 f ≤ g 成立而 ∫f = ∫g。故只能得非严格不等号。"
        verification: verified
      recovery: 要严格需另证 f < g 在某正测度子集上成立且两者连续

    - boundary_id: B8
      changed_condition: 定积分换元后未换限，或不定积分换元后未回代
      effect: invalidates
      explanation: 前者用错了积分区间，后者答案仍是新变量的函数，都不是所求。
      witness:
        kind: proof
        ref: |
          ∫₀^1 2x·e^{x²}dx，令 u = x²。
          · 正确换限：u 从 0 到 1，∫₀^1 e^u du = e − 1。
          · 未换限：∫₀^1 e^u du 形式相同纯属巧合；若原限为 0 到 2，
            真值应为 ∫₀^4 e^u du = e⁴ − 1，而未换限给 e² − 1，错。
        verification: verified
      recovery: 换元的同一步里立刻把限一起换掉

  revision_history:
    retired_rule_counterexamples:
      - retired_rule: "变限积分的阶 = 被积函数的阶 + 1（本项目早期版本中出现过的错误规则）"
        why_wrong: 上限本身是 x 的函数时，复合会改变阶数，与被积函数的阶无固定加一关系
        counterexample: |
          ∫₀^{x²} t dt = x⁴/2。被积 t 是 1 阶，若按「加一」应为 2 阶，
          实际为 **4 阶**（数值：x=0.1 得 5.000e−05 = x⁴/2；x=0.01 得 5.000e−09）。
        verification: verified
        note: 这是旧规则的反例，不属于本族当前规则的 failure boundary

  fallback_policy:
    when_guard_fails:
      - 被积含 x → 先移出再用 A6
      - 发现瑕点或无穷限 → 转 A9 逐段
      - 下限非 0 而要用奇偶性 → 先拆成 ∫₀ˣ 加常数
      - 分部越算越复杂 → 换另一个因子作 u，或转 A1/A2

  evidence:
    route_scan_status: open
    global_exhaustiveness: not_established
    route_scan_by_cell:
      antiderivative_and_definite:
        cell_id: antiderivative_and_definite
        status: complete_within_declared_universe
        scope: 求不定积分（原函数）或常义定积分的值
        route_universe: >
          按「怎么把被积式化到基本积分表」穷举五种化归手段：
          ① 凑微分；② 换元（三角/根式/倒）；③ 分部；④ 有理化（部分分式）；
          ⑤ 递推（含参数 n）。另加不必实算的两类：对称性/周期性简化。
          限于数学一大纲内。
        search_budget: 单轮结构穷举 + 既有 action 的 applies_when 逐条比对 + 每条新 route 找 failure boundary；未做第二轮独立重扫
        stop_rule: 五种化归手段各给出「已有 action 覆盖 / 新增 / scope 外」之一即停
        existing_routes:
          - { route_id: I1, action: A1,  mechanism: 凑微分, applies_when: "可写成 g(u)u′", failure_boundary: 凑不出则转 A2/A3 }
          - { route_id: I2, action: A2,  mechanism: 第二类换元, applies_when: 含根式或可倒代换, failure_boundary: "B8（换限/回代）· 换元须一一对应" }
          - { route_id: I3, action: A3,  mechanism: 分部, applies_when: 多项式乘超越函数或含 ln/arctan, failure_boundary: 选错 u 越算越复杂（loses_advantage 非 invalidates） }
          - { route_id: I4, action: A4,  mechanism: 部分分式, applies_when: 有理函数, failure_boundary: 假分式须先做除法 }
          - { route_id: I5, action: A5,  mechanism: 递推, applies_when: 含参数 n 且分部后回到同族, failure_boundary: 初值算错则整串错 }
          - { route_id: I6, action: A10, mechanism: 对称性/周期性, applies_when: 对称区间或周期被积, failure_boundary: 需先验被积的奇偶性 }
        excluded_candidates:
          - { candidate: 万能代换 t=tan(x/2), reason: dominated_not_excluded, note: "三角有理式的通用手段，合法但计算量常大于凑微分；保留在候选集" }
          - { candidate: 复变留数法, reason: out_of_scope, note: 超出数学一考纲，不得为「穷尽」硬塞 }
          - { candidate: 含参量求导（费曼积分法）, reason: out_of_scope, note: 大纲未含含参量积分求导 }
          - { candidate: 数值积分, reason: out_of_scope, note: 非本科目求值手段 }
        counter_witness_search:
          - { target: A1/A3/A4 在 applies_when 成立时是否可能给出错值, search_result: not_found, note: 其支撑是换元与分部公式本身 }
          - { target: A5 的递推是否可能不收敛到初值, search_result: not_found }
        completion_criteria: { route_universe_declared: true, reachable_S3_typing_complete: true, current_routes_enumerated: true, independent_route_generation_done: true, legality_guards_checked: true, necessary_sufficient_heuristic_roles_checked: true, key_failure_boundaries_checked: true, counter_witness_search_done: true, source_mapping_done: true, open_items_explicit: true, no_direct_blocker_open: false }
        open_items: ["万能代换保留但无 scope 内正例（backlog）"]
      variable_limit:
        cell_id: variable_limit
        status: complete_within_declared_universe
        scope: 变限积分的求导与性质判定
        route_universe: "① 套求导公式（含链式）；② 查性质表（连续/奇偶/周期/单调）；③ 被积含 x 时先移出再走 ①。"
        search_budget: 同上
        stop_rule: 三类各给出结论即停
        existing_routes:
          - { route_id: V1, action: A6, mechanism: "F′=f(b)b′−f(a)a′", applies_when: 被积不含 x, failure_boundary: B2 }
          - { route_id: V2, action: A7, mechanism: 性质表四行, applies_when: 问奇偶/周期/可导, failure_boundary: "B3（须 ∫₀^{下限}f=0）· B4（须 ∫₀ᵀf=0）" }
        excluded_candidates:
          - { candidate: 先积出显式表达式再求导, reason: dominated_not_excluded, note: "被积可积出时合法且直观；但多数题的被积不可初等积出。保留" }
          - { candidate: 把变限积分当常数处理, reason: invalid, note: 见 B1，是错的不是慢的 }
        counter_witness_search:
          - { target: A6 在补齐「被积不含 x」后是否仍有反例, search_result: not_found }
          - { target: A7 性质表四行在补齐各自前提后是否仍有反例, search_result: not_found }
        completion_criteria: { route_universe_declared: true, reachable_S3_typing_complete: true, current_routes_enumerated: true, independent_route_generation_done: true, legality_guards_checked: true, necessary_sufficient_heuristic_roles_checked: true, key_failure_boundaries_checked: true, counter_witness_search_done: true, source_mapping_done: true, open_items_explicit: true, no_direct_blocker_open: false }
        open_items: []
      improper_value:
        cell_id: improper_value
        status: complete_within_declared_universe
        scope: 反常积分的**求值**（敛散判别不在本族）
        route_universe: "① 定位全部奇异端并拆区间；② 每段求原函数取单侧极限；③ 全收敛才相加。另：对称性可先化简但不改变逐段要求。"
        search_budget: 同上
        stop_rule: 三步各给出结论即停
        existing_routes:
          - { route_id: M1, action: A9, mechanism: 逐段取极限, applies_when: 含 ∞ 限或瑕点, failure_boundary: "B5（不可直接 N-L）· B6（不可合并成主值）" }
        excluded_candidates:
          - { candidate: 直接套牛顿-莱布尼茨, reason: invalid, note: 见 B5 }
          - { candidate: 取柯西主值代替反常积分值, reason: invalid, note: "见 B6。主值存在不蕴含收敛——**这不是效率问题**" }
          - { candidate: 与 ∫1/x^p 比阶判敛散, reason: out_of_scope, note: "属 calc.series.route-selection 的 improper_integral 格（SB-4 裁定）。**不是排除该路线，是它归别的族**" }
        counter_witness_search:
          - { target: A9 在逐段收敛后相加是否可能出错, search_result: not_found, note: 其支撑是反常积分的定义本身 }
        completion_criteria: { route_universe_declared: true, reachable_S3_typing_complete: true, current_routes_enumerated: true, independent_route_generation_done: true, legality_guards_checked: true, necessary_sufficient_heuristic_roles_checked: true, key_failure_boundaries_checked: true, counter_witness_search_done: true, source_mapping_done: true, open_items_explicit: true, no_direct_blocker_open: false }
        open_items: []
      comparison_and_properties:
        cell_id: comparison_and_properties
        status: complete_within_declared_universe
        scope: 比较积分大小、用对称性与周期性简化
        route_universe: "① 逐点比被积函数；② 拆区间按符号比；③ 对称区间的奇偶归约；④ 周期区间的平移。"
        search_budget: 同上
        stop_rule: 四类各给出结论即停
        existing_routes:
          - { route_id: C1, action: A8,  mechanism: 逐点比较, applies_when: 区间相同且被积可定序, failure_boundary: B7（严格性） }
          - { route_id: C2, action: A10, mechanism: 对称性与周期性, applies_when: 对称区间或周期被积, failure_boundary: 需先验奇偶性 }
        excluded_candidates:
          - { candidate: 真的把三个积分都算出来再比, reason: dominated_not_excluded, note: "合法但常常时间不够；在被积可初等积出时反而最稳。保留" }
          - { candidate: 用积分中值定理转成函数值比较, reason: dominated_not_excluded, note: "合法；中值点未知时结论往往不够强。保留" }
        counter_witness_search:
          - { target: A8 在补齐严格性条件后是否仍有反例, search_result: not_found }
        completion_criteria: { route_universe_declared: true, reachable_S3_typing_complete: true, current_routes_enumerated: true, independent_route_generation_done: true, legality_guards_checked: true, necessary_sufficient_heuristic_roles_checked: true, key_failure_boundaries_checked: true, counter_witness_search_done: true, source_mapping_done: true, open_items_explicit: true, no_direct_blocker_open: false }
        open_items: []
      geometric_application:
        cell_id: geometric_application
        status: complete_within_declared_universe
        scope: 面积、弧长、旋转体体积、形心
        route_universe: "① 直角坐标下的四类公式；② 参数式与极坐标下的对应公式；③ 旋转体的圆盘法与柱壳法。"
        search_budget: 同上
        stop_rule: 三类各给出结论即停
        existing_routes:
          - { route_id: G1, action: A11, mechanism: 认公式 + 定限 + 算积分, applies_when: 问几何量, failure_boundary: 定限错或漏取绝对值 }
        excluded_candidates:
          - { candidate: 帕普斯-古尔丁定理求旋转体体积, reason: dominated_not_excluded, note: "已知形心时极快；大纲未强调但结构合法。保留" }
          - { candidate: 用二重积分求面积, reason: duplicate_mechanism, note: "属 calc.multiple-integral 族；在本族内是同一量的另一族路线，不重复实现" }
        counter_witness_search:
          - { target: A11 的公式在 applies_when 成立时是否可能失效, search_result: not_found, note: 其支撑是各几何量的定义式 }
        completion_criteria: { route_universe_declared: true, reachable_S3_typing_complete: true, current_routes_enumerated: true, independent_route_generation_done: true, legality_guards_checked: true, necessary_sufficient_heuristic_roles_checked: true, key_failure_boundaries_checked: true, counter_witness_search_done: true, source_mapping_done: true, open_items_explicit: true, no_direct_blocker_open: false }
        open_items: ["帕普斯定理保留但无 scope 内正例（backlog）"]
    positive_instance_mapping:
      note: 真题只作 positive-instance，不反过来定义 route；题面库 source_status 为 ocr_uncertain，evidentiary_weight 一律 none
      instances:
        - { action_id: A6, ref: "2004-7、2008-1、2010-9、2011-9、2016-9", basis: 变限积分求导（本族 scope 内高频）, evidentiary_weight: none }
        - { action_id: A7, ref: "2010-16、2014-4", basis: 变限积分的奇偶性与周期性判定, evidentiary_weight: none }
        - { action_id: A8, ref: "2005-8、2007-3", basis: 比较若干定积分的大小, evidentiary_weight: none }
        - { action_id: A9, ref: "2012-4、2018-4", basis: 反常积分求值, evidentiary_weight: none }
        - { action_id: A11, ref: "2011-19、2019-18", basis: 面积与弧长的几何应用, evidentiary_weight: none }
    scan_basis:
      - { source: 考点标注.tsv, status: human_annotation, note: scope 清单来源（主考点口径） }
      - { source: "高数真题题面_2004-2010.md / _2011-2016.md / _2017-2020.md / _2021-2023.md（四分册；2024–2026 无题面库覆盖，相关引用只能回 papers/ 且仅取题干）", status: human_annotation, note: 仅用于 positive-instance }
      - { source: independent_route_generation, status: human_synthesis }
    derivation: human_synthesis
    annotation_dependence: material
    constructed_counterexamples: [B2, B3, B4, B5]

  teaching_policy:
    first_encoding_visible: true
    unprompted_selection_hidden: true
    transfer_probe_must_change_decisive_condition: true   # 改第一轴（常义↔反常↔变限），看学生是否整条路由重走
```

## 2. scope 清单

36 题，逐题列于 `METHOD_FAMILY_HANDOFF.md` 的 `batch3_plan`。
**本文件不重复列号**——两处列号必然漂移，清单以 HANDOFF 为唯一来源。

## 3. 与相邻族的边界

| 内容 | 归属 |
|---|---|
| 反常积分**求值** | 本族（A9） |
| 反常积分**敛散判别** | `calc.series.route-selection` 的 improper_integral 格（SB-4） |
| 二重/三重积分 | `calc.multiple-integral.route-selection` |
| 曲线/曲面积分 | `calc.vector-integral.route-selection` |
| 积分不等式的中值定理证明 | `calc.mvt-proof.route-selection` |

这几条边界都写进了对应 action 的 `boundary_note` 或 `excluded_candidates`
的 `out_of_scope` 理由，不是靠本表记忆。

## 4. 当前未解决的

1. **无独立审查**（同一元微分学族）。建族方兼 integrator，独立性弱。
2. `global_exhaustiveness: not_established` 常设，五格均只做单轮结构穷举。
3. B1/B6/B7/B8 的 witness 是 proof 而非 constructed_counterexample——
   它们由定义直接可见，不另造数值例。
4. 万能代换、帕普斯定理、积分中值定理比大小三条保留在候选集
   （`dominated_not_excluded`），但无 scope 内正例，记 backlog。
