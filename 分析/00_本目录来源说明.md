# 本目录的来源与合并方式

生成日期：2026-09-07。本文件只说明 `分析/` 这一份是**怎么拼出来的**，不含任何分析内容。
内容入口见 `10_高等数学_资料与覆盖索引.md`。

## 为什么需要这份说明

`分析/` 此前不存在于 `main`（停在 2026-07-24），只散落在十余个工作分支上，
各分支持有的文件数从 5 到 95 不等。本次把它收拢到一处。

## 合并口径

| 来源分支 | head 日期 | 取了什么 |
|---|---|---|
| `claude/batch3-parallel-family-audit-ekzi6c` | 2026-08-30 | `分析/` 全部 95 个文件作为基线；另取 `CLAUDE.md`、`.claude/commands/analyze.md` |
| `claude/multivariable-calculus-review-p26i3f` | 2026-09-06 | 覆盖 5 个文件（见下） |

从 p26i3f 覆盖的 5 份：

- `方法族-高数-多元微分.md`（+71 / −21 行）
- `高数逐考点认知模型.md`（+1 / −1 行，differentiability 一行）
- `审查/README.md`
- `审查/claude-errata-multivar-differentiability-afd6c98.md`
- `审查/claude-errata-multivar-differentiability-afd6c98-补充1.md`

依据：该勘误文件 §0 自述其被审 head 即 `afd6c98`（batch3），且明确写明
「目标分支原先不含 `分析/`……未合并任何旧分支，只按需引入三份文件」。
故 batch3 打底、p26i3f 覆盖，是这两者唯一自洽的叠放次序。

## 已核验

- `分析/tests/lint_method_families.py`：PASS，error 0 · warning 4
  （4 条均为 `calc.extrema.constraint-selection` 的 followup 未标 kind，属既有待迁移项 S3）

## 未处理

- 其余 14 个分支的 `分析/` **未逐一比对**。它们文件数均少于 batch3，
  按时间与包含关系判断为中间态，但本次没有做逐文件 blob 核对——
  若某分支存在 batch3 之后的独立修订，本合并会漏掉它。
- `main` / `master` 未改动。本目录目前只存在于 `claude/new-session-ovg3ab`。
- Stage_A.md 引用的 `sources/11_线性代数_资料与覆盖索引.md` 在全库均不存在，
  只有高数的 `10_`。线代侧对应材料是 `线性代数方法速查.md` 与 `线性代数考法统计.md`。
