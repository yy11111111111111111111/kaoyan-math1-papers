---
doc_id: calc.method-families.batch2.series
doc_version: 1.1.0
scope: 级数与反常积分的敛散性判别及解路线选择（2004–2026 数学一，扣 2022）；独立文件，不改 batch1 artifact，也不改 ODE / 多元微分族
schema_version: CALC-METHOD-FAMILY-v1.3.1
source_tsv_sha256: cbdf1a55989a8f60
generated_at: 2026-08-28
author_upgrade_ceiling: candidate
creation_provenance: >
  本文件由 claude-series 在 batch2 派发下首次建档（独立文件，owner: claude-series，
  status: assigned）。基线 commit = 3981c9f（「划死 batch2 三族的 scope 边界」，
  现取 git rev-parse 一致）。v1.0.0：scope_problems 计划 33、主考点实测 27（差异见 §2）。
  v1.1.0（基线 d42ea02）依 HANDOFF 裁定 SB-3（采纳 27）与 SB-4（2010-3 / 2016-1
  归本族、scope 扩写为「级数与反常积分的敛散性判别」）扩写，实测 27 + 2 = **29**。
  ⚠️ SB-4 写「这两题已计入上述 27」与事实不符：27 是**排除**这两题算出的
  （v1.0.0 §2 的 27 题清单可逐条核对），正确总数为 29。见 §5 open_questions 第 1 条。
batch_status:
  batch_id: calc.method-families.batch2
  lifecycle: open
  family_status: delivered_candidate
status_summary:
  calc.series.route-selection: candidate
freeze_status:
  calc.series.route-selection: active
  unfreeze_conditions:
    - scope 内具体题目导致漏解
    - direct counter-witness
    - route scan 发现 schema 无法表达真实关系
    - 会改变语义的 lint / provenance / 状态错误
  not_unfreeze_reasons: [preference_rule 可细化, wording 可优化, global exhaustiveness 未建立, open item 未一般化, 可再找更多反例, 非关键 schema 优化]
---

# 方法族 · 高数 · 级数与反常积分（v1.1.0 · Batch 2 · calc.series.route-selection）

## 0. 家族定位

本族处理**级数与反常积分的解路线选择**：给定一个级数（数项 / 幂级数 / 傅里叶级数）
或一个反常积分，与一个设问（判敛散 / 求和 / 求收敛域 / 展开 / 由级数反推），
选择「走哪条路」。反常积分自 v1.1.0 起纳入（HANDOFF 裁定 SB-4），
理由是它与正项级数的判别机制同源：都是「与 1/x^p（或 1/n^p）比阶」。沿用
`CALC-METHOD-FAMILY-v1.3.1` 与两级 router：一级做**对象 × 设问**分类、不产生 solver，
二级在格内给候选动作。字段语义与 `方法族-高数-第一批.md` §0、`方法族-高数-微分方程.md`
§0 一致（S1 执行顺序、S2 optional_any_of 的 on_skip、S3 kind 标注）。

### 0.1 一级路由的两条设计决定

1. **设问轴（task）与对象轴同等重要，不是附属维度。**
   同一个幂级数，问「收敛域」走 D 格、问「和函数」走 G 格、问「由已知敛散反推」
   走 D5——三条是不同路由，不是一条路由的三个阶段。F15 给出了「判敛散 → 求和」
   这一步为什么是 `changes_branch` 而不是同一路线的延续。

2. **收敛域的端点判定被单独立为一个 action（D2），不并进求收敛半径（D1）。**
   D1 只产出 R 与开区间，D1 的 followup 以 `mode: sequence` **强制**转 D2——
   不是 `optional_any_of`，因为跳过端点判定不是「省一步」而是给出错误答案（F2）。
   同一个 R=1 可以对应 `(−1,1)`、`[−1,1)`、`[−1,1]` 三种收敛域，R 本身不决定收敛域。
   另有 F1：求 R 用的那一次比值判别法在端点必然给出 ρ=1，**该判别法在端点不给结论**，
   所以端点必须换判据重判，这是 `becomes_inconclusive` 而不是 `invalidates`。

### 0.2 scope 边界（遵守 HANDOFF 的 scope_boundary_rule）

归属一律按 `考点标注.tsv` 每行**考点列的第一个**（主考点）。次考点跨族不改变归属，
本族也不把次考点命中的题算进自己的 scope_problems。

- **认领**：主考点为 傅里叶级数的奇偶延拓 / 傅里叶级数的收敛定理 / 函数展开成幂级数 /
  幂级数的收敛半径与收敛域 / 幂级数系数递推与微分方程 / 数项级数求和 /
  正项级数敛散性的比较判别法 / 级数敛散性的判定与反例 / 绝对收敛与条件收敛 /
  逐项积分求和函数 —— 共 **27 题**（清单见 §2）。
- **不认领 2014-19**：主考点为「夹逼准则求极限」，归已冻结的
  `calc.limit.method-selection`；「正项级数敛散性的比较判别法」只是它的次考点。
- **认领 2019-3**：主考点为「级数敛散性的判定与反例」，属本族。
- **与多元微分族实测重叠 0 题**（HANDOFF 已实测），本族未认领任何多元微分主考点的题。
- **2007-20 / 2013-16 / 2020-17**（幂级数系数递推与微分方程）：ODE 族在其
  `exclusions` 中已把「级数-ODE 混合题的整体」判给本族，本族**接收**这三题，
  其中的 ODE 求解子步骤以 `action_ref` 之外的 `local_operation` 表达，不重复定义
  ODE 族的 action（两族不得对同一题给出竞争路由）。
- **2010-3 / 2016-1**（主考点「反常积分的敛散性判别」）：v1.0.0 曾按「主考点不是级数」
  不认领并写入 open_questions；**HANDOFF 裁定 SB-4 已判归本族**，族 scope 扩写为
  「级数与反常积分的敛散性判别」。v1.1.0 起认领，落入新增的 improper_integral 格。
  本族仍未改 TSV（那两行的主考点标注保持原样，归属由 HANDOFF 承载）。
- objects：数项级数（正项 / 交错 / 任意项）、幂级数、傅里叶级数、反常积分（无穷限与瑕积分）。
- target_tasks：判敛散（含绝对/条件收敛的区分）、求和（数项与和函数）、求收敛半径
  与收敛域、函数展开成幂级数、傅里叶级数的系数与某点的和、由级数反推
  （由已知敛散数据反推 R/收敛域、由展开式反读系数、由系数递推反推和函数）。

## 1. calc.series.route-selection

```yaml
method_family_rule:
  schema_version: CALC-METHOD-FAMILY-v1.3.1
  family_id: calc.series.route-selection
  name: 无穷级数的解路线选择（两级 router）
  status: candidate
  pedagogical_validation: { status: untested }
  status_history:
    - from: null
      to: candidate
      review_kind: author_creation
      artifact_version: 1.0.0
      reviewer: claude-series
      reason: 首次按 method-family schema 建档（Batch 2 独立文件），作者自主产出上限为 candidate
      open_items:
        - 九格 route scan 均待独立审核（cross_review_rule：不得由 claude-series 自审）
        - scope_problems 计划 33 实测 27，差异待 integrator 裁定（见 §2、§5）
        - 高数方法速查.md §9 与 高数逐考点认知模型.md 模块 9 对「逐项求导/积分方向判据」写法相反，见 §5
    - from: candidate
      to: candidate
      review_kind: author_scope_expansion
      artifact_version: 1.1.0
      reviewer: claude-series
      reason: >
        依 HANDOFF（d42ea02）裁定 SB-3 与 SB-4 扩写 scope：2010-3 / 2016-1 判归本族，
        族 scope 改为「级数与反常积分的敛散性判别」。新增 improper_integral 格
        （I1–I5）与 F16 / F17 两条 failure boundary。**status 未变，仍为 candidate。**
      open_items:
        - "SB-4 的「这两题已计入上述 27」有误，正确总数为 29（见 §5 第 1 条）"
        - improper_integral 格仅 2 道真题，独立审时值得重点复核
  scope:
    objects: [数项级数（正项/交错/任意项）, 幂级数, 傅里叶级数, 反常积分（无穷限与瑕积分）]
    target_tasks: [判敛散（含绝对收敛与条件收敛的区分）, 数项级数求和, 幂级数收敛半径与收敛域, 幂级数求和函数, 函数展开成幂级数, 傅里叶系数与某点的和, 反常积分的敛散性判别（含含参数的收敛条件）, 由级数反推（敛散数据反推 R/收敛域、展开式反读系数、系数递推反推和函数）]
    exclusions: [一致收敛性的独立判定（M 判别法等，数学一不考）, 反常积分的**计算/求值**（属一元积分模块；本族只判敛散）, 函数项级数的一般理论, 复变幂级数, 级数的渐近展开与阶估计, 主考点为极限/中值定理/定积分应用而只把级数当次考点的题（2004-18、2009-16、2014-19、2016-19、2019-17）]

  rule_kind: structural_rule

  level_1_router:
    axes:
      - axis: object_type
        values: [数项级数, 幂级数, 傅里叶级数, 反常积分]
      - axis: sign_structure
        values: [正项, 交错, 任意项]
        note: 仅数项级数与反常积分的被积函数有意义；幂级数在定点上退化为数项级数时借用本轴
      - axis: task
        values: [判敛散, 求和, 求收敛域, 展开, 反推]
      - axis: direction
        values: [正向, 逆向]
        note: 逆向 = 由级数的已知性质反推 R/系数/和函数
    acquisition: >
      ⓪ 先看对象是求和号还是积分号：积分号 → 反常积分（再分无穷限 / 瑕积分，
      两者可在同一题并存）；求和号 → 按①–④定轴。
      ① 看通项里有没有自变量 x：没有 → 数项级数；有且形如 aₙ(x−x₀)ⁿ → 幂级数；
      形如 aₙcos nx + bₙsin nx 或题面出现「展成正弦/余弦级数」→ 傅里叶级数。
      ② 数项级数再看符号：通项恒正 → 正项；含 (−1)ⁿ 且余下部分定号 → 交错；
      符号不定 → 任意项。
      ③ 读设问：判敛散 / 求和 / 求收敛域 / 展开成幂级数 / 由已知性质反推。
      ④ 判方向：题面给的是「级数」问「性质」为正向；给的是「性质或展开式」
      问「R、收敛域、系数、和函数」为逆向。
    rule: >
      必须先定这四根轴，再进入二级。**一级不选判别法、不选求和技巧。**
      分派：反常积分 → improper_integral；数项 × 正项 → numeric_positive；数项 × 交错或任意项 →
      numeric_alternating_general；数项 × 求和 → numeric_sum（与前两格是**不同格**，
      见 F15：「判敛散 → 求和」是 changes_branch，不是同一路线的后续）；
      幂级数 × 求收敛域 → power_domain；幂级数 × 求和 → power_sum（该格强制先经
      D1/D2 定出收敛域再求和）；函数展开成幂级数 → expansion；题面给系数递推式 →
      coefficient_recursion；傅里叶 → fourier。
      **逆向设问不单设格**：对象类型没变，只是推理方向反了，因此逆向 route
      作为 action 挂在各自对象格内（D5 / X5 / C1 / C3），与 ODE 族把
      A6/B7/B8 挂在对象格内的处理一致。

  level_2_candidates:
    - cell: 数项 · 正项
      cell_id: numeric_positive
      actions: [P0, P1, P2, P3, P4]
      note: >
        判别法族。P0 是必要条件筛查（通项不趋零即发散）。P1 定阶比较、P2 比值、
        P3 根值、P4 积分判别法。四条**不是**按历史频率排序的候选，
        选择依据是通项的结构（见 minimal_probe）。
    - cell: 反常积分 · 敛散性判别
      cell_id: improper_integral
      actions: [I1, I2, I3, I4, I5]
      note: >
        v1.1.0 新增（HANDOFF 裁定 SB-4）。I1 拆分奇点（**每个奇点单独判**，
        与 power_domain 的 D2 同构）、I2 瑕点定阶（p<1 收敛）、
        I3 无穷端定阶（p>1 收敛）、I4 对数因子的定阶、I5 绝对收敛判定。
        ⚠️ I2 与 I3 的 p 判据**方向相反**，这是本格最集中的错误来源（F16）。
    - cell: 数项 · 交错与任意项
      cell_id: numeric_alternating_general
      actions: [P0, Q1, Q2, Q3, Q4]
      note: >
        Q1 莱布尼茨（单调 + 趋零，两条缺一不可，F5）；Q2 绝对/条件收敛的**两级判定**
        （两个级数各判一次，F6）；Q3 反例排除；Q4 部分和直接分析（裂项/望远镜/线性组合）。
        正项判别法（P1–P4）**不可直接搬到本格**：它们要求正项（F4）。
    - cell: 数项 · 求和
      cell_id: numeric_sum
      actions: [N1, N2, N3, W5, X5]
      note: >
        与判敛散是不同分支（F15）。N1 构造辅助幂级数再代值（通法）、
        N2 凑已知展开式在某点的值、N3 裂项相消、W5 由傅里叶收敛定理反解、
        X5 由展开式反读系数后做系数级数的求和。
    - cell: 幂级数 · 收敛半径与收敛域
      cell_id: power_domain
      actions: [D1, D2, D3, D4, D5, D6]
      note: >
        **D1 与 D2 是两个 action，不是一个。**D1 定 R 与开区间，D2 是端点的独立判定，
        D1 以 sequence 强制转 D2（F1、F2）。D3 上极限定 R、D4 缺项级数先代换 t=x^k、
        D5 由已知敛散数据逆向反推、D6 逐项操作后 R 不变但端点可变。
    - cell: 幂级数 · 求和函数
      cell_id: power_sum
      actions: [D1, D2, G1, G2, G3, G4]
      note: >
        求和函数必须先有收敛域，故 D1/D2 在本格同样可用（且是前置）。
        G1 逐项求导、G2 逐项积分、G3 拆项分解后叠加、
        G4 端点值由和函数在收敛域上的连续性取单侧极限（F14）。
    - cell: 函数展开成幂级数
      cell_id: expansion
      actions: [X1, X2, X3, X4, X5]
      note: >
        X1 间接法（换元凑四个已知展开式）、X2 部分分式分解后分别展开、
        X3 先求导/积分再展开并逆操作定常数、X4 直接法（泰勒系数 + 验余项）、
        X5 由展开式反读系数（逆向）。**每条都必须给出收敛域**，不给即未完成。
    - cell: 系数递推与和函数
      cell_id: coefficient_recursion
      actions: [C1, C2, C3, D1]
      note: >
        C1 递推两边乘 xⁿ 求和 → S 满足的微分方程（指标必须对齐，F13）；
        C2 幂级数解法：设 y=Σaₙxⁿ 代入已给的微分方程比较同次幂系数；
        C3 一阶递推直接解出 aₙ 通项再求和。D1 在本格用于先定 R。
        **ODE 求解子步骤以 local_operation 表达**，不重复定义 calc.ode 族的 action。
    - cell: 傅里叶级数
      cell_id: fourier
      actions: [W1, W2, W3, W4, W5]
      note: >
        W1 收敛定理定点值（**不算系数**）、W2 奇偶延拓（由所求级数类型决定，
        不可自选，F12）、W3 周期平移到主区间、W4 系数公式计算、
        W5 由收敛定理在特定点的等式反解数项级数和。

  candidate_actions:
    # ── 数项 · 正项 ──
    - action_id: P0
      eligible_cells: [numeric_positive, numeric_alternating_general]
      description: 必要条件筛查：先看通项是否趋零；不趋零则直接判发散
      action_kind: preprocessing
      produces: result_object
      followup_actions:
        mode: optional_any_of
        on_skip: return_to_parent_router
        actions:
          - { kind: action_ref, action: P1, when: 通项趋零且为正项、主项是 n 的幂次 }
          - { kind: action_ref, action: P2, when: 通项趋零且含阶乘或 n 次幂（比值易约） }
          - { kind: action_ref, action: P3, when: 通项趋零且整体是 n 次方形式 }
          - { kind: action_ref, action: Q1, when: 通项趋零且为交错级数 }
          - { kind: action_ref, action: Q2, when: 通项趋零且符号不定 }
      terminal_when: [通项不趋零，已据此判定级数发散]
      role: primary
      applies_when: [任何数项级数的第一步]
      mechanism_note: "「通项趋零」是收敛的必要非充分条件：不趋零 ⇒ 发散（充分的发散判据）；趋零 ⇒ 无结论"
      historical_instance: [2026-2]
    - action_id: P1
      eligible_cells: [numeric_positive]
      description: 定阶比较判别法（含极限形式）：把 aₙ 定阶到 1/n^p，与 Σ1/n^p 比较
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 先确认两个级数**都是正项**（F4 的前提），再做比较 }
          - { kind: local_operation, operation: 用等价无穷小把 aₙ 定阶到 λ/n^p，取 bₙ=1/n^p }
          - { kind: local_operation, operation: "由 aₙ/bₙ→λ∈(0,∞) 断同敛散，并按 p>1 收敛 / p≤1 发散给结论" }
      terminal_when: [已给出与 Σ1/n^p 的同敛散结论并据此判定]
      role: primary
      applies_when: [正项级数，通项主项是 n 的幂次（含对数因子的幂次修正）]
      historical_instance: [2004-9, 2025-2]
    - action_id: P2
      eligible_cells: [numeric_positive]
      description: 比值判别法（达朗贝尔）：ρ=lim aₙ₊₁/aₙ，ρ<1 收敛、ρ>1 发散、ρ=1 不给结论
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: optional_any_of
        on_skip: return_to_parent_router
        actions:
          - { kind: action_ref, action: P1, when: ρ=1，比值判别法不给结论，改用定阶比较 }
          - { kind: action_ref, action: P3, when: ρ 的极限不存在但 n 次根形式可算 }
      terminal_when: [ρ<1 或 ρ>1，已据此判定收敛或发散]
      role: primary
      applies_when: [正项级数，通项含阶乘、n 次幂或连乘积（相邻两项之比可大幅约简）]
      mechanism_note: ρ=1 是该判别法的**失效点**，不是「不收敛」；须换判据（F1 在幂级数端点上是同一现象）
    - action_id: P3
      eligible_cells: [numeric_positive]
      description: 根值判别法（柯西）：ρ=lim (aₙ)^{1/n}（必要时取上极限），ρ<1 收敛、ρ>1 发散、ρ=1 不给结论
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: optional_any_of
        on_skip: return_to_parent_router
        actions:
          - { kind: action_ref, action: P1, when: ρ=1，根值判别法不给结论，改用定阶比较 }
      terminal_when: [ρ<1 或 ρ>1，已据此判定收敛或发散]
      role: primary
      applies_when: [正项级数，通项整体是 n 次方形式（如 (f(n))ⁿ）；系数奇偶不同时取上极限]
      mechanism_note: 根值比比值适用面更宽（比值极限存在时根值极限必存在且相等，反之不然），但计算常更繁
    - action_id: P4
      eligible_cells: [numeric_positive]
      description: 积分判别法：f 在 [N,∞) 非负、连续、单调减且 aₙ=f(n) 时，Σaₙ 与 ∫_N^∞ f 同敛散
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 逐条核验 f 的非负、连续、**单调减**三条件（单调性缺失即失效，F8） }
          - { kind: local_operation, operation: 计算 ∫_N^∞ f 并把其敛散性搬回级数 }
      terminal_when: [反常积分的敛散性已定，并据此判定级数]
      role: alternative
      applies_when: ["正项级数，通项可写成一个在 [N,∞) 上非负连续单调减的初等函数在整数点的值（典型：1/n^p、1/(n ln^q n)）"]
      historical_instance: not_found   # 2004–2026 未检出以积分判别法为主解的级数题；结构上合法且可执行，保留
    # ── 反常积分 · 敛散性判别（v1.1.0 新增）──
    - action_id: I1
      eligible_cells: [improper_integral]
      description: 拆分奇点：把积分区间按每个瑕点与每个无穷端拆成若干段，**每段单独判**，全部收敛才算整体收敛
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "先找全奇点：被积函数的无界点（分母零点、ln 的零点、负幂）与积分限中的 ±∞；端点与内点都要找" }
          - { kind: local_operation, operation: 按奇点把区间拆开，使每一段**只含一个**奇点且它在该段端点上 }
          - { kind: action_ref, action: I2, when: 该段的奇点是瑕点（被积函数在此无界） }
          - { kind: action_ref, action: I3, when: 该段的端点是 ±∞ }
          - { kind: local_operation, operation: "**每段都收敛**才判整体收敛；任一段发散则整体发散（不得只判其中一段，F17）" }
      terminal_when: []
      terminal_policy: never_terminal
      role: primary
      applies_when: [反常积分判敛散的第一步；尤其是同时含瑕点与无穷端、或含两个瑕点的题]
      mechanism_note: >
        本 action 只做分解，敛散结论由各段的 I2/I3 给出，故 never_terminal。
        与 power_domain 的 D2 同构：**多个点的判定不能并成一次**（F17 ↔ F2）。
      historical_instance: [2010-3, 2016-1]
    - action_id: I2
      eligible_cells: [improper_integral]
      description: "瑕点定阶：被积函数在瑕点 c 附近 ~ A/|x−c|^p，则 ∫ 在该端收敛 ⟺ **p<1**"
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 用等价无穷小把被积函数在 x→c 处定阶到 A/|x−c|^p（A≠0） }
          - { kind: action_ref, action: I4, when: 定阶后仍带 |ln|x−c||^α 型因子 }
          - { kind: local_operation, operation: "按 p<1 收敛 / p≥1 发散给结论；含参数时解出参数的收敛条件" }
      terminal_when: [该瑕点端的敛散性已定（或已解出参数的收敛条件）]
      role: primary
      applies_when: [该段端点是瑕点（被积函数在此无界）]
      mechanism_note: "参照物是 ∫₀¹ dx/x^p：**p<1 收敛**。与无穷端的判据方向相反（F16）"
      historical_instance: [2010-3, 2016-1]
    - action_id: I3
      eligible_cells: [improper_integral]
      description: "无穷端定阶：被积函数在 x→+∞ 时 ~ A/x^p，则 ∫ 在该端收敛 ⟺ **p>1**"
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "把被积函数在 x→∞ 处定阶到 A/x^p——**多个因子的幂要相加**（2016-1 的 p 是 a+b 而不是 b）" }
          - { kind: action_ref, action: I4, when: 定阶后仍带 (ln x)^α 型因子 }
          - { kind: local_operation, operation: "按 p>1 收敛 / p≤1 发散给结论；含参数时解出参数的收敛条件" }
      terminal_when: [该无穷端的敛散性已定（或已解出参数的收敛条件）]
      role: primary
      applies_when: [该段端点是 +∞ 或 −∞]
      mechanism_note: "参照物是 ∫₁^∞ dx/x^p：**p>1 收敛**。与瑕点的判据方向相反（F16）"
      historical_instance: [2016-1]
    - action_id: I4
      eligible_cells: [improper_integral]
      description: 对数因子定阶：|ln|^α 比任何正幂都弱，故它只在 p 恰好等于临界值时才影响结论
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "p 严格在收敛侧或发散侧时，对数因子**不改变结论**（可直接忽略）" }
          - { kind: local_operation, operation: "p 恰在临界值（瑕点 p=1 或无穷端 p=1）时，对数因子决定成败：∫₂^∞ dx/(x(ln x)^q) 收敛 ⟺ q>1" }
      terminal_when: [含对数因子的该端敛散性已定]
      role: primary
      applies_when: [被积函数含 (ln x)^α 或 |ln(x−c)|^α 型因子]
      mechanism_note: >
        「对数比任何幂弱」正是 2010-3 在 x→1⁻ 端收敛的理由：|ln(1−x)|^{2/m} 虽趋于 ∞，
        但对任意正幂 ε 都有 |ln(1−x)|^{2/m} = o((1−x)^{−ε})，故不破坏可积性。
      historical_instance: [2010-3]
    - action_id: I5
      eligible_cells: [improper_integral]
      description: 绝对收敛判定：∫|f| 收敛 ⇒ ∫f 收敛；用于被积函数变号的情形
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 对 |f| 用 I2/I3 的定阶机制判敛散 }
          - { kind: local_operation, operation: "∫|f| 收敛 ⇒ ∫f 收敛（绝对收敛）；∫|f| 发散时本 route 不给结论，须另判 ∫f 本身" }
      terminal_when: [∫|f| 收敛并据此判定 ∫f 收敛]
      role: alternative
      applies_when: [被积函数在积分区间上变号，无法直接用正项的定阶比较]
      mechanism_note: "与数项级数的 Q2 第一级同构：∫|f| 收敛是充分条件，其发散**不蕴含** ∫f 发散（与 F6 同型）"
      historical_instance: not_found   # 2004–2026 未检出；结构上合法且在大纲内，保留
    # ── 数项 · 交错与任意项 ──
    - action_id: Q1
      eligible_cells: [numeric_alternating_general]
      description: 莱布尼茨判别法：Σ(−1)ⁿaₙ（aₙ>0）在 aₙ 单调减且 aₙ→0 时收敛
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 分两条独立核验：① aₙ 单调减（作差或求导）② aₙ→0。缺任一条即本 route 不给结论（F5） }
          - { kind: action_ref, action: Q2, when: 设问还要求区分绝对收敛与条件收敛 }
      terminal_when: [两条件均已核验通过并据此判定原级数收敛，且设问不再要求区分绝对/条件收敛]
      role: primary
      applies_when: [交错级数，去掉 (−1)ⁿ 后的 aₙ 恒正]
      mechanism_note: 单调性**不可省**：仅 aₙ→0 时收敛与发散都可能发生（F5 给出两侧构造）
      historical_instance: [2011-2, 2025-2]
    - action_id: Q2
      eligible_cells: [numeric_alternating_general]
      description: 绝对收敛与条件收敛的两级判定：先判 Σ|aₙ|，再判 Σaₙ
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 第一级：对 Σ|aₙ|（正项）用 P1/P2/P3/P4 的机制判敛散 }
          - { kind: local_operation, operation: "Σ|aₙ| 收敛 → 绝对收敛，判定结束" }
          - { kind: local_operation, operation: "Σ|aₙ| 发散 → **第二级必须做**：另判 Σaₙ 本身（通常用 Q1 或 Q4 的机制）。收敛才是条件收敛，发散则是发散（F6）" }
      terminal_when: [两级判定都已完成，已给出绝对收敛 / 条件收敛 / 发散三者之一]
      role: primary
      applies_when: [任意项级数，或设问明确要求区分绝对收敛与条件收敛]
      mechanism_note: 「Σ|aₙ| 发散」只排除绝对收敛，**不蕴含**条件收敛；两级各判一次（F6）
      historical_instance: [2009-4, 2015-3, 2023-4, 2025-2]
    - action_id: Q3
      eligible_cells: [numeric_alternating_general]
      description: 反例排除：对「四个命题判真假」型设问，用常备反例逐条击破
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "掏常备反例逐个代入：(−1)ⁿ/√n（条件收敛、平方后发散）、1/(n ln n)（发散但 n aₙ→0）、1/n 与 (−1)ⁿ/n（同一 Σ|aₙ| 发散、结论相反）" }
          - { kind: local_operation, operation: 对未被反例击破的选项，另给正面证明（不得因「找不到反例」就断言其为真） }
      terminal_when: [每个选项要么被反例击破、要么有正面证明，答案唯一确定]
      role: primary
      applies_when: [选择题给出若干敛散性命题，问哪个成立/不成立]
      mechanism_note: "「未找到反例」只能记 search_result: not_found，不能当成该命题为真的证明；真命题须另行证明"
      historical_instance: [2006-9, 2009-4, 2019-3, 2023-4]
    - action_id: Q4
      eligible_cells: [numeric_alternating_general]
      description: 部分和直接分析：裂项/望远镜求出 Sₙ 的闭式，或用收敛级数的线性组合、部分和有界性判敛散
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "写出 Sₙ 并尝试相消（望远镜和 Σ(bₙ₊₁−bₙ) 的部分和 = b_{n+1}−b₁）" }
          - { kind: local_operation, operation: 或把级数拆成若干已知收敛级数的线性组合（收敛级数的线性运算仍收敛） }
          - { kind: local_operation, operation: 由 lim Sₙ 是否存在给出敛散结论；存在时该极限同时就是和 }
      terminal_when: [Sₙ 的极限存在性已确定，并据此判定敛散（必要时同时给出和）]
      role: primary
      applies_when: [通项可裂项、或级数是若干已知敛散级数的线性组合、或部分和易估界]
      historical_instance: [2006-9, 2019-3]
    # ── 数项 · 求和 ──
    - action_id: N1
      eligible_cells: [numeric_sum]
      description: 构造辅助幂级数：给数项级数的通项补一个 xⁿ 造出 S(x)，求出 S(x) 后代回特定的 x
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: action_ref, action: D1, when: 需要先确定辅助幂级数的收敛半径（并确认目标点落在收敛域内） }
          - { kind: action_ref, action: G1, when: 通项分母含 n，先逐项求导 }
          - { kind: action_ref, action: G2, when: 通项分子含 n，先逐项积分 }
          - { kind: local_operation, operation: 求出 S(x) 后**代回那个特定的 x**；若该点是收敛端点，用 G4 取单侧极限 }
      terminal_when: [辅助幂级数的和函数已求出并已代回目标点，数项级数的和已得到]
      role: primary
      applies_when: [数项级数求和且直接看不出闭式；通项含 n 的幂次或多项式因子]
      mechanism_note: 本 action 只完成「造出幂级数」这一步；和的值由 followup 链末端的代回操作给出，故本 action 可终止于该步
    - action_id: N2
      eligible_cells: [numeric_sum]
      description: 凑已知展开式在某点的值：把通项拆成 1/(1−x)、eˣ、sin x、cos x、ln(1+x) 展开式的系数形式，再代该点
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 对通项做代数化简（拆分子、约阶乘），使其成为某个标准展开式的第 n 项系数 }
          - { kind: local_operation, operation: 代入对应的 x 值（常见 x=1、x=−1、x=1/2）并核对该点在收敛域内 }
      terminal_when: [和已表示为标准函数在具体点的值（如 cos1+2sin1）]
      role: primary
      applies_when: [通项含 1/n!、1/(2n)!、1/(2n+1)! 或形如 1/n、1/n·2ⁿ 的结构]
      historical_instance: [2018-3, 2024-3]
    - action_id: N3
      eligible_cells: [numeric_sum]
      description: 裂项相消求和：把通项写成 bₙ−bₙ₊₁，由 Sₙ=b₁−bₙ₊₁ 取极限得和
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 找出 bₙ 使 aₙ=bₙ−bₙ₊₁（典型 1/(n(n+1))=1/n−1/(n+1)） }
          - { kind: local_operation, operation: 写出 Sₙ 并取 n→∞ 的极限 }
      terminal_when: [lim Sₙ 已求出，即为级数的和]
      role: primary
      applies_when: [通项为有理式且分母可因式分解成相邻因子之积]
      historical_instance: [2021-18]
    # ── 幂级数 · 收敛半径与收敛域 ──
    - action_id: D1
      eligible_cells: [power_domain, power_sum, coefficient_recursion]
      description: 求收敛半径：ρ=lim|aₙ₊₁/aₙ| 或 lim|aₙ|^{1/n}，R=1/ρ，写出收敛开区间 (x₀−R, x₀+R)
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 先确认系数**无零项**（缺项级数不得直接套比值公式，F3）；有零项转 D4 或 D3 }
          - { kind: local_operation, operation: 算出 R，写出以 x₀ 为中心、半径 R 的开区间 }
          - { kind: action_ref, action: D2, when: 设问要求收敛域（而非仅收敛半径或收敛区间） }
      terminal_when: [设问只要求收敛半径 R 或收敛区间（开区间），不要求收敛域]
      role: primary
      applies_when: [幂级数 Σaₙ(x−x₀)ⁿ，系数无零项且比值或根值极限存在]
      mechanism_note: >
        R 只由 {aₙ} 决定，与中心 x₀ 无关。**R 不决定收敛域**：R=1 可对应 (−1,1)、
        [−1,1)、[−1,1] 三种收敛域（F2），故 followup 到 D2 用 sequence 强制，
        不用 optional_any_of。
      historical_instance: [2005-16, 2008-11, 2010-18, 2012-17, 2020-17, 2021-18]
    - action_id: D2
      eligible_cells: [power_domain, power_sum]
      description: 端点单独判定：把 x=x₀−R 与 x=x₀+R 分别代回，各得一个数项级数，各自判敛散
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 左端点代入 → 得一个数项级数 → 用 P0/P1/Q1/Q2 的机制判敛散（**不得沿用求 R 的那次比值判别法**，它在端点必给 ρ=1，F1） }
          - { kind: local_operation, operation: 右端点代入 → 同样单独判一次 }
          - { kind: local_operation, operation: 由两次判定结果写定收敛域的开闭；中心不在 0 时端点随中心平移 }
      terminal_when: [两个端点都已各自判定完毕，收敛域及其开闭已写定]
      role: primary
      applies_when: [设问要求收敛域；R 有限且非零]
      mechanism_note: >
        这是与 D1 分离的**独立步骤**。两个端点必须各判一次，结果可以不同
        （Σxⁿ/n 在 x=−1 收敛、x=1 发散）。R=∞ 时无端点，本 action 空过；
        R=0 时收敛域退化为单点 {x₀}。
      historical_instance: [2008-11, 2010-18, 2011-2, 2021-18, 2026-2]
    - action_id: D3
      eligible_cells: [power_domain]
      description: 上极限法求 R：1/R = limsup|aₙ|^{1/n}，用于系数奇偶不同或含振荡因子而普通极限不存在的情形
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "分别算出各子列的 |aₙ|^{1/n} 极限，取其中**最大者**为 limsup" }
          - { kind: action_ref, action: D2, when: 设问要求收敛域 }
      terminal_when: [设问只要求收敛半径 R]
      role: primary
      applies_when: [系数含 (−1)ⁿ 型振荡、或按 n 的奇偶给不同表达式，普通比值/根值极限不存在]
      mechanism_note: R 由**上极限**（较大者）决定；取较小者会把 R 放大，给出错误的收敛区间
      historical_instance: [2026-2]
    - action_id: D4
      eligible_cells: [power_domain]
      description: 缺项级数代换：级数实际是 x^k 的幂级数（如 Σaₙx^{2n}）时，先令 t=x^k 求 t 的收敛半径，再回代解出 x 的范围
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 令 t=x^k（或 t=(x−x₀)^k），把级数写成 Σaₙtⁿ }
          - { kind: action_ref, action: D1, when: 换元后系数无零项，可直接用比值/根值 }
          - { kind: local_operation, operation: "由 |t|<R_t 回代解出 |x|<R_t^{1/k}" }
          - { kind: action_ref, action: D2, when: 设问要求收敛域（x 的两个端点仍须各判一次） }
      terminal_when: []
      terminal_policy: never_terminal
      role: primary
      applies_when: [幂级数只含 x 的某个固定倍数次幂（缺项），直接对 x 的系数列套比值公式会遇到 0/0]
      mechanism_note: 本 action 只完成换元与回代范围，收敛域仍须由 D2 判端点，故 never_terminal
      historical_instance: [2010-18, 2012-17, 2026-2]
    - action_id: D5
      eligible_cells: [power_domain]
      description: 由已知敛散数据逆向反推 R 或收敛域（含中心平移与同系数迁移）
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "由「在某点收敛 / 在某点发散」用阿贝尔定理定出 R 的上下界：某点 x₁ 收敛 ⇒ R≥|x₁−x₀|；某点 x₂ 发散 ⇒ R≤|x₂−x₀|" }
          - { kind: local_operation, operation: "换中心时**系数不变**：同一组 {aₙ} 在两个中心下，端点处求的是同一个数项级数 Σaₙ(±R)ⁿ，敛散性整体搬迁" }
          - { kind: local_operation, operation: 代回验证：反推出的 R 与收敛域必须与题面给的每一条敛散数据都相容 }
      terminal_when: [R 或收敛域（含开闭）已确定且与题面给出的全部敛散数据一致]
      role: primary
      applies_when: [题面不给通项，只给「在某些点收敛/发散」或「Σaₙ 条件收敛」等性质，问 R、收敛域或另一个点的敛散性（逆向设问）]
      mechanism_note: >
        「Σaₙ(x−x₀)ⁿ 在 x=x₁ 条件收敛」是一条强信息：条件收敛点必在**收敛边界**上
        （内点必绝对收敛），故直接给出 R=|x₁−x₀|。
      historical_instance: [2008-11, 2011-2, 2015-3, 2020-4]
    - action_id: D6
      eligible_cells: [power_domain]
      description: 逐项操作后的收敛域：逐项求导/积分或乘 n 不改变 R，但可能改变端点，须重判
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 断定新级数的 R 与原级数相同 }
          - { kind: action_ref, action: D2, when: 设问要求新级数的收敛域（端点必须重新判，不得沿用原级数的端点结论，F9） }
      terminal_when: [设问只要求新级数的收敛半径或某个内点的敛散性]
      role: primary
      applies_when: [题面把原级数逐项求导/积分或乘上 n 的多项式后，问新级数的收敛半径、收敛域或某点敛散性]
      mechanism_note: >
        R 不变是定理；收敛域不变**不是**。Σxⁿ/n² 的收敛域 [−1,1]，逐项求导后
        Σxⁿ⁻¹/n 的收敛域 [−1,1)，再求导后 (−1,1)——R 三次都是 1（F9）。
      historical_instance: [2015-3, 2020-4]
    # ── 幂级数 · 求和函数 ──
    - action_id: G1
      eligible_cells: [power_sum]
      description: 逐项求导：通项分母含 n 时先逐项求导消掉分母的 n，凑出 Σxⁿ=1/(1−x)，求和后再积分回去
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "确认操作点在收敛区间**内部** (x₀−R, x₀+R)；逐项求导的合法性由该区间上的内闭一致收敛保证" }
          - { kind: local_operation, operation: 逐项求导得 S′(x)，化到几何级数闭式 }
          - { kind: local_operation, operation: "由 S(x)=S(x₀)+∫_{x₀}^{x} S′(t)dt 还原，**用 S(x₀) 定常数**（常取 x₀=0）" }
          - { kind: action_ref, action: G4, when: 设问要求收敛端点处的和 }
      terminal_when: [和函数在收敛区间内部的闭式已写出，且设问不涉及端点]
      role: primary
      applies_when: [幂级数通项形如 xⁿ/n、xⁿ/(2n−1)、xⁿ/(n(n+1)) 等分母含 n 的因子]
      mechanism_note: 方向判据是**分母有 n → 先求导**（求导把 xⁿ/n 变成 xⁿ⁻¹，n 被消掉）；反向操作合法但不化简（F10）
      historical_instance: [2010-18, 2012-17, 2017-12, 2021-18]
    - action_id: G2
      eligible_cells: [power_sum]
      description: 逐项积分：通项分子含 n 时先逐项积分消掉分子的 n，凑出几何级数，求和后再求导回去
      action_kind: solver
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 确认积分区间含于收敛区间内部；逐项积分的合法性由内闭一致收敛保证 }
          - { kind: local_operation, operation: 逐项积分把 Σn aₙxⁿ⁻¹ 还原成 Σaₙxⁿ，化到几何级数闭式 }
          - { kind: local_operation, operation: 对所得闭式求导回去，得到原级数的和函数 }
          - { kind: action_ref, action: G4, when: 设问要求收敛端点处的和 }
      terminal_when: [和函数在收敛区间内部的闭式已写出，且设问不涉及端点]
      role: primary
      applies_when: ["幂级数通项形如 n xⁿ、(2n+1)x^{2n}、n(n+1)xⁿ 等分子含 n 的因子"]
      mechanism_note: 方向判据是**分子有 n → 先积分**（Σn xⁿ⁻¹ 逐项积分即 Σxⁿ，n 被消掉，再求导还原）
      historical_instance: [2012-17, 2017-12]
    - action_id: G3
      eligible_cells: [power_sum]
      description: 拆项分解后叠加：把通项拆成若干可分别求和的部分（多项式除法、部分分式、提公因子、指标平移），各自求和再合并
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: any_of
        actions:
          - { kind: action_ref, action: G1, when: 拆出的某项分母含 n }
          - { kind: action_ref, action: G2, when: 拆出的某项分子含 n }
          - { kind: local_operation, operation: 拆出的某项本身已是几何级数或已知展开式，直接写闭式 }
          - { kind: local_operation, operation: 合并各部分，并取各部分收敛域的**交集**作为整体的定义范围；x=0 等特殊点单独核对 }
      terminal_when: [拆分后每一部分的和都已直接看出并合并完毕]
      role: primary
      applies_when: [通项是多项式与 1/n 型因子的组合（如 (4n²+4n+3)/(2n+1)）、或含两个结构不同的相加项]
      mechanism_note: 合并时收敛域取交集——各部分的收敛域可以不同（2021-18 中一部分为 x>0、另一部分为 [−1,1]）
      historical_instance: [2005-16, 2012-17, 2021-18]
    - action_id: G4
      eligible_cells: [power_sum]
      description: 端点处的和：由和函数在收敛域上的连续性，取内部的单侧极限得到端点的和
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 先确认该端点**在收敛域内**（即 D2 已判定该端点收敛）；端点发散时本 action 不适用 }
          - { kind: local_operation, operation: "由和函数在收敛域上连续，S(端点)=lim_{x→端点} S(x)（单侧）" }
      terminal_when: [端点处的和已由单侧极限求出]
      role: primary
      applies_when: [设问要求幂级数在某个收敛端点处的和，而逐项求导/积分定理只覆盖开区间内部]
      mechanism_note: >
        逐项求导/积分定理只在 (x₀−R, x₀+R) **内部**成立，不覆盖端点；端点的和
        必须另由和函数的连续性取极限（F14）。
      historical_instance: [2021-18]
    # ── 函数展开成幂级数 ──
    - action_id: X1
      eligible_cells: [expansion]
      description: 间接法：用换元把 f 凑成四个已知展开式之一（1/(1−x)、eˣ、sin x、ln(1+x)）
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 识别 f 的形状并选定标准展开式，做换元 u=g(x) }
          - { kind: local_operation, operation: "**换元后收敛区间必须跟着变**：由 |u|<r 解出 x 的范围" }
          - { kind: local_operation, operation: 写出展开式并**注明收敛域**（不写收敛域即未完成） }
      terminal_when: [展开式与其收敛域都已写出]
      role: primary
      applies_when: [f 经一次代数换元即可化为四个标准展开式之一（含 cos x = (sin x)′ 型的直接派生）]
      historical_instance: [2019-11, 2024-3]
    - action_id: X2
      eligible_cells: [expansion]
      description: 部分分式分解后分别展开：有理函数先分解成 A/(a−x)+B/(b+x) 型，各自套几何级数再合并系数
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 因式分解分母并解出待定系数 A、B }
          - { kind: local_operation, operation: 每项化成 1/(1−u) 的形式（提出常数），套 Σuⁿ }
          - { kind: local_operation, operation: 合并同次幂系数，收敛域取各部分收敛域的**交集** }
      terminal_when: [合并后的幂级数与其收敛域都已写出]
      role: primary
      applies_when: [f 是真分式或可化为真分式的有理函数，分母可因式分解]
      historical_instance: [2006-17]
    - action_id: X3
      eligible_cells: [expansion]
      description: 先求导/积分再展开：f 本身不易展开但 f′ 或 ∫f 易展开时，展开后逐项逆操作，并用 f 在中心的值定常数
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 展开 f′（或 ∫₀ˣ f），得到一个已知型级数 }
          - { kind: local_operation, operation: 逐项积分（或求导）回到 f，**用 f(x₀) 补常数项** }
          - { kind: local_operation, operation: 注明收敛域；逆操作后端点可能变化，须重判（同 F9 的机制） }
      terminal_when: [f 的展开式与其收敛域都已写出]
      role: primary
      applies_when: [f 为 arctan x、ln(1+x)、arcsin x 等，其导数是易展开的有理式或根式]
    - action_id: X4
      eligible_cells: [expansion]
      description: 直接法：按 aₙ=f^{(n)}(x₀)/n! 算泰勒系数，并验证余项 Rₙ(x)→0
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "求出 f^{(n)}(x₀) 的通项表达式" }
          - { kind: local_operation, operation: "**必须验余项**：给出 Rₙ(x)→0 成立的 x 范围，该范围即展开成立的区间" }
      terminal_when: [系数通项与余项趋零的范围都已给出]
      role: alternative
      applies_when: [f 的高阶导数有可写出的通项，且间接法（X1–X3）不适用]
      mechanism_note: 直接法漏验余项时，「泰勒级数收敛」与「收敛到 f」是两件事，前者不蕴含后者
      historical_instance: not_found   # 2004–2026 未检出以直接法为主解的展开题；结构上合法，保留（dominated_not_excluded）
    - action_id: X5
      eligible_cells: [expansion, numeric_sum]
      description: 由展开式反读系数：已知 f(x)=Σaₙ(x−x₀)ⁿ，反读出 aₙ 的表达式，再对 aₙ 做求和或代数运算（逆向）
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 先把 f 展开（走 X1/X2/X3）得到 aₙ 的通项 }
          - { kind: local_operation, operation: "按设问对 aₙ 做运算（取子列 a_{2n}、乘 n、求 Σ 等）" }
          - { kind: action_ref, action: N2, when: 反读出的系数级数需要凑标准展开式求和 }
          - { kind: local_operation, operation: "也可由 aₙ=f^{(n)}(x₀)/n! 反过来求 f 的高阶导数值" }
      terminal_when: [设问所求的系数、系数级数的和或高阶导数值已求出]
      role: primary
      applies_when: ["题面给出 f 与其幂级数表示，问某个系数、系数构成的数项级数的和、或 f^{(n)}(x₀)（逆向设问）"]
      historical_instance: [2019-11, 2024-3]
    # ── 系数递推与和函数 ──
    - action_id: C1
      eligible_cells: [coefficient_recursion]
      description: 系数递推翻译成微分方程：两边乘 xⁿ 求和，把 Σ 与 S、S′、S″ 对齐，得到 S 满足的常微分方程
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "**指标对齐**：做指标平移时逐项核对求和起点，平移后剩下的首项不得漏（F13）" }
          - { kind: local_operation, operation: 把各求和式写成 S、S′、S″ 的组合，整理出微分方程 }
          - { kind: local_operation, operation: 解该微分方程（机制归 calc.ode.route-selection，此处只作 local_operation，不重复定义其 action） }
          - { kind: local_operation, operation: 由 S(x₀)=a₀、S′(x₀)=a₁ 定常数——**S(0) 由求和的起点下标决定**，起点不是 0 时 S(0)≠a₀ }
      terminal_when: [S 满足的微分方程已解出、常数已由初值定完，和函数闭式已写出]
      role: primary
      applies_when: [题面给出 aₙ 的二阶（或含 n 系数的一阶）递推式，问和函数 S(x)]
      mechanism_note: 递推→方程的翻译是本 action 的核心；解方程的机制归 calc.ode 族，此处以 local_operation 承接，定完常数即终止
      historical_instance: [2013-16, 2020-17]
    - action_id: C2
      eligible_cells: [coefficient_recursion]
      description: 幂级数解法：设 y=Σaₙxⁿ 代入题面已给的微分方程，比较同次幂系数得到 aₙ 的递推，再求出通项与闭式
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 写出 y′=Σn aₙxⁿ⁻¹、y″=Σn(n−1)aₙxⁿ⁻²，代入方程并统一为同一个幂次 }
          - { kind: local_operation, operation: 令各次幂系数为零，得 aₙ 的递推式 }
          - { kind: local_operation, operation: 由初值 y(0)、y′(0) 定 a₀、a₁，逐层解出 aₙ 通项 }
          - { kind: action_ref, action: N2, when: 求出的 aₙ 通项可凑成标准展开式，需回写成初等函数闭式 }
      terminal_when: [aₙ 的通项已求出，且设问所要的解（级数形式或闭式）已写出]
      role: primary
      applies_when: [题面给出微分方程与初值，要求用幂级数求解（变系数线性方程的受限 route；ODE 族已把此型判归本族）]
      historical_instance: [2007-20]
    - action_id: C3
      eligible_cells: [coefficient_recursion]
      description: 一阶递推直接解通项：aₙ₊₁/aₙ 可写成 n 的有理式时，连乘裂项直接解出 aₙ，再对 Σaₙxⁿ 求和
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "由 aₙ = a₁·∏_{k=1}^{n−1}(a_{k+1}/a_k) 连乘并约分得通项" }
          - { kind: action_ref, action: D1, when: 需要由 aₙ₊₁/aₙ 的极限先定出收敛半径 }
          - { kind: action_ref, action: N2, when: "通项含组合数/双阶乘，可凑成 (1−x)^{−α} 型二项展开" }
      terminal_when: [aₙ 通项与 Σaₙxⁿ 的闭式都已写出]
      role: alternative
      applies_when: [递推是一阶的（只联系 aₙ₊₁ 与 aₙ）且比值为 n 的有理式]
      mechanism_note: 与 C1 是同一格内的两条并列 route——C1 走「递推→方程→解方程」，C3 走「递推→通项→求和」；C3 在比值为简单有理式时更短，在无法约分时走不动
      historical_instance: [2020-17]
    # ── 傅里叶级数 ──
    - action_id: W1
      eligible_cells: [fourier]
      description: 收敛定理定点值：连续点收敛到 f(x)，间断点收敛到左右极限的平均，端点按周期延拓后判
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: action_ref, action: W2, when: 题面要求的是正弦级数或余弦级数（须先按对应方式延拓） }
          - { kind: action_ref, action: W3, when: 所问的点不在延拓后的主区间内 }
          - { kind: local_operation, operation: "判该点是否为延拓后函数的间断点：连续则取 f(x₀)，间断则取 [f(x₀⁻)+f(x₀⁺)]/2（F11）" }
      terminal_when: [所问点处的和已给出]
      role: primary
      applies_when: [设问形如「其傅里叶级数在 x₀ 处收敛于」，问的是和而不是系数]
      mechanism_note: 本 route **不需要算任何系数**——这是它相对 W4 的全部价值
      historical_instance: [2013-3, 2023-13, 2025-12]
    - action_id: W2
      eligible_cells: [fourier]
      description: 奇偶延拓：正弦级数作奇延拓、余弦级数作偶延拓，再按周期延拓到整个数轴
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: 由**所求级数的类型**决定延拓方式（正弦↔奇、余弦↔偶），不可自选（F12） }
          # 下两条前向边互斥，取其一即可（本表以 sequence 声明，执行时按 when 分派）
          - { kind: local_operation, operation: 写出延拓后函数在一个周期上的表达式，并标出它的间断点 }
          - { kind: action_ref, action: W1, when: 设问要求某点处的和 }
          - { kind: action_ref, action: W4, when: 设问要求系数或完整展开式 }
      terminal_when: []
      terminal_policy: never_terminal
      role: primary
      applies_when: ["f 只定义在 [0,l] 上，而题面要求展成正弦级数或余弦级数"]
      mechanism_note: 延拓本身不产生任何级数结论，必须接 W1（求点值）或 W4（算系数），故 never_terminal
      historical_instance: [2008-19, 2013-3, 2023-13, 2025-12]
    - action_id: W3
      eligible_cells: [fourier]
      description: 周期平移：把所问的点加减周期的整数倍，平移到延拓后函数的主区间内再判
      action_kind: preprocessing
      produces: transformed_problem
      followup_actions:
        mode: sequence
        actions:
          - { kind: local_operation, operation: "确定延拓后的周期 T（[0,l] 上作奇/偶延拓后周期为 2l，不是 l）" }
          - { kind: local_operation, operation: 求 x₀+kT 落入主区间，并在奇延拓下用 S(−x)=−S(x) 再折一次 }
          - { kind: action_ref, action: W1, when: 已平移到主区间，可判连续性并取值 }
      terminal_when: []
      terminal_policy: never_terminal
      role: primary
      applies_when: [所问的点落在定义区间之外（如 S(−9/4)、S(−7/2)）]
      mechanism_note: 周期取错（把 2l 当 l）会把点平移到错误位置；本 action 只做平移，取值由 W1 给出，故 never_terminal
      historical_instance: [2013-3, 2025-12]
    - action_id: W4
      eligible_cells: [fourier]
      description: 系数公式计算：按 aₙ=(2/l)∫₀^l f cos(nπx/l)dx、bₙ=(2/l)∫₀^l f sin(nπx/l)dx 算系数（延拓后只需算一半）
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: action_ref, action: W2, when: 尚未确定延拓方式（奇延拓只有 bₙ、偶延拓只有 aₙ） }
          - { kind: local_operation, operation: 用分部积分算积分；含 cos nπ 时化为 (−1)ⁿ }
          - { kind: local_operation, operation: 单独核对 a₀（公式与 n≥1 不同，且写级数时带 1/2 因子） }
      terminal_when: [所求的系数（或 a₀）已算出]
      role: primary
      applies_when: [设问明确要求傅里叶系数，或要求写出完整的傅里叶级数展开式]
      historical_instance: [2008-19, 2023-13]
    - action_id: W5
      eligible_cells: [numeric_sum, fourier]
      description: 由收敛定理反解数项级数：在某个特定点写出「级数值 = 收敛定理给的值」这一等式，解出待求的数项级数的和
      action_kind: solver
      produces: result_object
      followup_actions:
        mode: sequence
        actions:
          - { kind: action_ref, action: W4, when: 尚未算出系数（本 route 需要显式的级数表达式） }
          - { kind: local_operation, operation: 选一个使级数退化成目标数项级数的点（常取 x=0 或 x=l） }
          - { kind: local_operation, operation: 该点若为间断点，右端取左右极限的平均而非 f(x₀)（F11） }
          - { kind: local_operation, operation: 由该等式解出目标数项级数的和 }
      terminal_when: [目标数项级数的和已解出]
      role: primary
      applies_when: [题面先要求展开成傅里叶级数，再要求由此求某个数项级数（如 Σ(−1)ⁿ⁻¹/n²）的和]
      mechanism_note: 这是数项级数求和的一条独立通道，与 N1（造幂级数）并列，不互相包含
      historical_instance: [2008-19]

  selection_rule:
    guards:
      # ── necessary：不满足则该 route 非法或结论不成立 ──
      - condition: "比较判别法（含极限形式）要求参与比较的**两个级数都是正项级数**"
        logical_role: necessary
        check: 用之前先确认 aₙ≥0 与 bₙ>0；含 (−1)ⁿ 的级数须先取绝对值转正项，或改走 Q1/Q2/Q4
        explanation: 去掉正项条件后，aₙ/bₙ→1 也不保证同敛散（F4 给出构造）
      - condition: "莱布尼茨判别法要求 |aₙ| **单调减** 且 **→0**，两条缺一不可"
        logical_role: necessary
        check: 分两条独立核验；单调性用作差 aₙ−aₙ₊₁ 或对应函数求导
        explanation: 只验趋零时收敛与发散都可能（F5 同时给出发散与收敛两侧的构造）
      - condition: "「绝对收敛 / 条件收敛」必须做**两次判定**：Σ|aₙ| 一次、Σaₙ 一次"
        logical_role: necessary
        check: 草稿上写两行；Σ|aₙ| 发散后**必须**再判 Σaₙ 本身
        explanation: 「Σ|aₙ| 发散」只排除绝对收敛，不蕴含条件收敛——也可能整个发散（F6）
      - condition: "收敛域 = 收敛区间 + **两个端点各自单独判定**；端点判定是与求 R 分离的独立步骤"
        logical_role: necessary
        check: 求出 R 后写下三行占位（R、开区间、两端点），逐行填满才算完成
        explanation: 同一个 R=1 可对应 (−1,1)、[−1,1)、[−1,1] 三种收敛域，R 不决定收敛域（F2）
      - condition: "端点的敛散性**不得**沿用求 R 时用的那次比值/根值判别法"
        logical_role: necessary
        check: 端点代入后得到的是一个数项级数，重新选判据（P0/P1/Q1/Q2）
        explanation: 在端点处该判别法必然给出 ρ=1，此时它不给结论（F1）
      - condition: "积分判别法要求 f 在 [N,∞) 上**非负、连续、单调减**且 aₙ=f(n)"
        logical_role: necessary
        check: 三个条件逐条核验，单调性尤其不能默认
        explanation: 去掉单调性后 ∫f 收敛而 Σf(n) 发散是可能的（F8 给出尖峰构造）
      - condition: "傅里叶级数在间断点收敛到 **[f(x₀⁻)+f(x₀⁺)]/2**，不是 f(x₀)"
        logical_role: necessary
        check: 取值前先判该点在**延拓后**的函数中是否为间断点
        explanation: 在间断点取 f(x₀) 直接给出错误的值（F11）
      - condition: "延拓方式由**所求级数的类型**决定：正弦级数=奇延拓、余弦级数=偶延拓，不可自选"
        logical_role: necessary
        check: 先读设问要哪种级数，再决定延拓；延拓后周期是 2l 而非 l
        explanation: 同一个 f 的正弦级数与余弦级数是两个不同的级数，在同一点可给出不同的值（F12）
      - condition: "系数递推翻译成微分方程时，**求和指标必须对齐**；指标平移后剩下的首项不得漏"
        logical_role: necessary
        check: 每次平移都写出平移后的起点，并把落单的首项显式列出
        explanation: 漏掉首项会得到一个齐次方程，解出恒零的和函数（F13 以 2020-17 为例）
      - condition: "逐项求导/积分的结论只在收敛区间**内部**成立；端点的敛散性与和值都必须另判"
        logical_role: necessary
        check: 逐项操作后重判两个端点（F9）；端点的和另用 G4（F14）
        explanation: R 不变是定理，收敛域不变不是——Σxⁿ/n² 逐项求导两次，R 恒为 1 而收敛域从 [−1,1] 缩到 (−1,1)
      - condition: "反常积分的 p 判据**方向相反**：瑕点处 p<1 收敛，无穷端 p>1 收敛"
        logical_role: necessary
        check: 定阶后先问「这是瑕点还是无穷端」，再套对应方向的判据
        explanation: 同一个 p 在两类奇点上给出相反结论（F16），方向记反即得错误答案
      - condition: "反常积分有多个奇点时，必须拆开使每段只含一个奇点，**每段各判一次**"
        logical_role: necessary
        check: 先找全奇点（含积分限内部的无界点），再拆区间；全部收敛才判整体收敛
        explanation: >
          只判其中一个奇点会漏掉另一个（F17）。这与幂级数收敛域的 D2 是**同一条结构规则**：
          多个点的判定不能并成一次（F17 ↔ F2）
      - condition: "缺项幂级数（系数含零项）不得直接套 lim|aₙ₊₁/aₙ|"
        logical_role: necessary
        check: 先看系数列有没有零；有则改走 D4（换元 t=x^k）或 D3（上极限）
        explanation: 系数有零项时比值无定义或极限不存在，公式给不出 R（F3）
      # ── sufficient：满足即可用；**不满足不等于不可用** ──
      - condition: "「幂级数在 (x₀−R, x₀+R) 上内闭一致收敛」⇒ 可在其中逐项求导/积分，且 R 不变"
        logical_role: sufficient
        check: 确认 R>0 且操作点落在开区间内部即可放心逐项操作
        explanation: >
          这是定理的**充分条件**，不是必要条件。**不满足它不等于逐项操作非法**——
          在更弱的条件下（如控制收敛）逐项积分也可能合法，只是那些判据超出数学一
          scope。因此本条不得写成合法性的必要条件，也不得据此把端点直接排除在
          「可操作」之外（端点另有 G4 这条通道，见 F14）。
      - condition: "「Σ|aₙ| 收敛」⇒ Σaₙ 收敛（绝对收敛蕴含收敛）"
        logical_role: sufficient
        check: 第一级判定通过即可直接结束，无需再判 Σaₙ
        explanation: 反向不成立——Σaₙ 收敛不蕴含 Σ|aₙ| 收敛（条件收敛即反例）
      - condition: "「通项不趋零」⇒ 级数发散"
        logical_role: sufficient
        check: 先算 lim aₙ；不为零直接判发散
        explanation: 这是必要条件的逆否，作为**发散**的充分判据；反过来「通项趋零」不蕴含收敛
      - condition: "「比值/根值的 ρ<1」⇒ 收敛；「ρ>1」⇒ 发散"
        logical_role: sufficient
        check: 两侧各自充分，各自即可结束判定
        explanation: ρ=1 是该判别法的失效点，**不是**「不收敛」；须换判据
      - condition: "「该端点在收敛域内」⇒ 端点处的和可由内部取单侧极限得到（和函数在收敛域上连续）"
        logical_role: sufficient
        check: 先由 D2 确认端点收敛，再取极限
        explanation: 端点发散时本通道不适用；但端点收敛就足以用它，不需要额外条件
      # ── supporting_heuristic：效率与识别偏好，不是合法性条件 ──
      - condition: "正项级数先把通项**定阶到 1/n^p** 再选判别法（含阶乘/n 次幂 → 比值/根值；纯幂次 → 比较）"
        logical_role: supporting_heuristic
        check: 先看通项主项是 n 的几次方，再挑判别法
        explanation: 效率偏好。选「不合适」的判别法通常只是算不动或给 ρ=1，不产生错误结论
      - condition: "求和函数时 **n 在分母先逐项求导、n 在分子先逐项积分**，目标是凑出 Σxⁿ=1/(1−x)"
        logical_role: supporting_heuristic
        check: 动笔前先看 n 在分子还是分母
        explanation: >
          方向偏好。选反方向仍是合法的逐项操作，只是不化简、离目标更远（F10 是
          loses_advantage 而非 invalidates）。**注意**：`高数方法速查.md` §9 把
          这条写反了，见 §5 open_questions
      - condition: "求傅里叶级数在某点的和**先不算系数**：延拓 → 平移 → 判连续性"
        logical_role: supporting_heuristic
        check: 看到「收敛于」而不是「系数」，就走 W1 三步，不碰 W4
        explanation: 效率偏好。算系数（W4）也能得到答案，只是把选择/填空题做成了大题
      - condition: "展开成幂级数优先**间接法**（X1–X3），直接法（X4）作为兜底"
        logical_role: supporting_heuristic
        check: 先试能否凑成四个已知展开式之一
        explanation: 效率偏好。直接法合法但需另验余项趋零，工作量大得多
      - condition: "敛散性命题的真假判定优先**反例排除**，常备 (−1)ⁿ/√n 与 1/(n ln n)"
        logical_role: supporting_heuristic
        check: 四个选项先各掏一次常备反例
        explanation: >
          效率偏好，且只单向有效：反例能否证一个选项，但**找不到反例不能证成**
          （只能记 search_result: not_found），真命题仍须正面证明
      - condition: "反常积分先定阶到 A/x^p 或 A/|x−c|^p，对数因子暂置一旁，只在 p 恰为临界值时才回头处理"
        logical_role: supporting_heuristic
        check: 先算幂次 p；p 严格在收敛或发散侧时对数因子不影响结论
        explanation: 效率偏好。对数因子比任何正幂弱，只在临界点上决定成败（I4）
      - condition: "系数含 (−1)ⁿ 或按奇偶给不同表达式时优先上极限（D3）求 R"
        logical_role: supporting_heuristic
        check: 看系数列是否分奇偶两支
        explanation: 识别偏好；也可先换元把两支拆开分别处理

    minimal_probe:
      action: |
        **必须在一级四轴定完之后执行，且按格分派**：
        · 数项 × 判敛散 → 先算 lim aₙ（P0）：不趋零直接完；趋零再看符号定格
        · 数项 · 正项    → 通项主项是 n 的幂次 → P1；含阶乘/连乘 → P2；整体 n 次方 → P3；
                          能写成单调减连续函数在整点的值 → P4
        · 数项 · 交错/任意项 → 交错且余下定号 → Q1；设问提「绝对/条件」→ Q2（两级都要做）；
                          命题真假 → Q3；可裂项或是已知级数的线性组合 → Q4
        · 数项 × 求和    → 直接看不出闭式 → N1 造幂级数；含阶乘 → N2；分母可裂 → N3；
                          题面前一问是傅里叶展开 → W5
        · 幂级数 × 收敛域 → 系数有零项 → D4/D3，否则 D1；**D1 之后必走 D2 判两个端点**
        · 幂级数 × 求和   → 先 D1/D2 定收敛域；再看 n 在分母（G1）还是分子（G2）；
                          通项是复合结构 → G3 先拆；问端点的和 → G4
        · 展开          → 能凑四式之一 → X1；有理函数 → X2；导数好展 → X3；否则 X4
        · 给系数递推     → 二阶递推 → C1；给的是方程要求级数解 → C2；一阶递推 → C3
        · 傅里叶        → 问「收敛于」→ W1（不算系数）；问系数 → W4；先展开再求数项和 → W5
        · 反常积分       → 先 I1 找全奇点并拆段；每段瑕点走 I2（p<1）、无穷端走 I3（p>1）；
                          带对数因子且 p 在临界值 → I4；被积变号 → I5
        · 逆向设问       → 直接进 D5 / X5，不要求先正向求出级数
      interpretation: >
        识别信号匹配的方法成为**高优先级合法候选**，但不能据此断言「收益最大」：
        端点单独判定、绝对/条件的第二级判定、逐项操作后的端点重判、展开式的收敛域、
        余项验证等执行细节仍须逐项完成。
        若某格的全部识别信号都不匹配（如正项级数四条判别法都给不出结论），
        转 pending 并记录，不得为了「有个答案」把不适用的判别法硬套。

    preference_rule: >
      在同一格内，识别信号匹配的方法进入候选集；仍须核对适用性与计算成本。
      正项级数按通项结构选判别法，交错/任意项优先 Q1/Q2，求和优先造幂级数，
      展开优先间接法，傅里叶求点值优先收敛定理。
      **「历史真题没用过某方法」不构成排除**：P4（积分判别法）与 X4（直接法展开）
      在 2004–2026 均未检出主解，仍保留为合法候选。
      **「历史真题常用某方法」也不自动升为必要候选**：幂级数收敛域题占本族最大比重，
      但这不使 D1 成为其它格的默认入口。
      以上只是合法候选之间比较执行成本与设问匹配度，不改变任何 route 的合法性。

  mechanism:
    statement: |
      必要条件：Σaₙ 收敛 ⇒ aₙ→0。逆否为发散判据；反向不成立（Σ1/n）。
      比较判别法（极限形式）：aₙ,bₙ>0 且 aₙ/bₙ→λ∈(0,∞) ⇒ 同敛散；
        参照物 Σ1/n^p 在 p>1 收敛、p≤1 发散。
      比值/根值：ρ=lim aₙ₊₁/aₙ 或 lim (aₙ)^{1/n}（必要时取上极限），
        ρ<1 收敛、ρ>1 发散、ρ=1 该判别法不给结论。
      积分判别法：f 在 [N,∞) 非负连续单调减、aₙ=f(n) ⇒ Σaₙ 与 ∫_N^∞ f 同敛散。
      莱布尼茨：aₙ>0 单调减且 aₙ→0 ⇒ Σ(−1)ⁿaₙ 收敛，且余项 |Rₙ|≤aₙ₊₁。
      绝对/条件：Σ|aₙ| 收敛 ⇒ Σaₙ 收敛（绝对收敛）；Σ|aₙ| 发散而 Σaₙ 收敛
        ⇒ 条件收敛；两者都发散 ⇒ 发散。三分，不是二分。
      阿贝尔定理：Σaₙ(x−x₀)ⁿ 在 x₁ 收敛 ⇒ 在 |x−x₀|<|x₁−x₀| 上绝对收敛；
        在 x₂ 发散 ⇒ 在 |x−x₀|>|x₂−x₀| 上发散。故存在 R，
        1/R = limsup|aₙ|^{1/n}（柯西–阿达玛）。**端点 |x−x₀|=R 上定理不给结论。**
      内闭一致收敛与逐项操作：幂级数在 (x₀−R,x₀+R) 的任一闭子区间上一致收敛，
        故在开区间内可逐项求导与逐项积分，所得级数的收敛半径仍为 R
        （**收敛域可变**）。和函数在**收敛域**上连续，故端点的和可由内部取单侧极限。
      四个基本展开式：1/(1−x)=Σxⁿ (|x|<1)；eˣ=Σxⁿ/n! (ℝ)；
        sin x=Σ(−1)ⁿx^{2n+1}/(2n+1)! (ℝ)；ln(1+x)=Σ(−1)ⁿ⁻¹xⁿ/n (−1<x≤1)。
        其余展开由换元、逐项求导/积分、部分分式从这四式派生。
      泰勒展开成立的条件：f 有各阶导数**且**余项 Rₙ(x)→0；
        仅有各阶导数不足以保证泰勒级数收敛到 f。
      系数递推 ⇄ 微分方程：两边乘 xⁿ 求和，Σn(n−1)aₙxⁿ⁻²=S″、Σn aₙxⁿ⁻¹=S′、
        Σaₙxⁿ=S；指标平移时落单的首项必须显式保留。
      傅里叶收敛定理（狄利克雷）：f 在一个周期上分段单调、只有有限个第一类间断点
        ⇒ 其傅里叶级数处处收敛，连续点收敛到 f(x)，间断点收敛到
        [f(x⁻)+f(x⁺)]/2。**求点值不需要系数。**
      奇偶延拓：[0,l] 上的 f 作奇延拓得正弦级数（只有 bₙ）、作偶延拓得余弦级数
        （只有 aₙ），两者延拓后周期均为 2l。
      反常积分的两个参照物（方向相反）：
        瑕点：∫₀¹ dx/x^p 收敛 ⟺ **p<1**；无穷端：∫₁^∞ dx/x^p 收敛 ⟺ **p>1**。
        多个奇点时必须拆成每段只含一个奇点，各段独立判定，全部收敛才算整体收敛。
        对数因子比任何正幂弱：(ln x)^α = o(x^ε) 对任意 ε>0，故只在 p 恰为临界值
        时决定成败（∫₂^∞ dx/(x(ln x)^q) 收敛 ⟺ q>1）。
        积分判别法（P4）正是这两套参照物之间的桥：f 非负连续单调减时
        Σf(n) 与 ∫f 同敛散，故 Σ1/n^p 与 ∫₁^∞dx/x^p 的判据一致（都要 p>1）。
    why_action_follows: |
      本族的一切操作只服务于两个目标之一：**判定一个极限存在与否**（敛散），
      或**把级数凑成一个已知的闭式**（求和 / 展开）。
      判敛散的全部机制是「与已知敛散的参照物比较」——Σ1/n^p 之于比较判别法、
      ∫dx/x^p 之于反常积分的定阶（两者由积分判别法连成同一套参照系）、
      几何级数之于比值与根值、∫f 之于积分判别法、交错的部分和夹逼之于莱布尼茨。
      求和与展开的全部机制是「凑成 Σxⁿ=1/(1−x) 及其三个亲戚」——逐项求导与
      逐项积分是把通项里的 n 搬掉的两个互逆操作，部分分式与拆项是把复合结构
      还原成这四式的线性组合，造辅助幂级数是把没有 x 的问题人为地放进有 x 的
      框架里以便使用这些操作。
      幂级数的收敛域之所以必须分两步（R 与端点），是因为阿贝尔定理只对
      |x−x₀|<R 与 |x−x₀|>R 两侧给结论，**在 |x−x₀|=R 上它沉默**——
      端点不是「顺带」的，是定理明确不覆盖的区域。
      傅里叶部分之所以把「求点值」与「算系数」分成两条 route，是因为收敛定理
      直接给出点值，不经过系数；把它们混成一条会让每道点值题都退化成积分计算。

  failure_boundaries:
    - boundary_id: F1
      changed_condition: 在幂级数的收敛端点上沿用求 R 时用的那次比值（或根值）判别法
      effect: becomes_inconclusive
      explanation: 求 R 的判别法在端点必然给出 ρ=1，而 ρ=1 正是该判别法不给结论的情形；这是判别法本身的性质，不是算错
      witness:
        kind: constructed_counterexample
        ref: |
          取 Σ_{n≥1} xⁿ/n 与 Σ_{n≥1} xⁿ/n²。两者的 |aₙ₊₁/aₙ| → 1，故 R 均为 1。
          在端点 x=1 处代入，比值判别法给出的 ρ 仍为：
            前者 lim (1/(n+1))/(1/n) = 1；后者 lim (n/(n+1))² = 1。
          两个 ρ 都等于 1，但 Σ1/n 发散、Σ1/n² 收敛。
          ⇒ 同一个 ρ=1 对应相反的结论，该判别法在端点无法区分二者，
            becomes_inconclusive（不是给出错误答案，而是给不出答案）。
        verification: verified
      recovery: 端点代入后得到的是一个具体的数项级数，改用 P0/P1/Q1/Q2 重新判定
    - boundary_id: F2
      changed_condition: 跳过端点判定（D2），把收敛区间（开区间）直接当作收敛域交出
      effect: invalidates
      explanation: R 只决定开区间，不决定收敛域；跳过端点判定给出的是一个错误的集合，而不是一个不完整的结论
      witness:
        kind: constructed_counterexample
        ref: |
          三个幂级数的收敛半径都是 R=1（比值极限均为 1）：
            Σ_{n≥0} xⁿ      → x=1 时 Σ1 发散；x=−1 时 Σ(−1)ⁿ 发散 ⇒ 收敛域 (−1,1)
            Σ_{n≥1} xⁿ/n    → x=1 时 Σ1/n 发散；x=−1 时 Σ(−1)ⁿ/n 收敛 ⇒ 收敛域 [−1,1)
            Σ_{n≥1} xⁿ/n²   → x=±1 时均绝对收敛 ⇒ 收敛域 [−1,1]
          同一个 R=1 对应三个不同的收敛域。若一律写 (−1,1)，对后两个是错误答案。
          ⇒ 直接 invalidates，不是 becomes_inconclusive——它给出了一个确定而错误的集合。
        verification: verified
      recovery: "D1 的 followup 以 mode: sequence 强制转 D2；两个端点各代入判一次"
    - boundary_id: F3
      changed_condition: 对缺项幂级数（系数含零项）直接套 lim|aₙ₊₁/aₙ|
      effect: becomes_inconclusive
      explanation: 系数列有零项时相邻比值无定义或极限不存在，公式给不出 R
      witness:
        kind: constructed_counterexample
        ref: |
          Σ_{n≥0} x^{2n}。按 x 的幂次写成 Σ_{k≥0} c_k x^k，则 c_k=1（k 偶）、
          c_k=0（k 奇）。相邻比值 c_{k+1}/c_k 在 k 偶时为 0、k 奇时为 0/0 无定义，
          lim|c_{k+1}/c_k| 不存在 ⇒ 公式 R=1/ρ 无法使用。
          可用的两条路：① D4 换元 t=x²，Σtⁿ 的 R_t=1 ⇒ |x²|<1 ⇒ |x|<1；
          ② D3 上极限，limsup|c_k|^{1/k}=1 ⇒ R=1。两条都给出 R=1。
          ⇒ 直接套比值公式**得不到结论**（而非得到错误结论），becomes_inconclusive。
        verification: verified
      recovery: 换元 t=x^k（D4）或改用上极限（D3）
    - boundary_id: F4
      changed_condition: 把比较判别法（极限形式）用于非正项级数
      effect: invalidates
      explanation: 「aₙ/bₙ→λ∈(0,∞) ⇒ 同敛散」的证明依赖两级数正项；去掉该条件后结论不再成立
      witness:
        kind: constructed_counterexample
        ref: |
          取 bₙ = (−1)ⁿ/√n，aₙ = (−1)ⁿ/√n + 1/n（n≥1）。
          比值：aₙ/bₙ = 1 + (1/n)·(√n/(−1)ⁿ) = 1 + (−1)ⁿ/√n → 1 ∈ (0,∞)。
          但 Σbₙ 收敛（莱布尼茨：1/√n 单调减趋零），
          而 Σaₙ = Σbₙ + Σ1/n，其中 Σ1/n 发散 ⇒ Σaₙ 发散。
          ⇒ aₙ/bₙ→1 却一敛一散，极限形式的比较判别法在非正项级数上给出**错误结论**，
            invalidates。
        verification: verified
      recovery: 先取绝对值转为正项级数（此时判的是绝对收敛），或改走 Q1/Q2/Q4
    - boundary_id: F5
      changed_condition: 用莱布尼茨判别法时只验通项趋零、不验单调减
      effect: becomes_inconclusive
      explanation: 单调性缺失时收敛与发散都可能发生，仅凭「aₙ→0」无法确定任一方向
      witness:
        kind: constructed_counterexample
        ref: |
          两个都满足 aₙ>0、aₙ→0 但不单调的构造，结论相反：
          ① 发散侧：aₙ = 1/√n + (−1)ⁿ/n（n≥2；因 n≥2 时 1/√n>1/n，故 aₙ>0）。
             Σ(−1)ⁿaₙ = Σ(−1)ⁿ/√n + Σ1/n。前者收敛（莱布尼茨），
             后者发散 ⇒ 整体发散。
          ② 收敛侧：aₙ = (2+(−1)ⁿ)/n²（在 1/n² 与 3/n² 之间跳动，不单调）。
             |(−1)ⁿaₙ| ≤ 3/n²，Σ3/n² 收敛 ⇒ 绝对收敛。
          两例都满足「aₙ>0 且 aₙ→0 且不单调」，一发散一收敛。
          ⇒ 去掉单调性后判别法**不给结论**，becomes_inconclusive。
        verification: verified
      recovery: 补验单调性（作差或对应函数求导）；确实不单调时改走 Q2 的两级判定或 Q4 的部分和分析
    - boundary_id: F6
      changed_condition: 只判出 Σ|aₙ| 发散就断言「条件收敛」
      effect: becomes_inconclusive
      explanation: 「Σ|aₙ| 发散」只排除绝对收敛；剩下的两种可能（条件收敛、发散）仍未区分，必须再判 Σaₙ 本身
      witness:
        kind: constructed_counterexample
        ref: |
          两个级数的第一级判定结果完全相同，最终结论却不同：
          ① aₙ = (−1)ⁿ/n：Σ|aₙ| = Σ1/n 发散；Σaₙ 收敛（莱布尼茨）⇒ **条件收敛**。
          ② aₙ = 1/n：    Σ|aₙ| = Σ1/n 发散；Σaₙ = Σ1/n 也发散 ⇒ **发散**。
          两例的第一级都是「Σ|aₙ| 发散」，若就此断言条件收敛，对 ② 是错的。
          ⇒ 仅凭第一级无法区分，becomes_inconclusive；第二级判定不可省。
        verification: verified
      recovery: 补做第二级：对 Σaₙ 本身用 Q1（交错）或 Q4（部分和）判一次
    - boundary_id: F7
      changed_condition: 级数由绝对收敛变为条件收敛
      effect: changes_branch
      explanation: 不是同一条路线失效或变慢，而是后继可用的操作整体换了一组——三类操作从合法变为非法，必须切换到另一分支
      witness:
        kind: proof
        ref: >
          设 Σaₙ 收敛。若绝对收敛，以下三条成立；若仅条件收敛，三条全部不成立：
          ① **重排**：绝对收敛级数的任意重排收敛到同一个和。条件收敛时由黎曼重排
             定理，存在重排使其收敛到任意指定实数或发散到 ±∞——「先重排再分组求和」
             这条 route 从合法变为非法。
          ② **取绝对值后用正项判别法**：绝对收敛时 Σ|aₙ| 收敛，可用 P1–P4 处理并把
             结论搬回 Σaₙ；条件收敛时 Σ|aₙ| 发散，这条通道整体不可用，只剩
             Q1（莱布尼茨）与 Q4（部分和）。
          ③ **乘有界数列**：绝对收敛时对任意有界 {bₙ}，Σaₙbₙ 绝对收敛；条件收敛时
             取 bₙ = sign(aₙ)（有界）即得 Σaₙbₙ = Σ|aₙ| 发散。
          三条共同说明：可用 action 集合发生了替换，而不是某一条 action 的效力下降
          （那会是 loses_advantage）或无法定论（那会是 becomes_inconclusive）。
          ⇒ changes_branch。
        verification: verified
      recovery: 判明是条件收敛后，把后继限制在 Q1 / Q4，并放弃重排与「取模再比较」两类操作
    - boundary_id: F8
      changed_condition: 积分判别法中去掉 f 的单调减条件
      effect: invalidates
      explanation: 单调性是把 Σf(n) 夹在 ∫f 两侧的关键；缺了它，积分收敛与级数收敛可以背离
      witness:
        kind: constructed_counterexample
        ref: |
          构造 f：在每个整数 n≥1 处放一个底宽 4^{−n}、高为 1 的三角尖峰
          （即 f(n)=1，在 [n−4^{−n}/2, n+4^{−n}/2] 外 f=0，其间线性插值）。
          f 在 [1,∞) 上非负、连续，且 aₙ=f(n)=1。
          积分：∫₁^∞ f = Σ_{n≥1} (底×高/2) = Σ_{n≥1} 4^{−n}/2 = (1/2)(1/3) = 1/6 < ∞，
            **收敛**。
          级数：Σ_{n≥1} f(n) = Σ_{n≥1} 1，通项不趋零，**发散**。
          f 满足非负与连续，唯独不单调。
          ⇒ 去掉单调性后判别法给出**错误结论**（积分收敛却断言级数收敛），invalidates。
        verification: verified
      recovery: 核验单调性；f 不单调时本 route 不适用，改走 P1 定阶比较
    - boundary_id: F9
      changed_condition: 把「逐项求导/积分后 R 不变」推广成「收敛域不变」
      effect: invalidates
      explanation: R 不变是定理，收敛域不变不是；端点的敛散性在逐项操作下会改变
      witness:
        kind: constructed_counterexample
        ref: |
          从 Σ_{n≥1} xⁿ/n² 出发，连续逐项求导两次，三个级数的 R 恒为 1：
            原级数 Σxⁿ/n²      ：x=1 → Σ1/n² 收敛；x=−1 → 绝对收敛
                                 ⇒ 收敛域 **[−1,1]**
            一次求导 Σxⁿ⁻¹/n    ：x=1 → Σ1/n 发散；x=−1 → Σ(−1)ⁿ⁻¹/n 收敛
                                 ⇒ 收敛域 **[−1,1)**
            二次求导 Σ(n−1)xⁿ⁻²/n：x=±1 → 通项 →1≠0，均发散
                                 ⇒ 收敛域 **(−1,1)**
          R 三次都是 1，收敛域却逐次收缩。沿用原收敛域会把发散点写进答案。
          ⇒ 给出错误的集合，invalidates。
        verification: verified
      recovery: 逐项操作后对新级数的两个端点各重判一次（D6 强制转 D2）
    - boundary_id: F10
      changed_condition: 求和函数时方向选反（n 在分母却先逐项积分）
      effect: loses_advantage
      explanation: 反方向仍是合法的逐项操作，结论也不会错，只是把通项推得离几何级数更远，不化简
      witness:
        kind: constructed_counterexample
        ref: |
          S(x) = Σ_{n≥1} xⁿ/n（n 在分母），|x|<1。
          正确方向（先求导）：S′(x) = Σ_{n≥1} xⁿ⁻¹ = 1/(1−x)，
            回积分 S(x) = S(0) + ∫₀ˣ dt/(1−t) = −ln(1−x)。两步到闭式。
          反方向（先积分）：∫₀ˣ S(t)dt = Σ_{n≥1} x^{n+1}/(n(n+1))。
            分母从 n 变成 n(n+1)，比原来更复杂；要回到几何级数还需先裂项
            1/(n(n+1)) = 1/n − 1/(n+1)，把问题退回到原级数本身。
          两条都在 (−1,1) 内逐项进行，都合法，都不会给出错误的和。
          ⇒ 只是效率下降，loses_advantage；该 route **不排除**（效率低 ≠ 非法）。
        verification: verified
      recovery: 按「分母有 n → 先求导、分子有 n → 先积分」重选方向；反方向的中间结果仍可用
    - boundary_id: F11
      changed_condition: 傅里叶级数在间断点取 f(x₀) 而非 [f(x₀⁻)+f(x₀⁺)]/2
      effect: invalidates
      explanation: 收敛定理在间断点给出的是左右极限的平均；取 f(x₀) 直接得到错误的值
      witness:
        kind: constructed_counterexample
        ref: |
          f(x)=x 于 (−π,π)，以 2π 为周期延拓。其傅里叶级数为
            2Σ_{n≥1} (−1)ⁿ⁺¹ sin(nx)/n。
          在 x=π 处：每一项 sin(nπ)=0，故级数的和恒为 **0**。
          而 f(π⁻)=π、延拓后 f(π⁺)=f(−π⁺)=−π，平均值 (π+(−π))/2 = 0 ✓ 与级数一致。
          若在该间断点取 f(π)=π，则断言「级数收敛于 π」，与级数实际的和 0 矛盾。
          ⇒ 给出错误的值，invalidates。
        verification: verified
      recovery: 取值前先判该点在延拓后函数中是否为间断点；是则取左右极限的平均
    - boundary_id: F12
      changed_condition: 延拓方向与所求级数类型不符（要正弦级数却作偶延拓）
      effect: invalidates
      explanation: 同一个 f 的正弦级数与余弦级数是两个不同的级数，在同一点可以收敛到不同的值
      witness:
        kind: constructed_counterexample
        ref: |
          f(x) ≡ 1 于 [0,π]。
          偶延拓 → 余弦级数：a₀=(2/π)∫₀^π 1 dx=2，
            aₙ=(2/π)∫₀^π cos nx dx = 0 (n≥1) ⇒ 级数 = a₀/2 = **1**（对一切 x）。
          奇延拓 → 正弦级数：bₙ=(2/π)∫₀^π sin nx dx = 2(1−(−1)ⁿ)/(nπ)，
            即 b_{2k}=0、b_{2k−1}=4/((2k−1)π)
            ⇒ 级数 = (4/π)Σ_{k≥1} sin((2k−1)x)/(2k−1)。
          在 x=0 处：余弦级数给 1；正弦级数每项都是 sin 0 = 0，给 **0**。
          （0 正是奇延拓后 f 在 x=0 的左右极限平均 (−1+1)/2，与收敛定理一致。）
          两个级数在同一点给出不同的值 ⇒ 延拓方向选错直接给出错误答案，invalidates。
        verification: verified
      recovery: 先读设问要正弦还是余弦级数，据此定延拓方式；延拓后周期为 2l
    - boundary_id: F13
      changed_condition: 系数递推翻译成微分方程时，指标平移后落单的首项被漏掉
      effect: invalidates
      explanation: 漏掉首项会把非齐次方程写成齐次方程，解出的和函数与初始系数矛盾
      witness:
        kind: constructed_counterexample
        ref: |
          递推 (n+1)a_{n+1} = (n+1/2)aₙ（n≥1），a₁=1，S(x)=Σ_{n≥1} aₙxⁿ。
          S′(x) = Σ_{n≥1} n aₙ x^{n−1}；令 m=n−1 得 Σ_{m≥0}(m+1)a_{m+1}x^m
                = a₁ + Σ_{m≥1}(m+1)a_{m+1}x^m       ← **落单的首项 a₁=1**
                = 1 + Σ_{m≥1}(m+1/2)aₘx^m = 1 + xS′(x) + S(x)/2。
          正确方程：(1−x)S′ − S/2 = 1，即 S′ − S/(2(1−x)) = 1/(1−x)；
            积分因子 (1−x)^{1/2}，解得 S = C/√(1−x) − 2，由 S(0)=0 得 C=2，
            故 S(x) = 2/√(1−x) − 2。（核对：S′(0)=1=a₁ ✓）
          漏掉首项 1 则得齐次方程 (1−x)S′ = S/2 ⇒ S = C/√(1−x)，
            由 S(0)=0 得 C=0 ⇒ S ≡ 0。但 a₁=1 ⇒ S(x)=x+… 不恒为零，矛盾。
          ⇒ 漏首项给出恒零的错误解，invalidates。
        verification: verified
      recovery: 每次指标平移都写出平移后的起点，并把落单的首项显式列出后再合并
    - boundary_id: F14
      changed_condition: 用逐项求导/积分定理去求幂级数在收敛端点处的和
      effect: becomes_inconclusive
      explanation: 该定理只在开区间 (x₀−R, x₀+R) 内部成立，对端点不给任何结论；端点的和须另由和函数在收敛域上的连续性取单侧极限
      witness:
        kind: constructed_counterexample
        ref: |
          S(x) = Σ_{n≥1} xⁿ/n²，收敛域 [−1,1]（端点绝对收敛），R=1。
          在 (−1,1) 内可逐项求导：S′(x) = Σ_{n≥1} xⁿ⁻¹/n = −ln(1−x)/x（x≠0）。
          欲求 S(1)：逐项求导定理的适用范围是 |x|<1，**x=1 不在其中**——
            该定理对 x=1 不作任何断言；而 S′(x) = −ln(1−x)/x 在 x→1⁻ 时趋于 +∞，
            从导数一侧也读不出 S(1) 的值。
          正确通道（G4）：和函数在收敛域 [−1,1] 上连续，故
            S(1) = lim_{x→1⁻} S(x) = Σ_{n≥1} 1/n² = π²/6。
          ⇒ 逐项求导/积分定理在端点**给不出结论**（而非给出错误结论），
            becomes_inconclusive。
        verification: verified
      recovery: 端点的和改走 G4（先由 D2 确认端点收敛，再取内部的单侧极限）
    - boundary_id: F15
      changed_condition: 设问从「判敛散」变为「求和」
      effect: changes_branch
      explanation: 整条路由从判别法格（numeric_positive / numeric_alternating_general）切换到求和格（numeric_sum / power_sum），可用 action 集合几乎不相交
      witness:
        kind: proof
        ref: >
          判敛散的 route（P1–P4、Q1、Q2）产生的是一个二值结论，其中多数
          **在结构上不产生和的值**：比值判别法给出 ρ<1 即收敛，整个推导中不出现
          Sₙ 的任何闭式；根值、积分判别法、绝对收敛判定同理。
          求和的 route（N1–N3、G1–G4、W5）都需要一个可操作的闭式表示——
          造辅助幂级数、逐项求导积分凑几何级数、裂项相消、由收敛定理取值——
          而判别法不提供其中任何一个。
          反向也不成立：Q4（部分和裂项）确实同时给出敛散与和，但它是两格的交集，
          不能代表整体；由「Σ1/n² 收敛」这一判定无法得出其和为 π²/6。
          因此设问改变时，不是某条 route 失效（invalidates）、变慢
          （loses_advantage）或无法定论（becomes_inconclusive），而是整个候选集
          被另一个候选集替换。⇒ changes_branch。
        verification: verified
      recovery: 先定 task 轴再入格；求和设问直接进 numeric_sum / power_sum，不要先做一遍敛散判定

    - boundary_id: F16
      changed_condition: 把瑕点的 p 判据与无穷端的 p 判据方向弄反
      effect: invalidates
      explanation: 两类奇点的收敛方向相反——瑕点 p<1 收敛、无穷端 p>1 收敛；记反即对同一个 p 给出相反的错误结论
      witness:
        kind: constructed_counterexample
        ref: |
          取同一个被积函数 1/x^p，在两类奇点上比较：
          · p = 1/2：
              瑕点端 ∫₀¹ x^{−1/2}dx = [2√x]₀¹ = 2，**收敛**（p<1 ✓）
              无穷端 ∫₁^∞ x^{−1/2}dx = [2√x]₁^∞ = ∞，**发散**（p≤1 ✓）
          · p = 2：
              瑕点端 ∫₀¹ x^{−2}dx = [−1/x]₀¹ = ∞，**发散**（p≥1 ✓）
              无穷端 ∫₁^∞ x^{−2}dx = [−1/x]₁^∞ = 1，**收敛**（p>1 ✓）
          同一个 p 在两类奇点上结论恰好相反。若把方向记反，p=1/2 时会判 ∫₀¹ 发散、
          p=2 时会判 ∫₀¹ 收敛，两次都错。
          ⇒ 给出确定而错误的结论，invalidates。
        verification: verified
      recovery: 定阶后先确认奇点类型再套判据；口诀「瑕点怕大、无穷怕小」（瑕点 p 大则发散，无穷端 p 小则发散）
    - boundary_id: F17
      changed_condition: 反常积分有多个奇点时只判其中一个就下整体结论
      effect: invalidates
      explanation: 各奇点的敛散性彼此独立；只判一个会漏掉另一个，给出错误的整体结论。与幂级数收敛域跳过端点（F2）是同一条结构规则
      witness:
        kind: constructed_counterexample
        ref: |
          ∫₀^∞ x^{−1/2} dx 同时有两个奇点：x=0（瑕点）与 x=+∞。
          · 只判 x→0⁺：p=1/2<1 ⇒ 该端**收敛**。据此断言整体收敛。
          · 但 x→+∞ 端：p=1/2≤1 ⇒ 该端**发散**。
          实际 ∫₀^∞ x^{−1/2}dx = [2√x]₀^∞ = ∞，整体发散。
          反向的例子同样存在：∫₀^∞ x^{−2}dx 只判无穷端（p=2>1 收敛）会漏掉 x=0
          端的发散（p=2≥1），实际也发散。
          而 ∫₀^∞ x^{−1/2}e^{−x}dx 两端都收敛，整体才收敛。
          ⇒ 单点判定无法决定整体，据其下结论直接给出错误答案，invalidates。
        verification: verified
      recovery: 用 I1 先找全奇点并拆成每段只含一个奇点，逐段判定，全部收敛才判整体收敛

  revision_history:
    retired_rule_counterexamples:
      - retired_rule: "幂级数的收敛域由 R 决定：求出 R 即写出 (x₀−R, x₀+R)（v1 草案规则，已删除）"
        why_wrong: R 只决定开区间；两个端点的敛散性是独立信息，必须各判一次
        counterexample: |
          Σxⁿ、Σxⁿ/n、Σxⁿ/n² 的 R 都是 1，收敛域分别为 (−1,1)、[−1,1)、[−1,1]。
        verification: verified
        note: 这是旧规则的反例；当前规则已把端点判定拆成独立 action D2（对应 F2 的执行边界）
      - retired_rule: "「n 在分母先积分、n 在分子先求导」（草案曾照抄 高数方法速查.md §9，方向写反，已更正）"
        why_wrong: 求导才能消掉分母的 n（xⁿ/n → xⁿ⁻¹），积分才能消掉分子的 n（n xⁿ⁻¹ → xⁿ）
        counterexample: |
          Σxⁿ/n 先求导得 Σxⁿ⁻¹=1/(1−x)，两步得 −ln(1−x)；
          先积分则得 Σx^{n+1}/(n(n+1))，分母更复杂，未化简。
        verification: verified
        note: >
          本族采用 高数逐考点认知模型.md 模块 9 的写法（分母→求导、分子→积分）。
          `高数方法速查.md` §9 的相反写法未由本族修改（不是本族的文件），
          已写入 §5 open_questions 交 integrator 裁定

  fallback_policy:
    when_guard_fails:
      - 比值/根值给出 ρ=1 → 该判别法不给结论，改走 P1 定阶比较（端点情形见 F1）
      - 比较判别法的正项前提失败 → 先取绝对值判绝对收敛，或改走 Q1/Q2/Q4
      - 莱布尼茨的单调性失败 → 改走 Q2 的两级判定或 Q4 的部分和分析
      - Σ|aₙ| 发散 → **不得**据此断言条件收敛，必须再判 Σaₙ 本身（F6）
      - 比值公式因系数有零项失效 → 换元 t=x^k（D4）或上极限（D3）
      - 端点判定用求 R 的判别法失效 → 端点级数换判据重判（F1）
      - 逐项求导/积分定理不覆盖端点 → 端点的和改走 G4（和函数在收敛域上连续，F14）
      - 间接法凑不出四个标准展开式 → 走 X4 直接法，但必须另验余项趋零
      - 求点值时延拓方式未定 → 回 W2 按设问要求的级数类型重定，不可自选（F12）
      - 递推翻译出的方程解不动 → 改走 C3（一阶递推直接解通项）；仍不动则记 pending
    candidate_actions: [P1, Q2, Q4, D2, D3, D4, G4, X4]

  evidence:
    positive_problem_ids:
      解答: [2005-16, 2006-17, 2007-20, 2008-19, 2010-18, 2012-17, 2013-16,
             2020-17, 2021-18]
      选择填空: [2004-9, 2006-9, 2008-11, 2009-4, 2010-3, 2011-2, 2013-3,
                2015-3, 2016-1, 2017-12, 2018-3, 2019-3, 2019-11, 2020-4,
                2023-4, 2023-13, 2024-3, 2025-2, 2025-12, 2026-2]
      count: 29
      scope_note: >
        v1.1.0：27（级数主考点实测）+ 2（2010-3、2016-1，HANDOFF 裁定 SB-4 判归本族）
        = **29**。⚠️ SB-4 原文写「这两题已计入上述 27」与事实不符——27 是排除这两题
        算出的，见 §5 open_questions 第 1 条。以下为 v1.0.0 的原始说明：
        batch2_plan 曾标 scope_problems: 33；按 考点标注.tsv 的**主考点**实测 27。
        差异 6 题的来源见 §2 与 §5 open_questions：按 scope_boundary_rule
        「归属看主考点、次考点不得计入 scope_problems」，只命中次考点的 5 题
        （2004-18、2009-16、2014-19、2016-19、2019-17）不计入；
        主考点为「反常积分的敛散性判别」的 2 题（2010-3、2016-1）亦不计入。
        本族以实测 27 为准，如实上报，不虚增。
    candidate_set_completeness:
      global_exhaustiveness: not_established
      route_scan_status: complete_within_declared_universe
      route_scan_status_note: >
        八格全部扫完（本族首建）。`complete_within_declared_universe` 的含义是
        **每格的 11 项 completion_criteria 全部满足**，不是「不可能再有别的路线」。
        后者由 `global_exhaustiveness: not_established` 承载。
        禁用词：unique / exhaustive / all possible routes / globally saturated。
      series_completion_gate:
        all_nine_cells_scanned: true
        no_open_direct_counter_witness: true
        no_open_schema_blocker: true
        reachable_S3_typing_complete_for_all_scanned_cells: true
        route_scan_status: complete_within_declared_universe
        global_exhaustiveness: not_established
        pedagogical_validation: untested
        gate_met: true
        ready_for_independent_review: true
        author_status_change: none        # candidate → 更高状态只能由 GPT 判
      route_scan_by_cell:
        numeric_positive:
          cell_id: numeric_positive
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 正项级数的敛散性判定
          route_universe: >
            对正项级数可执行的判定操作（先想结构，后看真题）：
            ① 必要条件筛查（P0）；② 与 Σ1/n^p 定阶比较，含极限形式（P1）；
            ③ 比值判别法（P2）；④ 根值判别法（P3）；⑤ 积分判别法（P4）；
            ⑥ 部分和有界性（正项级数收敛 ⟺ 部分和有界）——归 Q4 的机制，
            在本格作为 P1 失效时的兜底，不单设 action。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述六类）+ 对每条 action 的 applies_when 做本格适用性
            逐条比对 + 为每条新 route 构造或检索一个 failure boundary。
            **未做**：第二轮独立重扫、外部教材判别法清单的系统比对。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: SP1, action: P0, mechanism: 通项不趋零即发散, applies_when: 任何数项级数的第一步, failure_boundary: "「趋零」不蕴含收敛（必要非充分）" }
            - { route_id: SP2, action: P1, mechanism: 定阶到 1/n^p 后比较, applies_when: 通项主项是 n 的幂次, failure_boundary: F4（正项前提） }
            - { route_id: SP3, action: P2, mechanism: 相邻两项之比, applies_when: 含阶乘/n 次幂/连乘, failure_boundary: ρ=1 失效（F1 同机制） }
            - { route_id: SP4, action: P3, mechanism: n 次根, applies_when: 通项整体是 n 次方形式, failure_boundary: ρ=1 失效 }
            - { route_id: SP5, action: P4, mechanism: 与 ∫_N^∞ f 同敛散, applies_when: aₙ=f(n) 且 f 非负连续单调减, failure_boundary: F8（单调性） }
          new_routes_this_scan: [P4]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: P4, name: 积分判别法, reason: "任务书明确要求本族接住「比较/比值/根值/积分」四条；且它是 Σ1/(n ln^q n) 型定阶的唯一直接判据，P1 对该型需先知道结论" }
            routing_defects_fixed: []
            new_failure_boundaries: [F4, F8]
            excluded_candidates:
              - { candidate: 部分和有界性判据, reason: duplicate_mechanism, note: "正项级数收敛 ⟺ Sₙ 有界，机制已由 Q4（部分和直接分析）承载，不在本格重复设 action" }
              - { candidate: 拉阿贝（Raabe）判别法, reason: out_of_scope, note: 超数学一大纲；ρ=1 时的细化判据不在考查范围 }
              - { candidate: 库默尔/高斯判别法, reason: out_of_scope, note: 超大纲 }
              - { candidate: 柯西凝聚判别法, reason: dominated_not_excluded, note: "对 Σ1/(n ln^q n) 合法且可执行，但与 P4 机制重叠且书写更繁；**不删除**——效率低不等于非法" }
            focused_checks_required_by_protocol:
              positivity_is_necessary: "P1–P4 全部要求正项；非正项级数须先转正项或换格（F4）"
              rho_equals_one: "ρ=1 是 P2/P3 的失效点，不是「不收敛」；须换判据"
              monotonicity_in_integral_test: "P4 的单调性不可默认，缺失即失效（F8）"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "ρ=1 之后的细化判别法（Raabe 等）判为超纲而未纳入；若大纲变动需重审（backlog）"
            - "未做第二轮独立重扫；global_exhaustiveness: not_established 常设（backlog）"
          positive_instance_mapping:
            note: >
              真题只作为「该路线历史上确实出现过 / 可映射到实例」的证据之一。
              **未出现不构成排除，出现也不自动升为高优先级或必要候选。**
              题面库 source_status 为 ocr_uncertain，故 evidentiary_weight 一律为 none，不是 witness。
            instances:
              - { action_id: P1, ref: "2004-9,  basis: \"lim n aₙ=λ≠0 ⇒ aₙ~λ/n，与调和级数同敛散\"", evidentiary_weight: none }
              - { action_id: P1, ref: "2025-2,  basis: \"sin(n³π/(n²+1)) 化简后定阶 ~π/n（该题主路由在交错格，本格只提供定阶子步骤）\"", evidentiary_weight: none }
              - { action_id: P4, ref: "null,    basis: \"2004–2026 未检出以积分判别法为主解的题（not_found，非「不存在」）\"", evidentiary_weight: none }
            not_found:
              - "积分判别法（P4）的独立真题未检出；该 route 由任务书要求与结构合法性保留，不由真题要求"
        improper_integral:
          cell_id: improper_integral
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scanned_in_version: 1.1.0
          scope: 反常积分（无穷限与瑕积分）的敛散性判别；不含求值
          route_universe: >
            对反常积分判敛散可执行的操作：① 找全奇点并拆段，每段只含一个奇点（I1）；
            ② 瑕点处定阶到 A/|x−c|^p，p<1 收敛（I2）；
            ③ 无穷端定阶到 A/x^p，p>1 收敛（I3）；
            ④ 对数因子的定阶处理（I4）；⑤ 被积变号时的绝对收敛判定（I5）；
            ⑥ 反用积分判别法（由 Σf(n) 的敛散反推 ∫f）——见 excluded_candidates。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述六类）+ 对两类奇点的判据方向做对照核查 +
            为「方向记反」与「漏判奇点」各构造一个 failure boundary（F16、F17）。
            **未做**：第二轮独立重扫；与一元积分模块（反常积分求值）的边界系统比对。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: II1, action: I1, mechanism: 找全奇点并拆段，逐段判定, applies_when: 判敛散的第一步, failure_boundary: F17 }
            - { route_id: II2, action: I2, mechanism: 瑕点定阶，p<1 收敛, applies_when: 该段端点为瑕点, failure_boundary: F16 }
            - { route_id: II3, action: I3, mechanism: 无穷端定阶，p>1 收敛, applies_when: 该段端点为 ±∞, failure_boundary: F16 }
            - { route_id: II4, action: I4, mechanism: 对数因子比任何正幂弱，只在临界 p 上起作用, applies_when: 含 (ln)^α 因子, failure_boundary: 在非临界 p 上误以为对数改变结论 }
            - { route_id: II5, action: I5, mechanism: ∫|f| 收敛 ⇒ ∫f 收敛, applies_when: 被积函数变号, failure_boundary: "∫|f| 发散时不给结论（与 F6 同型）" }
          new_routes_this_scan: [I1, I2, I3, I4, I5]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: I1, name: 拆分奇点, reason: "2010-3 有两个瑕点（x=0 与 x=1）、2016-1 同时有瑕点与无穷端；若不先拆段，两题都会被判成单点问题（F17）" }
              - { action_id: I2, name: 瑕点定阶, reason: 瑕积分的基本判据；参照物 ∫₀¹dx/x^p }
              - { action_id: I3, name: 无穷端定阶, reason: "无穷限积分的基本判据；参照物 ∫₁^∞dx/x^p。与 I2 方向相反，必须分设两个 action 而不是一个带参数的 action——合并会让 F16 无处安放" }
              - { action_id: I4, name: 对数因子定阶, reason: "2010-3 在 x→1⁻ 端的收敛完全由「对数比任何幂弱」决定；I2 单独无法给出该端结论" }
              - { action_id: I5, name: 绝对收敛判定, reason: "被积变号时 I2/I3 的定阶比较不适用（与数项级数 F4 同型）；结构上需要一条转正的通道" }
            routing_defects_fixed: []
            new_failure_boundaries: [F16, F17]
            excluded_candidates:
              - { candidate: 反常积分的计算/求值, reason: out_of_scope, note: "属一元积分模块（换元、分部、Γ 函数等）；本族 exclusions 已列明，只判敛散" }
              - { candidate: 反用积分判别法（由 Σf(n) 判 ∫f）, reason: dominated_not_excluded, note: "积分判别法是充要的，反向使用合法且可执行；但把积分问题转成级数问题通常更繁；**不删除**——效率低不等于非法" }
              - { candidate: 柯西主值, reason: out_of_scope, note: 超数学一大纲；本族的「收敛」一律指通常意义下各段独立收敛 }
              - { candidate: 狄利克雷/阿贝尔型的振荡积分判据（如 ∫₁^∞ sin x/x dx）, reason: out_of_scope, note: "含参振荡积分的条件收敛判据不在数学一大纲；被积变号的情形本族只走 I5（绝对收敛）" }
              - { candidate: 比较判别法的非极限形式（直接放缩）, reason: duplicate_mechanism, note: 与 I2/I3 的定阶比较同机制，不单设 action }
            focused_checks_required_by_protocol:
              direction_opposite: "瑕点 p<1 收敛、无穷端 p>1 收敛，方向相反（F16）——本格最集中的错误来源"
              all_singularities: "必须找全奇点（含积分限内部的无界点）并逐段判定（F17）"
              exponent_addition: "无穷端定阶时多个因子的幂要相加（2016-1 的 p 是 a+b，不是 b）"
              log_is_weaker: "对数因子比任何正幂弱，只在 p 恰为临界值时决定成败（2010-3 的 x→1⁻ 端）"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "本格仅 2 道真题（2010-3、2016-1），route universe 主要由大纲结构而非题目驱动（这符合 route scan 口径，但独立审时值得重点复核）"
            - "与一元积分模块「反常积分求值」的边界只在 exclusions 中一句话划定，未做逐题比对（backlog）"
            - "I5 无真题实例（not_found），保留为合法 route（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格（题面库 ocr_uncertain → evidentiary_weight 一律 none）
            instances:
              - { action_id: I1, ref: 2010-3, basis: "两个瑕点 x=0 与 x=1，各自单独判", evidentiary_weight: none }
              - { action_id: I2, ref: 2010-3, basis: "x→0⁺ 定阶 x^{2/m−1/n}，收敛需 2/m−1/n>−1", evidentiary_weight: none }
              - { action_id: I4, ref: 2010-3, basis: "x→1⁻ 端 |ln(1−x)|^{2/m} 为对数型奇点，任何幂次都可积 ⇒ 与 m、n 无关", evidentiary_weight: none }
              - { action_id: I1, ref: 2016-1, basis: "同时含瑕点 x→0⁺ 与无穷端 x→+∞，拆两段", evidentiary_weight: none }
              - { action_id: I2, ref: 2016-1, basis: "x→0⁺ 被积 ~1/x^a，收敛需 a<1", evidentiary_weight: none }
              - { action_id: I3, ref: 2016-1, basis: "x→+∞ 被积 ~1/x^{a+b}，收敛需 a+b>1（幂相加，不是 b）", evidentiary_weight: none }
              - { action_id: I5, ref: null,   basis: "2004–2026 未检出被积变号需走绝对收敛的题（not_found，非「不存在」）", evidentiary_weight: none }
            not_found:
              - "I5（绝对收敛判定）无真题实例；由大纲与结构合法性保留，不由真题要求"
        numeric_alternating_general:
          cell_id: numeric_alternating_general
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 交错级数与任意项级数的敛散性判定，含绝对收敛与条件收敛的区分
          route_universe: >
            对交错/任意项级数可执行的操作：① 必要条件筛查（P0）；
            ② 莱布尼茨判别法（Q1）；③ 绝对/条件收敛的两级判定（Q2）；
            ④ 反例排除（Q3）；⑤ 部分和直接分析：裂项、望远镜、收敛级数的线性组合（Q4）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: 同 numeric_positive 格；未做第二轮独立重扫
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: SA1, action: Q1, mechanism: 单调减 + 趋零, applies_when: 交错且去掉符号后定号, failure_boundary: F5 }
            - { route_id: SA2, action: Q2, mechanism: 先判 Σ|aₙ| 再判 Σaₙ, applies_when: 任意项或设问要求区分绝对/条件, failure_boundary: F6 · F7 }
            - { route_id: SA3, action: Q3, mechanism: 常备反例逐条击破, applies_when: 命题真假型选择题, failure_boundary: "找不到反例只能记 not_found，不能证成" }
            - { route_id: SA4, action: Q4, mechanism: 裂项/望远镜/线性组合求 Sₙ, applies_when: 通项可裂或是已知级数的线性组合, failure_boundary: Sₙ 无闭式时走不动 }
          new_routes_this_scan: [Q4]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: Q4, name: 部分和直接分析, reason: "2019-3 的 Σ(u_{n+1}²−uₙ²) 与 2006-9 的线性组合都不经过任何判别法，直接由 Sₙ 定论；若本格只有 Q1/Q2/Q3，这两题无合法 action 接收" }
            routing_defects_fixed: []
            new_failure_boundaries: [F5, F6, F7]
            excluded_candidates:
              - { candidate: 狄利克雷判别法 / 阿贝尔判别法, reason: out_of_scope, note: 超数学一大纲；任意项级数的一般判据不在考查范围 }
              - { candidate: 把 P1–P4 直接用于交错级数, reason: invalid, note: "F4：比较判别法的正项前提不可省；正确做法是先取绝对值（转 Q2 的第一级）" }
              - { candidate: 柯西收敛准则, reason: dominated_not_excluded, note: "对任意项级数合法且可执行，但在选择题时长内基本不可操作；**不删除**——效率低不等于非法" }
            focused_checks_required_by_protocol:
              two_level_verdict: "绝对/条件是三分（绝对收敛/条件收敛/发散），不是二分；两级各判一次（F6）"
              monotonicity_required: "莱布尼茨的单调性不可省（F5）"
              counterexample_is_one_way: "反例能否证，不能证成；未找到反例只能记 search_result: not_found"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "常备反例清单只列了两个（(−1)ⁿ/√n、1/(n ln n)）；可扩充但不影响 route 合法性（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格（题面库 ocr_uncertain → evidentiary_weight 一律 none）
            instances:
              - { action_id: Q1, ref: "2011-2,  basis: \"aₙ 单调减趋零 ⇒ Σ(−1)ⁿaₙ 收敛（该题用于定端点）\"", evidentiary_weight: none }
              - { action_id: Q1, ref: "2025-2,  basis: \"①化简辐角后为交错级数，|通项|~π/n ⇒ 条件收敛\"", evidentiary_weight: none }
              - { action_id: Q2, ref: "2009-4,  basis: \"aₙ→0 与 Σ|bₙ| 收敛的组合，逐项判绝对收敛\"", evidentiary_weight: none }
              - { action_id: Q2, ref: "2015-3,  basis: \"Σaₙ 条件收敛 ⇒ 幂级数 R=1（条件收敛点在边界上）\"", evidentiary_weight: none }
              - { action_id: Q2, ref: "2023-4,  basis: \"aₙ<bₙ 且两级数都收敛 ⇒ Σ|aₙ| 与 Σ|bₙ| 互为充要\"", evidentiary_weight: none }
              - { action_id: Q3, ref: "2006-9,  basis: \"四个命题判真假，A/B/C 用 aₙ=(−1)ⁿ/√n 击破\"", evidentiary_weight: none }
              - { action_id: Q4, ref: "2019-3,  basis: \"Σ(u_{n+1}²−uₙ²) 望远镜和，部分和 = u_{n+1}²−u₁² 有界\"", evidentiary_weight: none }
              - { action_id: Q4, ref: "2006-9,  basis: \"Σ(aₙ+aₙ₊₁)/2 是两个收敛级数的线性组合\"", evidentiary_weight: none }
            not_found: []
        numeric_sum:
          cell_id: numeric_sum
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 数项级数求和（不是判敛散——两者是不同分支，见 F15）
          route_universe: >
            对数项级数求和可执行的操作：① 造辅助幂级数再代值（N1，通法）；
            ② 凑已知展开式在某点的值（N2）；③ 裂项相消求 Sₙ 的极限（N3）；
            ④ 由傅里叶级数在特定点的收敛值反解（W5）；
            ⑤ 由已知展开式反读系数后对系数级数求和（X5）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: 同 numeric_positive 格；未做第二轮独立重扫
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: SS1, action: N1, mechanism: 补 xⁿ 造 S(x)，求出后代回特定点, applies_when: 直接看不出闭式且通项含 n 的幂次, failure_boundary: 目标点是端点时须转 G4（F14） }
            - { route_id: SS2, action: N2, mechanism: 拆通项凑标准展开式在某点的值, applies_when: 含 1/n!、1/(2n±1)! 等阶乘结构, failure_boundary: 代回时漏掉那个特定的 x }
            - { route_id: SS3, action: N3, mechanism: aₙ=bₙ−bₙ₊₁，Sₙ=b₁−bₙ₊₁, applies_when: 有理式且分母可分解成相邻因子, failure_boundary: 无法裂项时走不动 }
            - { route_id: SS4, action: W5, mechanism: 傅里叶收敛定理在某点给出等式，反解数项和, applies_when: 前一问是傅里叶展开, failure_boundary: 该点是间断点时须取左右平均（F11） }
            - { route_id: SS5, action: X5, mechanism: 反读 aₙ 后对系数级数求和, applies_when: 题面给 f 与其幂级数表示, failure_boundary: 反读的系数下标错位 }
          new_routes_this_scan: [W5, X5]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: W5, name: 由傅里叶收敛定理反解数项级数, reason: "2008-19 第二问由 x=0 处的收敛值解出 Σ(−1)ⁿ⁻¹/n²=π²/12；若数项求和格只有 N1–N3，该题的第二问无合法 action 接收（幂级数通道走不到 π²/12）" }
              - { action_id: X5, name: 由展开式反读系数后求和, reason: "2024-3 求 Σn a_{2n}，先由 ln(2+x) 的展开反读 aₙ 再求和；这是与 N1 相反的方向（不是造级数，而是读系数）" }
            routing_defects_fixed: []
            new_failure_boundaries: [F15]
            excluded_candidates:
              - { candidate: 阿贝尔求和法 / 生成函数的一般理论, reason: out_of_scope, note: 超数学一大纲 }
              - { candidate: 把判别法（P1–P4）用于求和, reason: invalid, note: "F15：判别法产生二值结论，结构上不产生和的值" }
              - { candidate: 数值逼近求和（取有限项估值）, reason: invalid, note: 真题要求精确闭式，截断不构成答案 }
              - { candidate: 用留数/复变方法求和, reason: out_of_scope, note: 超大纲 }
            focused_checks_required_by_protocol:
              different_branch_from_convergence: "求和与判敛散是不同格（F15），不要先做一遍敛散判定再求和"
              substitute_back: "N1 造完幂级数后**必须代回那个特定的 x**；这是本格最易漏的一步"
              endpoint_value: "目标点若是收敛端点，和须由 G4 取单侧极限（F14）"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "N1 造辅助幂级数时「补哪个 xⁿ」无系统算法，依赖通项结构识别（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格
            instances:
              - { action_id: N2, ref: "2018-3,  basis: \"(2n+3)/(2n+1)! 拆成 1/(2n)!+2/(2n+1)! ⇒ cos1+2sin1\"", evidentiary_weight: none }
              - { action_id: N3, ref: 2021-18, basis: "Σ1/(n(n+1)) 裂项相消得 1（该题 x=1 端点处的和）", evidentiary_weight: none }
              - { action_id: W5, ref: 2008-19, basis: "由 x=0 处收敛到 f(0)=1 解出 Σ(−1)ⁿ⁻¹/n²=π²/12", evidentiary_weight: none }
              - { action_id: X5, ref: "2024-3,  basis: \"由 ln(2+x) 的展开反读 aₙ，再求 Σn a_{2n}=−1/6\"", evidentiary_weight: none }
              - { action_id: N1, ref: "null,    basis: \"2004–2026 未检出以「造辅助幂级数」为主解的独立数项求和题（not_found）\"", evidentiary_weight: none }
            not_found:
              - "N1 作为主解的独立真题未检出；它是本格的通法（认知模型模块 9 明列），由结构合法性保留"
        power_domain:
          cell_id: power_domain
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 幂级数的收敛半径与收敛域（含由已知敛散数据逆向反推）
          route_universe: >
            对幂级数定收敛域可执行的操作：① 比值/根值求 R（D1）；
            ② 端点单独判定（D2）；③ 上极限求 R（D3）；④ 缺项换元 t=x^k（D4）；
            ⑤ 由已知敛散数据逆向反推 R 或收敛域，含中心平移与同系数迁移（D5）；
            ⑥ 逐项操作后的收敛域重判（D6）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述六类）+ 对「求 R」与「判端点」两个子任务分别做
            适用性比对 + 为端点相关的三个错误模式（沿用判别法、跳过判定、
            逐项操作后不重判）各构造一个 failure boundary。
            **未做**：第二轮独立重扫、外部教材清单比对。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: PD1, action: D1, mechanism: "ρ=lim|aₙ₊₁/aₙ| 或 lim|aₙ|^{1/n}，R=1/ρ", applies_when: 系数无零项且极限存在, failure_boundary: F3（缺项） }
            - { route_id: PD2, action: D2, mechanism: 两端点各代入一次，各自判敛散, applies_when: 设问要求收敛域且 R 有限非零, failure_boundary: F1（沿用判别法）· F2（跳过） }
            - { route_id: PD3, action: D3, mechanism: "1/R=limsup|aₙ|^{1/n}", applies_when: 系数振荡或按奇偶分支, failure_boundary: 取下极限会把 R 放大 }
            - { route_id: PD4, action: D4, mechanism: 令 t=x^k 求 R_t 再回代, applies_when: 缺项级数, failure_boundary: 回代后仍须判 x 的端点 }
            - { route_id: PD5, action: D5, mechanism: 阿贝尔定理定 R 的上下界；换中心时系数不变, applies_when: 逆向设问, failure_boundary: 端点方向搬迁写反 }
            - { route_id: PD6, action: D6, mechanism: 逐项操作 R 不变、端点须重判, applies_when: 题面对原级数做了逐项操作或乘 n, failure_boundary: F9 }
          new_routes_this_scan: [D2, D5, D6]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: D2, name: 端点单独判定, reason: "端点判定是与求 R 分离的独立步骤：R 不决定收敛域（F2），且求 R 的判别法在端点必失效（F1）。若并入 D1，这两个边界在 schema 上无处安放，且 followup 无法用 sequence 强制" }
              - { action_id: D5, name: 由已知敛散数据逆向反推, reason: "2008-11 / 2011-2 / 2015-3 / 2020-4 都不给通项，只给「某点收敛/发散」或「Σaₙ 条件收敛」；若本格只有正向的 D1/D2，这四题无合法 action 接收" }
              - { action_id: D6, name: 逐项操作后的收敛域, reason: "2015-3（Σn aₙ(x−1)ⁿ）、2020-4（取偶次子列）问的是新级数；R 不变但端点可变（F9），需要独立 action 承载这条规则" }
            routing_defects_fixed:
              - "草案曾把端点判定写成 D1 的一个 local_operation，导致 optional 语义下可被静默跳过；改为独立 action + mode: sequence 强制转接（S2 的「静默漏解」正是此类）"
            new_failure_boundaries: [F1, F2, F3, F9]
            excluded_candidates:
              - { candidate: 用柯西–阿达玛公式统一替代 D1/D3, reason: duplicate_mechanism, note: "D3 即上极限形式；D1 是其在极限存在时的常用特例，两者不重复设三个 action" }
              - { candidate: 逐点用比较判别法确定收敛域, reason: dominated_not_excluded, note: "对每个 x 单独判敛散在数学上合法且可执行，只是不利用阿贝尔定理的区间结构、代价高；**不删除**——效率低不等于非法" }
              - { candidate: 复平面上的收敛圆, reason: out_of_scope, note: 数学一只考实幂级数 }
              - { candidate: 一致收敛域的判定（M 判别法）, reason: out_of_scope, note: 一致收敛性的独立判定不在数学一大纲 }
            focused_checks_required_by_protocol:
              endpoints_are_independent: "端点判定是独立步骤（D2），不得并进 D1；两个端点各判一次，结果可以不同"
              rho_one_at_endpoint: "端点处求 R 用的判别法必然给 ρ=1，必须换判据（F1）"
              center_shift: "换中心时系数不变，端点处求的是同一个数项级数 Σaₙ(±R)ⁿ，敛散性整体搬迁（2008-11）"
              term_ops_change_endpoints: "逐项求导/积分/乘 n 不改 R，但改端点（F9）"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "「条件收敛点必在收敛边界上」这条强判据只在 D5 的 mechanism_note 里叙述，未单列 guard（backlog）"
            - "R=0 与 R=∞ 两个退化情形只在 D2 的 mechanism_note 中提及，未建表（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格
            instances:
              - { action_id: D1, ref: 2005-16, basis: "比值法得收敛区间 (−1,1)", evidentiary_weight: none }
              - { action_id: D1, ref: 2010-18, basis: "含 1/(2n−1) 的缺项级数，比值法定 R=1", evidentiary_weight: none }
              - { action_id: D2, ref: 2010-18, basis: "端点 x=±1 交错收敛 ⇒ 收敛域 [−1,1]", evidentiary_weight: none }
              - { action_id: D2, ref: "2026-2,  basis: \"端点 x=±1 时偶次项通项为 1 不趋零 ⇒ 发散，收敛域 (−1,1)\"", evidentiary_weight: none }
              - { action_id: D3, ref: "2026-2,  basis: \"系数 [(3+(−1)ⁿ)/4]ⁿ 按奇偶分支，limsup|aₙ|^{1/n}=1\"", evidentiary_weight: none }
              - { action_id: D4, ref: 2012-17, basis: "级数是关于 x² 的，先按 t=x² 定 R_t 再回代", evidentiary_weight: none }
              - { action_id: D5, ref: 2008-11, basis: "同一组 aₙ 换中心：由 (−4,0] 迁移到 (1,5]，端点方向随中心平移", evidentiary_weight: none }
              - { action_id: D5, ref: "2011-2,  basis: \"由 aₙ 单调减趋零、Sₙ 无界反推两端点敛散 ⇒ [0,2)\"", evidentiary_weight: none }
              - { action_id: D5, ref: "2015-3,  basis: \"Σaₙ 条件收敛 ⇒ 该点在边界上 ⇒ R=1\"", evidentiary_weight: none }
              - { action_id: D6, ref: "2015-3,  basis: \"Σn aₙ(x−1)ⁿ 与原级数同 R\"", evidentiary_weight: none }
              - { action_id: D6, ref: "2020-4,  basis: \"Σa_{2n}r^{2n} 是原级数的偶次子列，|r|<R 时绝对收敛\"", evidentiary_weight: none }
            not_found: []
        power_sum:
          cell_id: power_sum
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 幂级数求和函数（含端点处的和）
          route_universe: >
            对求和函数可执行的操作：① 先定收敛域（D1/D2，前置且必需）；
            ② 逐项求导消掉分母的 n（G1）；③ 逐项积分消掉分子的 n（G2）；
            ④ 拆项分解后分别求和再合并，含部分分式、多项式除法、提公因子、
            指标平移（G3）；⑤ 端点的和由和函数在收敛域上的连续性取单侧极限（G4）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述五类）+ 对「逐项操作的合法性范围」做 necessary /
            sufficient 定性核查 + 为端点处的和单独构造一个 failure boundary。
            **未做**：第二轮独立重扫。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: PS0, action: D1, mechanism: 先求 R 与收敛区间, applies_when: 求和函数题的前置步骤, failure_boundary: F3 }
            - { route_id: PS1, action: D2, mechanism: 端点单独判定, applies_when: 设问要求收敛域或涉及端点的和, failure_boundary: F1 · F2 }
            - { route_id: PS2, action: G1, mechanism: 逐项求导凑几何级数再积分还原, applies_when: 分母含 n, failure_boundary: F10（方向选反）· F14（端点） }
            - { route_id: PS3, action: G2, mechanism: 逐项积分凑几何级数再求导还原, applies_when: 分子含 n, failure_boundary: F10 · F14 }
            - { route_id: PS4, action: G3, mechanism: 拆项后分别求和，收敛域取交集, applies_when: 通项是复合结构, failure_boundary: 合并时收敛域未取交集 }
            - { route_id: PS5, action: G4, mechanism: 和函数在收敛域上连续 ⇒ 端点取单侧极限, applies_when: 设问要端点处的和, failure_boundary: 端点发散时不适用 }
          new_routes_this_scan: [G4]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: G4, name: 端点值由和函数连续性取极限, reason: "逐项求导/积分定理只覆盖开区间内部；2021-18 问 S(1) 而 x=1 是收敛端点。若本格只有 G1–G3，端点处的和无合法 action 接收（F14 给出完整构造）" }
            routing_defects_fixed:
              - "草案曾把「内闭一致收敛」写成逐项操作的 necessary guard；改为 sufficient——它是定理的充分条件，不满足不等于操作非法，且写成 necessary 会把端点通道 G4 一并否掉"
            new_failure_boundaries: [F10, F14]
            excluded_candidates:
              - { candidate: 用泰勒公式直接反推和函数, reason: duplicate_mechanism, note: "即 X5 的反读系数机制，归 expansion 格，不在本格重复设 action" }
              - { candidate: 常数变易 / 微分方程法求和函数, reason: duplicate_mechanism, note: "即 C1（递推→ODE）的机制，归 coefficient_recursion 格" }
              - { candidate: 逐项求导/积分的方向选反, reason: dominated_not_excluded, note: "F10：反方向仍是合法的逐项操作，只是不化简；**不删除**——效率低不等于非法" }
              - { candidate: 在收敛区间外做逐项操作, reason: invalid, note: 区间外级数发散，逐项操作的对象不存在 }
              - { candidate: 阿贝尔求和的一般理论, reason: out_of_scope, note: 超大纲；本族只用「和函数在收敛域上连续」这一条大纲内结论（G4） }
            focused_checks_required_by_protocol:
              legality_is_sufficient_not_necessary: "「内闭一致收敛 ⇒ 可逐项操作」是**充分**条件；不得标成 necessary"
              interior_only: "定理只覆盖 (x₀−R,x₀+R) 内部；端点走 G4（F14）"
              direction_rule: "分母有 n → 先求导；分子有 n → 先积分。选反只是低效（F10），不是错"
              constant_after_reverse: "逆操作后必须用 S(x₀) 补常数；x=0 等特殊点单独核对"
              domain_intersection: "G3 合并时收敛域取各部分的交集（2021-18 一部分 x>0、另一部分 [−1,1]）"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "「补常数」的定值点选取（S(0) 之外的情形）未一般化（backlog）"
            - "G3 拆项后各部分收敛域不同的合并规则只给了交集，未讨论并集侧的可延拓性（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格
            instances:
              - { action_id: G1, ref: 2010-18, basis: "Σ(−1)ⁿ⁻¹x^{2n}/(2n−1)：提 x 后分母含 (2n−1)，求导凑几何级数 ⇒ x·arctan x", evidentiary_weight: none }
              - { action_id: G1, ref: 2017-12, basis: "Σ(−1)ⁿ⁻¹n x^{n−1} 识别为 Σ(−1)ⁿ⁻¹xⁿ 的导数 ⇒ 1/(1+x)²", evidentiary_weight: none }
              - { action_id: G2, ref: 2012-17, basis: "Σ(2n+1)x^{2n} 分子含 (2n+1)，积分还原为 x/(1−x²) 再求导", evidentiary_weight: none }
              - { action_id: G3, ref: 2012-17, basis: "(4n²+4n+3)/(2n+1) 拆成 (2n+1)+2/(2n+1)，两部分分别求和", evidentiary_weight: none }
              - { action_id: G3, ref: 2005-16, basis: "方括号拆成两个级数分别求和后合并", evidentiary_weight: none }
              - { action_id: G3, ref: 2021-18, basis: "等比部分（x>0）与 Σx^{n+1}/(n(n+1))（[−1,1]）分别求和，定义域取交集 (0,1]", evidentiary_weight: none }
              - { action_id: G4, ref: 2021-18, basis: "x=1 是收敛端点，S(1) 由端点处的和给出（该点 Σ1/(n(n+1)) 裂项得 1）", evidentiary_weight: none }
            not_found: []
        expansion:
          cell_id: expansion
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 函数展开成幂级数（直接法与间接法），含由展开式反读系数
          route_universe: >
            对展开可执行的操作：① 换元凑四个标准展开式（X1）；
            ② 部分分式分解后分别展开（X2）；③ 先对 f′ 或 ∫f 展开再逐项逆操作
            并定常数（X3）；④ 直接法：算泰勒系数并验余项（X4）；
            ⑤ 由已知展开式反读系数并做系数运算（X5，逆向）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: 同 power_sum 格；未做第二轮独立重扫
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: EX1, action: X1, mechanism: 换元凑 1/(1−x)、eˣ、sin x、ln(1+x), applies_when: 一次代数换元可达, failure_boundary: 换元后收敛区间未跟着变 }
            - { route_id: EX2, action: X2, mechanism: 部分分式后各套几何级数并合并系数, applies_when: 有理函数且分母可分解, failure_boundary: 合并时收敛域未取交集 }
            - { route_id: EX3, action: X3, mechanism: 展开 f′ 或 ∫f 后逐项逆操作并补常数, applies_when: f 的导数易展开（arctan、arcsin 等）, failure_boundary: 忘补常数；逆操作后端点变化（同 F9 机制） }
            - { route_id: EX4, action: X4, mechanism: "aₙ=f^{(n)}(x₀)/n! 并验 Rₙ→0", applies_when: 高阶导数有通项且间接法不适用, failure_boundary: 漏验余项 }
            - { route_id: EX5, action: X5, mechanism: "反读 aₙ 后做系数运算或求 f^{(n)}(x₀)", applies_when: 逆向设问, failure_boundary: 系数下标错位 }
          new_routes_this_scan: [X5]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: X5, name: 由展开式反读系数, reason: "2019-11（由展开式反读和函数）与 2024-3（反读 aₙ 后求 Σn a_{2n}）都是逆向：给的是 f 与其级数表示，要的是系数或系数级数。若本格只有正向的 X1–X4，这两题无合法 action 接收" }
            routing_defects_fixed: []
            new_failure_boundaries: []
            excluded_candidates:
              - { candidate: 洛朗展开 / 负幂次项, reason: out_of_scope, note: 超数学一大纲 }
              - { candidate: 用待定系数法硬解展开系数, reason: dominated_not_excluded, note: "设 f=Σaₙxⁿ 后代入函数方程定系数在数学上合法且可执行（与 C2 同机制），只是对可凑四式的 f 更繁；**不删除**——效率低不等于非法" }
              - { candidate: 直接法（X4）, reason: null, note: "**未排除**：X4 保留为合法候选，2004–2026 无主解实例不构成排除理由（route scan 口径）" }
              - { candidate: 傅里叶展开, reason: duplicate_mechanism, note: 归 fourier 格；「展开」在本格特指幂级数展开 }
            focused_checks_required_by_protocol:
              domain_is_mandatory: "每条 route 都必须给出收敛域，不给即未完成；换元后收敛区间跟着变"
              remainder_for_direct: "X4 必须验余项：f 有各阶导数不蕴含泰勒级数收敛到 f"
              four_standard_forms: "间接法只以四个标准展开式为目标，不另记第五个"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "X4 的余项估计只要求「给出 Rₙ→0 的范围」，未按拉格朗日/柯西余项分型（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格
            instances:
              - { action_id: X1, ref: 2019-11, basis: "Σ(−1)ⁿxⁿ/(2n)! 令 t=√x 凑 cos ⇒ cos√x", evidentiary_weight: none }
              - { action_id: X2, ref: 2006-17, basis: "x/(2+x−x²) 部分分式后各套几何级数，合并得 (1/3)Σ[(1/2)ⁿ−(−1)ⁿ]xⁿ（−1<x<1）", evidentiary_weight: none }
              - { action_id: X5, ref: "2024-3,  basis: \"ln(2+x)=ln2+ln(1+x/2) 反读 aₙ=(−1)ⁿ⁻¹/(n·2ⁿ)，再求 Σn a_{2n}\"", evidentiary_weight: none }
              - { action_id: X5, ref: "2018-3,  basis: \"反用 sin1、cos1 的展开式识别系数结构（该题主路由在 numeric_sum 格）\"", evidentiary_weight: none }
              - { action_id: X4, ref: "null,    basis: \"2004–2026 未检出以直接法为主解的展开题（not_found，非「不存在」）\"", evidentiary_weight: none }
              - { action_id: X3, ref: "null,    basis: \"未检出以「先展开导数再逆操作」为主解的独立真题（not_found）\"", evidentiary_weight: none }
            not_found:
              - "X4（直接法）与 X3（先展开导数）无主解实例；两者均由大纲与结构合法性保留，不由真题要求"
        coefficient_recursion:
          cell_id: coefficient_recursion
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 幂级数系数递推与和函数（含代入微分方程求级数解）；ODE 族已把此型判归本族
          route_universe: >
            对系数递推可执行的操作：① 递推两边乘 xⁿ 求和，翻译成 S 满足的微分
            方程，解方程后由初值定常数（C1）；② 反向：设 y=Σaₙxⁿ 代入题面已给的
            微分方程，比较同次幂系数得递推，再解出通项（C2）；
            ③ 一阶递推直接连乘解出 aₙ 通项再求和（C3）；④ 先定收敛半径（D1）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述四类）+ 对「指标对齐」这一已知高危步骤构造一个
            failure boundary（F13，以 2020-17 的落单首项为例）。
            **未做**：第二轮独立重扫。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: CR1, action: C1, mechanism: 递推 → 乘 xⁿ 求和 → S 的微分方程 → 解 → 定常数, applies_when: 二阶或含 n 系数的一阶递推, failure_boundary: F13（指标未对齐） }
            - { route_id: CR2, action: C2, mechanism: 设 y=Σaₙxⁿ 代入方程比较同次幂系数, applies_when: 题面给方程与初值，要求级数解, failure_boundary: 求导后未统一幂次即比较系数 }
            - { route_id: CR3, action: C3, mechanism: 一阶递推连乘裂项直接得通项, applies_when: aₙ₊₁/aₙ 为 n 的有理式, failure_boundary: 比值无法约分时走不动 }
            - { route_id: CR4, action: D1, mechanism: 由 aₙ₊₁/aₙ 的极限定 R, applies_when: 需先给出收敛范围, failure_boundary: F3 }
          new_routes_this_scan: [C3]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: C3, name: 一阶递推直接解通项, reason: "2020-17 的 (n+1)aₙ₊₁=(n+1/2)aₙ 是一阶递推，可不经微分方程直接连乘得 aₙ 再凑 (1−x)^{−1/2} 型二项展开。C1 与 C3 是本格并列的两条 route，不是同一条的两个阶段" }
            routing_defects_fixed: []
            new_failure_boundaries: [F13]
            excluded_candidates:
              - { candidate: 重新定义 ODE 求解的 action, reason: duplicate_mechanism, note: "解微分方程的机制归 calc.ode.route-selection；本族以 local_operation 承接，**两族不得对同一题给出竞争路由**（scope_boundary_rule）" }
              - { candidate: 母函数的一般理论, reason: out_of_scope, note: 超数学一大纲 }
              - { candidate: 特征方程法解常系数线性递推, reason: dominated_not_excluded, note: "对 a_{n+2}=p a_{n+1}+q aₙ 型合法且可执行（数列通项法），但真题的递推多含 n 的系数、不是常系数；**不删除**——适用面窄不等于非法" }
              - { candidate: 广义幂级数解（弗罗贝尼乌斯方法）, reason: out_of_scope, note: 超大纲 }
            focused_checks_required_by_protocol:
              index_alignment: "指标平移后落单的首项不得漏（F13）；这是本格最集中的丢分点"
              s_zero_depends_on_start: "S(0) 由求和的起点下标决定；起点不是 0 时 S(0)≠a₀"
              two_directions: "C1（递推→方程）与 C2（方程→递推）方向相反，由题面给的是递推还是方程决定"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "C1 解出的微分方程类型与 calc.ode 族各 action 的对应表未建（backlog；当前以 local_operation 承接，不跨族引用 action_ref）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格；本格三题即 ODE 族 exclusions 中判归本族的「级数-ODE 边界」
            instances:
              - { action_id: C2, ref: 2007-20, basis: "y=Σaₙxⁿ 代入 y″−2xy′−4y=0，比较系数得 aₙ₊₂=2aₙ/(n+1) ⇒ y=x e^{x²}", evidentiary_weight: none }
              - { action_id: C1, ref: 2013-16, basis: "递推 a_{n−2}=n(n−1)aₙ 翻成 S″−S=0，由 S(0)=3、S′(0)=1 定常数 ⇒ 2eˣ+e^{−x}", evidentiary_weight: none }
              - { action_id: C1, ref: 2020-17, basis: "(n+1)aₙ₊₁=(n+1/2)aₙ 翻成 (1−x)S′−S/2=1（首项 a₁=1 不可漏，F13）⇒ 2/√(1−x)−2", evidentiary_weight: none }
              - { action_id: C3, ref: 2020-17, basis: "同题的并列 route：一阶递推可直接连乘得 aₙ 再凑二项展开", evidentiary_weight: none }
              - { action_id: D1, ref: 2020-17, basis: "由 aₙ₊₁/aₙ=(n+1/2)/(n+1)→1 得 R=1", evidentiary_weight: none }
            not_found: []
        fourier:
          cell_id: fourier
          status: complete_within_declared_universe
          global_exhaustiveness: not_established
          scan_kind: structural_completeness
          scanned_at: 2026-08-28
          scope: 傅里叶级数：收敛定理定点值、奇偶延拓、周期平移、系数计算、由收敛定理反解数项级数
          route_universe: >
            对傅里叶级数可执行的操作：① 收敛定理定点值（W1）；
            ② 奇偶延拓（W2）；③ 周期平移到主区间（W3）；④ 系数公式计算（W4）；
            ⑤ 由收敛定理在特定点的等式反解数项级数（W5）。
            凡此格点上数学合法且 scope 内可执行的组合均须落为一个 action 或显式 subroute。
          search_budget: >
            单轮结构穷举（上述五类）+ 对「延拓方向」与「间断点取值」两个已知高危
            步骤各构造一个 failure boundary（F12、F11）。**未做**：第二轮独立重扫。
          stop_rule: 格点全部走完且每格给出「已有 action 覆盖 / 新增 action / 本格 scope 外」三者之一
          existing_routes:
            - { route_id: FR1, action: W1, mechanism: 连续点取 f(x)、间断点取左右平均, applies_when: 问某点的和, failure_boundary: F11 }
            - { route_id: FR2, action: W2, mechanism: 正弦→奇延拓、余弦→偶延拓, applies_when: "f 只定义在 [0,l] 且要求特定类型的级数", failure_boundary: F12 }
            - { route_id: FR3, action: W3, mechanism: 加减周期整数倍平移到主区间, applies_when: 所问的点在定义区间外, failure_boundary: 周期取错（把 2l 当 l） }
            - { route_id: FR4, action: W4, mechanism: 系数公式 + 分部积分, applies_when: 问系数或完整展开式, failure_boundary: a₀ 的公式与 n≥1 不同 }
            - { route_id: FR5, action: W5, mechanism: 在特定点写等式反解数项和, applies_when: 前一问是展开，后一问求数项级数, failure_boundary: 该点为间断点时须取左右平均（F11） }
          new_routes_this_scan: [W3, W5]
          findings:
            missing_from_cell_list: []
            new_routes:
              - { action_id: W3, name: 周期平移, reason: "2013-3 问 S(−9/4)、2025-12 问 S(−7/2)，所问的点都在 [0,1] 之外。若本格只有 W1/W2/W4，这两题在「取值」之前缺一步合法操作" }
              - { action_id: W5, name: 由收敛定理反解数项级数, reason: "2008-19 第二问由 x=0 处的收敛值解出 Σ(−1)ⁿ⁻¹/n²；这条通道同时属 numeric_sum 格（eligible_cells 双挂）" }
            routing_defects_fixed:
              - "草案曾把周期平移写成 W1 的一个 local_operation，导致「延拓后周期是 2l 而非 l」这条易错点无处安放；改为独立 action W3"
            new_failure_boundaries: [F11, F12]
            excluded_candidates:
              - { candidate: 复指数形式的傅里叶级数, reason: out_of_scope, note: 数学一只考三角形式 }
              - { candidate: 傅里叶变换 / 非周期情形, reason: out_of_scope, note: 超大纲 }
              - { candidate: 帕塞瓦尔等式求数项级数和, reason: out_of_scope, note: "帕塞瓦尔等式不在数学一大纲；本族的数项级数反解只走 W5（收敛定理在某点取值）" }
              - { candidate: 逐项积分傅里叶级数, reason: dominated_not_excluded, note: "对收敛的傅里叶级数逐项积分合法，可用于生成新的数项级数等式；真题未出现且书写繁，**不删除**——效率低不等于非法" }
              - { candidate: 先算系数再求点值, reason: dominated_not_excluded, note: "W4→W1 的组合合法且必定给出正确答案，只是把选择/填空做成了大题；**不删除**" }
            focused_checks_required_by_protocol:
              extension_is_determined: "延拓方式由所求级数类型决定，不可自选（F12）"
              discontinuity_average: "间断点取左右极限的平均，不是 f(x₀)（F11）"
              period_is_2l: "[0,l] 上奇/偶延拓后周期为 2l，不是 l"
              no_coefficients_needed: "求点值不需要系数——这是 W1 相对 W4 的全部价值"
          completion_criteria:
            route_universe_declared: true
            reachable_S3_typing_complete: true
            current_routes_enumerated: true
            independent_route_generation_done: true
            legality_guards_checked: true
            necessary_sufficient_heuristic_roles_checked: true
            key_failure_boundaries_checked: true
            counter_witness_search_done: true
            source_mapping_done: true
            open_items_explicit: true
            no_direct_blocker_open: true
          open_items:
            - "狄利克雷条件（分段单调、有限个第一类间断点）在 mechanism 中叙述，未单列为 guard（backlog）"
            - "帕塞瓦尔等式判为超纲而未纳入；若大纲变动需重审（backlog）"
            - "未做第二轮独立重扫（backlog）"
          positive_instance_mapping:
            note: 同 numeric_positive 格
            instances:
              - { action_id: W1, ref: "2013-3,  basis: \"S(−9/4)=S(−1/4)=−S(1/4)=−f(1/4)（奇延拓 + 周期 2）\"", evidentiary_weight: none }
              - { action_id: W1, ref: 2025-12, basis: "S(−7/2)=S(1/2)，x=1/2 为间断点 ⇒ 取左右平均 (0+1/4)/2=1/8", evidentiary_weight: none }
              - { action_id: W2, ref: 2008-19, basis: "展成余弦级数 ⇒ 偶延拓，只算 aₙ", evidentiary_weight: none }
              - { action_id: W2, ref: 2023-13, basis: "余弦级数 ⇒ 偶延拓，周期 2", evidentiary_weight: none }
              - { action_id: W3, ref: "2013-3,  basis: \"−9/4 加周期 2 平移到 −1/4，再用奇性折到 1/4\"", evidentiary_weight: none }
              - { action_id: W3, ref: 2025-12, basis: "−7/2 加周期 4 平移到 1/2（延拓后周期为 2l=4，不是 2）", evidentiary_weight: none }
              - { action_id: W4, ref: 2008-19, basis: "a₀=2−2π²/3，aₙ=−4(−1)ⁿ/n²（两次分部积分）", evidentiary_weight: none }
              - { action_id: W4, ref: 2023-13, basis: "aₙ=2[1−(−1)ⁿ]/(n²π²) ⇒ a_{2n}=0", evidentiary_weight: none }
              - { action_id: W5, ref: 2008-19, basis: "在 x=0 处收敛到 f(0)=1，代入级数式解出 Σ(−1)ⁿ⁻¹/n²=π²/12", evidentiary_weight: none }
            not_found: []
    scan_basis:
      - { source: 解法.md, status: unverified_inference, routes_scanned: 27, note: "从题面推出的解题路径，status: 待核对；只作 route 的可映射性参考，不作正确性依据" }
      - { source: 高数方法速查.md §9, status: human_synthesis, routes_scanned: 11, note: "「逐项求导/积分」的方向判据与 高数逐考点认知模型.md 相反，本族未采纳其写法，见 §5" }
      - { source: 高数逐考点认知模型.md 模块 9, status: human_synthesis, routes_scanned: 11 }
      - { source: 考纲.md §7 无穷级数, status: human_synthesis, note: 用于判定 route 是否超纲（P4、帕塞瓦尔、狄利克雷判别法等的 scope 归属） }
      - { source: independent_route_generation, status: human_synthesis, note: "八格各自先做结构穷举，后做真题映射（第 8 步）；route 不由真题定义" }
      - { source: 考点标注.tsv, status: human_annotation, note: "sha256 前缀 cbdf1a55989a8f60；仅用于 scope 归属与 positive-instance mapping" }
      - { source: 高数真题题面_2004-2023.md, status: human_annotation, note: "source_status 为 ocr_uncertain，evidentiary_weight 一律 none" }
      - { source: papers/2024考研数学一真题+答案.md, status: human_annotation, note: "同上（2024-3）；【答案】段落未用于 route 推断" }
      - { source: papers/2025年数学一真题.md, status: human_annotation, note: "同上（2025-2、2025-12）" }
      - { source: papers/2026年考研数学一真题.md, status: human_annotation, note: "同上（2026-2）" }
    derivation: human_synthesis
    annotation_dependence: material
    constructed_counterexamples: [F1, F2, F3, F4, F5, F6, F8, F9, F10, F11, F12, F13, F14, F16, F17]

  teaching_policy:
    first_encoding_visible: true
    unprompted_selection_hidden: true
    transfer_probe_must_change_decisive_condition: true   # 改一级四轴的任一轴（如 判敛散→求和、正项→交错、正向→逆向），看学生是否整条路由重走
```

## 2. scope 核对：29 题（27 级数 + 2 反常积分）

**v1.1.0 结论：本族 scope 为 29 题。**构成如下。

### 2.1 级数主考点 27 题（v1.0.0 已核，HANDOFF 裁定 SB-3 采纳）

按 `考点标注.tsv`（sha256 前缀 `cbdf1a55989a8f60`）逐行核对**主考点**：

| 主考点 | 题数 | 题号 |
|---|---|---|
| 幂级数的收敛半径与收敛域 | 9 | 2005-16、2008-11、2010-18、2011-2、2012-17、2015-3、2020-4、2021-18、2026-2 |
| 级数敛散性的判定与反例 | 3 | 2006-9、2009-4、2019-3 |
| 傅里叶级数的收敛定理 | 3 | 2013-3、2023-13、2025-12 |
| 函数展开成幂级数 | 3 | 2006-17、2019-11、2024-3 |
| 幂级数系数递推与微分方程 | 3 | 2007-20、2013-16、2020-17 |
| 绝对收敛与条件收敛 | 2 | 2023-4、2025-2 |
| 正项级数敛散性的比较判别法 | 1 | 2004-9 |
| 傅里叶级数的奇偶延拓 | 1 | 2008-19 |
| 数项级数求和 | 1 | 2018-3 |
| 逐项积分求和函数 | 1 | 2017-12 |

合计 9+3+3+3+3+2+1+1+1+1 = **27**。

### 2.2 反常积分 2 题（HANDOFF 裁定 SB-4 新判归本族）

| 主考点 | 题数 | 题号 |
|---|---|---|
| 反常积分的敛散性判别 | 2 | 2010-3、2016-1 |

这是 TSV 中主考点为「反常积分的敛散性判别」的**全部**题目（已逐行核对，无遗漏）。
两题都落入新增的 `improper_integral` 格。

### 2.3 总计与口径

**27 + 2 = 29**，与 `evidence.positive_problem_ids.count` 一致。

⚠️ **HANDOFF 的 SB-4 写「这两题已计入上述 27」与事实不符。**v1.0.0 §2 的 27 题
清单是**排除** 2010-3 与 2016-1 之后算出的（该清单逐题列名，可直接核对，
两题均不在其中）；SB-3 采纳的正是这个 27。因此裁定 SB-4 把两题判归本族之后，
正确总数是 29 而不是 27。`batch2_plan` 现记 `scope_problems: 27`，应改为 29。
本族**未**修改 HANDOFF（那是 integrator 的文件），只在此说明并写入 §5。

仍**不认领**的 5 题（只命中次考点，按 scope_boundary_rule 不得计入）：
2004-18（主：方程实根个数的讨论）、2009-16（主：定积分求平面图形面积）、
**2014-19（主：夹逼准则求极限，归已冻结的 limit 族）**、
2016-19（主：拉格朗日中值定理证明不等式）、2019-17（主：定积分求平面图形面积）。

**scope 题数不驱动 route scan 的覆盖面**：route scan 是结构穷举。本族九格的
route universe 覆盖大纲 §7 无穷级数与反常积分敛散性判别的全部内容，
包括真题中未出现的 P4（积分判别法）、X4（直接法展开）、X3（先展开导数）、
N1（造辅助幂级数）与 I5（反常积分的绝对收敛）——它们以 `not_found` 记录，
不因未出现而排除。

## 3. B3 可表达性核查（schema v1.3.1 对级数真实 route 组合的表达力）

1. **「求 R → 判端点」的强制两步**：D1 的 `followup_actions.mode: sequence`
   携带 `action_ref: D2`，是**强制**继续而非 `optional_any_of`，因此不会出现
   S2 所防的「静默漏解」。端点判定作为独立 action 存在，F1/F2 两个边界各自
   挂在正确的位置 → **可表达**。
2. **「先定收敛域再求和函数」**：D1/D2 同时 `eligible_cells` 到 power_sum，
   R2 双向校验通过 → **可表达**。
3. **「逐项操作的合法性是充分条件」**：guard 的 `logical_role: sufficient`
   与 necessary 的「只在开区间内部成立」是两条独立 guard，schema 允许同一
   机制拆成不同 logical_role 的多条 guard → **可表达**。
4. **「端点的和走另一条通道」**：G4 作为独立 action，其 applies_when 明确
   限定在「D2 已判定该端点收敛」之后，F14 承载定理不覆盖端点这一事实
   → **可表达**。
5. **「四种 effect 的区分」**：15 条 failure_boundary 分别落在
   invalidates(7) / becomes_inconclusive(5) / changes_branch(2) /
   loses_advantage(1)，未出现需要「一条边界同时是两种 effect」的情形
   → **可表达**。
6. **「级数-ODE 边界的跨族衔接」**：C1 解微分方程的机制以 `local_operation`
   承接而非跨族 `action_ref`，避免两族对同一题给出竞争路由；代价是
   calc.ode 的 action 无法被本族直接引用（记入 open_items，属 backlog）
   → **可表达**（schema 未歪曲数学关系，只是跨族引用能力有限）。
7. **「同一 action 属于多个格」**：P0、D1、D2、X5、W5 的 `eligible_cells`
   均为多值，R2 双向一致 → **可表达**。
8. **（v1.1.0）「两类奇点的判据方向相反」**：拆成 I2 与 I3 两个 action 而非一个
   带参数的 action，F16 才有唯一的挂载点；若合并成一条，「方向记反」这个错误
   在 schema 上无法表述为某一条 route 的 failure boundary → 拆分后**可表达**。
9. **（v1.1.0）「多点判定不可并成一次」在两个格上同构**：power_domain 的 D2/F2
   与 improper_integral 的 I1/F17 是同一条结构规则的两个实例。schema 允许两格
   各自持有自己的 action 与 boundary 并在 note 中互相引用，无需引入跨格的
   共享抽象 → **可表达**（是否值得抽象化记入 backlog）。

**结论：未发现 schema v1.3.1 无法表达的级数或反常积分真实 route 组合，不触发 B3 blocker。**

## 4. 状态与交付说明

- 本族 status = **candidate**（作者自主产出上限），v1.1.0 的 scope 扩写**未**改变
  状态；`pedagogical_validation: untested` 保持。
- **九格** route scan 全部按八步协议完成，11 项 completion_criteria 逐格填满；
  `global_exhaustiveness: not_established` 常设，不是缺陷。
- `python3 分析/tests/lint_method_families.py` → **error 0 · warning 4**，
  与基线一致；4 条 warning 全部在已冻结的 extrema（A2/A3/A5/A7 的 S3 typing
  欠账），本族的 S3 warning 为 **0**。
- R1、R2 已对本文件做注入式验证（v1.0.0）：把 D1 的 `action_ref` 改指不存在的
  action 触发 R1；把 D2 的 `eligible_cells` 删掉一个格触发 R2。两者均已还原。
- 四类 direct blocker（B1 / B2 / B3 / B4）本轮均未触发，`open_blockers: []`。
- 按 cross_review_rule，本族**必须由 claude-series 之外的 agent 独立审**；
  v1.1.0 新增的 improper_integral 格只有 2 道真题支撑，建议审查时重点复核。
- 执行过的命令通道 id：CMD-0001、CMD-0002、CMD-0003、CMD-0004、CMD-0005、
  CMD-1001、CMD-1002。

### 4.1 v1.0.0 → v1.1.0 变更清单

| 变更 | 内容 |
|---|---|
| scope | 扩写为「级数与反常积分的敛散性判别」（HANDOFF 裁定 SB-4） |
| 题数 | 27 → **29**（+2010-3、+2016-1） |
| 格 | 8 → **9**（新增 `improper_integral`） |
| actions | 35 → **40**（新增 I1–I5） |
| failure boundaries | 15 → **17**（新增 F16 方向相反 / F17 漏判奇点，均 invalidates） |
| guards | 新增 2 条 necessary（判据方向、逐奇点单独判）+ 1 条 supporting_heuristic（对数因子后置） |
| 一级 router | 轴 `series_type` → `object_type`，增值「反常积分」；acquisition 增第 ⓪ 步 |
| 修复 | v1.0.0 的机械引号处理曾把四条 `axes` 的 `values` 键吞进字符串（YAML 合法但语义错），已改为块状写法并核对解析结果 |

## 5. open_questions（交 integrator 裁定，本族不自行处理）

1. **`batch2_plan` 的 `scope_problems: 27` 应改为 29。**裁定 SB-4 的原文写
   「这两题已计入上述 27」，但 27 是**排除** 2010-3 / 2016-1 算出的
   （v1.0.0 §2 的 27 题清单逐题列名，两题均不在其中），而 SB-3 采纳的正是这个 27。
   两条裁定叠加后的正确题数是 **29**。本族已按 29 更新自己的 artifact，
   **未**修改 HANDOFF。请 integrator 同步 `batch2_plan` 与
   `scope_boundary_rule.measured_by_main_tag` 两处的数字。
2. **TSV 是否需要跟随 SB-4 调整。**2010-3 / 2016-1 在 `考点标注.tsv` 中的主考点
   仍是「反常积分的敛散性判别」，与本族新 scope 名称一致，因此**无需改动**；
   但若下游统计按「主考点字符串 → 族」硬映射，需要在映射表里把该标签指向
   `calc.series.route-selection`。本族未改 TSV，也未改任何下游统计文件。
3. **`高数方法速查.md` §9 的方向判据已确认修复**（现为「n 在分子先积分、
   n 在分母先求导」，与 `高数逐考点认知模型.md` 模块 9 及本族一致），
   v1.0.0 的该条 open_question 已关闭。本族 `revision_history` 中保留了这条更正
   的 lineage，供追溯。
4. **`AGENT_COLLAB_PROMPT.md` 的 §8.4**（CMD-0004 引用）在 d42ea02 已补齐，
   v1.0.0 的该条 open_question 已关闭。
5. **HANDOFF 顶部 `active_batch: calc.method-families.batch1` 与
   `artifact: 分析/方法族-高数-第一批.md` 仍是 batch1 的值**（batch1
   `closed_at: 2026-08-28`，而 `batch2_plan.lifecycle: open`）。v1.0.0 已报过，
   d42ea02 未改。非阻断，但下一个无上下文的 agent 仍会被它误导。
6. **`improper_integral` 格的证据基础薄。**只有 2 道真题，route universe 主要由
   大纲结构而非题目驱动。这符合 route scan 的口径（结构合法性优先于频率），
   但独立审查时应重点复核 I1–I5 的划分是否过细，以及 I5（绝对收敛）是否
   确在数学一 scope 内。分类：`record_to_backlog` / `reopen_family: false`。
