# confirmed_fixed 独立复核任务 · 一元微分学

```yaml
assigned_by: codex（integrator）
assigned_to: 子 agent B（复核方；不得参与修复）
artifact_branch: claude/batch3-parallel-family-audit-ekzi6c
target_file: 分析/方法族-高数-一元微分学.md
source_audit: 分析/审查/claude-audit-batch3-diff1v-a635bd6.md
mode: pure_read_only
files_changed: []
```

不要读取任何修复方报告、修复过程消息或修复分支历史。只从当前目标文件、原始独立审查、
HANDOFF 的规则和 lint 独立判断。

## 三条事故教训

**L-1 改一条结论 ⇒ grep 它在 failure_boundary / local_operation / minimal_probe /
route_scan（route_universe、existing_routes、counter_witness_search、stop_rule）
的全部出现点。**

**L-2 lint 与内容判断冲突时留红并写进报告，不得改内容迁就 lint。**

**L-3 结论必须落在可执行字段（followup 的 local_operation/action_ref、applies_when、
terminal_when）；note/explanation/description 只能复述、不得独家承载。
验收法：遮住所有 note，只看可执行字段，看 route 能不能走通。**

integrator 统一裁定：`minimal_probe` 不算可执行字段，也要从 L-3 自检中遮去。
路线只能依赖 `level_2_candidates + applies_when + followup_actions + terminal_when`。

## 必审项目

逐条给出 `confirmed_fixed / partially_fixed / not_fixed`，不得由 integrator 代判：

1. BL-1：2004-1 是否有显式切线 cell，且可执行字段能完成“几何条件→斜率→反求切点→点斜式”。
2. BL-2：2014-2 是否有区间凸性到弦/切线整体位置的正式 action，并防单点符号外推。
3. BL-3：2023-3 在 x′(0) 不存在时，能否分段消参、保留分支范围并完成一/二阶正则性判断。
4. BL-5：2018-1 或 2019-2 的单侧/绝对值/取整差商是否由本族可执行字段自理，且未动 frozen limit 族。
5. regression：原合法 routes/excluded candidates 是否无不当消失；R2 双向一致；status/teaching_use/doc_version/cell status 未被修复方改动；lint 基线是否保持。
6. L-1 全局同步与“遮 note + minimal_probe”自检。
7. 对新发现的问题区分 direct blocker 与 non-blocking，并给出**可直接落地的处方**；若不通过，明确哪一字段怎样改。

另做一个独立只读附审：检查 `分析/方法族-高数-中值定理与证明.md` A5 自递归的
`when: 且存在有限阶 n 使 F^{(n)} 可定号` 是否把事后存在性断言当作执行入口。
只给判断与处方，不改文件；该附审不得影响 diff1v 的 confirmed_fixed 结论。

## 输出

在最终回复给出完整复核报告正文，首部必须含 reviewer 身份、artifact HEAD、lint、
`files_changed: []`、未读 solutions/与 2024–2026 papers、逐 blocker verdict、status recommendation、
confidence limits。不得创建或修改任何文件。
