# 协作看板

**跨机器、跨模型、跨对话的唯一总线。**

Claude Code 的 `ListAgents` / `SendMessage` 只能在**同一台机器**上互通。
远程会话（云端容器）与本机会话之间没有直连，所以协调必须走这份文件。

## 怎么用

**每个 agent 开工前先读这里，收工后更新自己那一行。**

- 一个 agent 只改**自己那一行**，不要改别人的。
- **分支名以你实际所在的分支为准**（`git rev-parse --abbrev-ref HEAD`）。
  云端会话的分支由 harness 分配，与派工时预写的名字不同**不算 inconsistency**，
  直接把实际名字写回你那一行即可。
- 已交付合入的行**保留不删**——它记录了「这块是谁在哪个分支上做的」，
  出问题时要靠它回溯。
- 改完连同你的产出一起 commit。改看板不单独开 commit。
- 冲突了就以后 push 的为准，把被覆盖的那行原样补回去——这份文件不值得为它解冲突。
- **它不是聊天记录。**只写「现在是什么状态」，不写过程。要留讨论痕迹就写进
  `分析/审查/` 下的报告文件。

## 当前状态

```yaml
board_version: 1
updated_at: 2026-08-28   # codex 接手 deepseek 的工作

agents:
  - name: claude-code-remote
    machine: 云端容器
    branch: claude/postgraduate-math-exam-analysis-czoi3t
    role: integrator
    doing: 已合入级数族与 codex 审查报告；已裁定 SB-1..SB-4；ODE 族已降 challenged
    blocked_on: null
    waiting_for: null
    last_output: "级数族 v1.0.0（已合入）· ODE 族 v1.0.1 challenged+quarantine"

  - name: codex-multivar
    machine: 本机
    branch: codex/multivar-family
    role: primary_executor
    doing: 可开工——SB-1/SB-2 已裁定
    blocked_on: null
    waiting_for: null
    last_output: null

  # ── 已交付并合入 ──
  - name: codex-audit-ode
    machine: 本机
    branch: codex/audit-ode-240c1dc
    role: independent_reviewer
    doing: 已交付并合入（4 blocker，BL-4 经 integrator 独立复核成立 → ODE 降 challenged）
    blocked_on: null
    waiting_for: null
    last_output: 分析/审查/Codex-audit-ode-240c1dc.md

  - name: claude-series
    machine: 云端
    branch: claude/series-family-construction-m8mwoo
    role: primary_executor
    doing: 已交付 v1.1.0（依裁定 SB-4 扩写为「级数与反常积分」，29 题 / 9 格 / 40 actions / 17 边界）
    blocked_on: null
    waiting_for: "integrator 同步题数：batch2_plan 与 scope_boundary_rule.measured_by_main_tag
      现记 calc.series.route-selection: 27，应为 **29**。裁定 SB-4 原文写「这两题已计入上述 27」
      与事实不符——27 是排除 2010-3 / 2016-1 算出的（v1.0.0 §2 清单逐题列名，两题均不在其中），
      而 SB-3 采纳的正是该 27，故叠加后为 29。详见 分析/方法族-高数-级数.md §2.3 与 §5 第 1 条。
      本族已按 29 更新自身 artifact，未改 HANDOFF。"
    last_output: 分析/方法族-高数-级数.md（v1.1.0）
    executed_command_ids: [CMD-0001, CMD-0002, CMD-0003, CMD-0004, CMD-0005, CMD-1001, CMD-1002]
    needs_independent_review: true   # cross_review_rule：不得由 claude-series 自审；建议重点复核 improper_integral 格（仅 2 题支撑）

  - name: deepseek-ode-family
    machine: 本机
    branch: deepseek/ode-family
    role: primary_executor
    doing: 已交付并合入；其产出经独立审查后降为 challenged
    blocked_on: null
    waiting_for: null
    last_output: 分析/方法族-高数-微分方程.md
```

## 需要对方回应时

写进自己那一行的 `waiting_for`，**并说清楚在哪个文件的哪一节**：

```yaml
waiting_for: "claude-code-remote 判定 A6p 的拆分是否过度切分（见
  分析/审查/DeepSeek-audit-batch1-<head>.md §findings.blockers 第 2 条）"
```

不要写「等 Claude 回复」——没人知道你在等什么。

## 发现 blocker 时

不要只写在看板上。按 `METHOD_FAMILY_HANDOFF.md` 的四类定义
（B1 漏解 / B2 direct counter-witness / B3 schema 表达力 / B4 语义级状态错误）
写进审查报告，看板这里只放一行指针。

看板会被人扫一眼，报告才会被认真读。
