# confirmed_fixed 独立复核任务 · 重积分

```yaml
assigned_by: codex（integrator）
assigned_to: 子 agent B（复核方；不得参与修复）
artifact_branch: claude/batch3-parallel-family-audit-ekzi6c
target_file: 分析/方法族-高数-重积分.md
source_audit: 分析/审查/claude-audit-batch3-multiple-integral-a635bd6.md
mode: pure_read_only
files_changed: []
```

不要读取任何修复方报告、修复过程消息、修复分支或提交历史。只从当前目标文件、原始独立审查、
HANDOFF 的规则和 lint 独立判断。不得读取 `solutions/`；不得打开 2024–2026 的 papers 题面。

## 三条事故教训

**L-1 改一条结论 ⇒ grep 它在 failure_boundary / local_operation / minimal_probe /
route_scan（route_universe、existing_routes、counter_witness_search、stop_rule）
的全部出现点。**

**L-2 lint 与内容判断冲突时留红并写进报告，不得改内容迁就 lint。**

**L-3 结论必须落在可执行字段（followup 的 local_operation/action_ref、applies_when、
terminal_when）；note/explanation/description 只能复述、不得独家承载。
验收法：遮住所有 note，只看可执行字段，看 route 能不能走通。**

integrator 统一裁定：`minimal_probe` 不算可执行字段，也要从 L-3 自检中遮去。
路线只能依赖 `level_2_candidates + applies_when + followup_actions + terminal_when`。

## 必审项目

逐条给出 `confirmed_fixed / partially_fixed / not_fixed`，不得由 integrator 代判：

1. BL-1：2006-8/2015-4 的表示互化、2009-2 的跨区域比较，是否有正式 action、分格入口和
   可执行终止条件；比较路线是否必须完成共同序关系而不只各块定号。
2. BL-2：2013-15 的换序是否可从“二重·求值”格进入 A7，并在换序后实际算到 terminal；
   换序条件是否以同一区域集合为中介，画图只作 supporting heuristic。
3. BL-4：A3 奇偶/轮换化简后，若关系尚未确定所求值，是否按真实计算路线进入 A1/A2/A4/A5/A6；
   A5/A6 是否自足实算并终止，A6 是否不再伪装成 A4 投影法。
4. **BL-3 必须独立重核**：此前仅由 integrator 半修、从未经过独立 confirmed_fixed。
   遮去 note 与 minimal_probe 后，2015-12 无奇偶性时能否由 A3 轮换入口进入，关系不足时
   继续 A4→A1 实算，而不是误终止或被 `applies_when` 拦下。
5. 顺带项：positive mapping、换序 route universe、B4 witness、A6、B1 一般交换映射命题。
   2024–2026 mapping 只核已有审计/TSV 证据，不得开 papers。
6. regression：原合法 routes/excluded candidates 无不当消失；R2 双向一致；
   status/teaching_use/doc_version/status_history 未被修复方改动；lint 基线保持。
7. L-1 全局同步与“遮 note + minimal_probe”自检。
8. 新问题须区分 direct blocker / non-blocking 并给字段级可落地处方。

## 输出

最终回复给出完整复核报告正文，首部必须含 reviewer、artifact HEAD、lint、
`files_changed: []`、未读 solutions/与 2024–2026 papers、逐 blocker verdict、status recommendation、
confidence limits。不得创建或修改任何文件。
