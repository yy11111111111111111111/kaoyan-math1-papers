# 派工单 · 子 agent A · 重积分族路线补全

```yaml
assigned_by: codex（integrator）
assigned_to: 子 agent A（修复方）
base_branch: claude/batch3-parallel-family-audit-ekzi6c
branch: 自开 agent-a/multiple-integral-route-completion
target_file: 分析/方法族-高数-重积分.md
required_reading: 分析/审查/claude-audit-batch3-multiple-integral-a635bd6.md
baseline_lint: error 0 · warning 4
```

## 三条事故教训（必须逐条执行）

**L-1 改一条结论 ⇒ grep 它在 failure_boundary / local_operation / minimal_probe /
route_scan（route_universe、existing_routes、counter_witness_search、stop_rule）
的全部出现点。**

**L-2 lint 与内容判断冲突时留红并写进报告，不得改内容迁就 lint。**

**L-3 结论必须落在可执行字段（followup 的 local_operation/action_ref、applies_when、
terminal_when）；note/explanation/description 只能复述、不得独家承载。
验收法：遮住所有 note，只看可执行字段，看 route 能不能走通。**

integrator 统一裁定：`minimal_probe` 不算 L-3 的可执行字段，不能用它补足 action 的
入口、步骤或终止条件。遮去 note 与 minimal_probe 后，路线仍须仅凭
`level_2_candidates + applies_when + followup_actions + terminal_when` 走通。

## 三条 blocker

### BL-1 · 一级设问轴缺表示互化与比较大小

扩展 router/cell，使两类设问有真实入口：

- 直角坐标与极坐标表示互化：载体 2006-8、2015-4。正式 action 必须能从区域不等式/边界
  确定极坐标范围，写 `dA=r dr dθ`，并在“不求值、只改写表示”时直接 terminal。
- 跨区域积分比较：载体 2009-2。正式 action 必须逐块利用区域对称、被积奇偶与符号判出
  各 I_k 的零/正/负，再执行跨块比较并 terminal；不能停在“各块定号”。

同步 level-1 values、level-2 cells/actions、eligible_cells、route scan 全套字段。

### BL-2 · 换序作为求值手段在二重求值格不可达

让 A7（或等价新 action）在“二重 · 求值”格成为合法候选，并在可执行字段写清：
原次序内层无初等原函数/难积时，先把累次积分还原为区域集合，重新确定另一积分次序，
再按新次序求值并 terminal。载体 2013-15。同步 guard#9、fallback、route universe、
existing route、counter witness、stop rule 与 R2 双向声明。

### BL-4 · A3 在三重格无实算出口

A3 对称性化简后必须按问题类型有真实后继：二重转 A1/A2 等相应求值 action；
三重直角/柱面/球面分别转 A4/A5/A6（以现有 action 的真实语义为准）。
不要用一个对所有分支强制执行的 `all_of`；用带 `when` 的合法结构表达互斥出口。
载体 2009-12、2010-12、2019-19；A9 强制进入 A3 的路径也须一并走通。

## 必须一并独立修核的既有 BL-3

此前 integrator 只改了 guard#1；A3 的 `applies_when` 后来已补为可接受轮换对称，
但从未独立复核。本轮修复方先做 L-3 自检并在报告单列：对 2015-12 的
`x+2y+3z`（无奇偶性），只凭 A3 的 executable fields 是否能进入轮换分支并继续求值。
不得为了让它通过而删除合法分支或改写题意。

## 顺带项

处理 NB-1、NB-3、NB-4、NB-5、NB-6 与 B1 加强结论：重做 positive mapping；
把“必须画图”降为 supporting heuristic、必要条件改成“以区域集合为中介重新定限”；
删掉 order_exchange 的唯一性断言；修正 B4 变量指认；A6 的后继不得把球坐标定限误称投影法；
B1 写回一般交换映射 `σ(x,y)=(y,x)` 下的自检失效机制。新增 boundary 时同步 evidence 索引。

## 硬约束与交付

- 只改目标族文件，另写 `分析/审查/agent-a-multiple-integral-route-completion-<base-head>.md`；不改看板。
- 不改任何 status、teaching_use、cell status、doc_version；维持 challenged + quarantine。
- 禁读 `solutions/`；禁止读取 2024–2026 的 papers（尤其 2024-17 题干为空，不得拿答案段补题）。
- 每新增一条 route 后跑 lint；R2 真不一致自行修，判断为 lint 误报则留红并报告。
- 报告逐条给出载体题完整可执行路径、BL-3 独立自检、基线/逐 route/最终 lint、
  保留红项、实际改动文件、`solutions_read: false`、`papers_2024_2026_read: false`。
- 提交到自己的分支，把 commit 返回 integrator；不要推 integrator 分支。
