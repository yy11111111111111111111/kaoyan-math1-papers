# confirmed_fixed 独立复核任务 · 一元积分学

```yaml
assigned_by: codex（integrator）
assigned_to: 子 agent B（复核方；不得参与修复）
artifact_branch: claude/batch3-parallel-family-audit-ekzi6c
target_file: 分析/方法族-高数-一元积分学.md
source_audit: 分析/审查/claude-audit-batch3-int1v-a635bd6.md
mode: pure_read_only
files_changed: []
```

不要读取任何修复方报告、修复过程消息、修复分支或提交历史。只从当前目标文件、原始独立审查、
HANDOFF 的规则和 lint 独立判断。不得读取 `solutions/`；不得打开 2024–2026 的 papers 题面。

## 三条事故教训

**L-1 改一条结论 ⇒ grep 它在 failure_boundary / local_operation / minimal_probe /
route_scan（route_universe、existing_routes、counter_witness_search、stop_rule）
的全部出现点。**

**L-2 lint 与内容判断冲突时留红并写进报告，不得改内容迁就 lint。**

**L-3 结论必须落在可执行字段（followup 的 local_operation/action_ref、applies_when、
terminal_when）；note/explanation/description 只能复述、不得独家承载。
验收法：遮住所有 note，只看可执行字段，看 route 能不能走通。**

integrator 统一裁定：`minimal_probe` 不算可执行字段，也要从 L-3 自检中遮去。
路线只能依赖 `level_2_candidates + applies_when + followup_actions + terminal_when`。

## 必审项目

逐条给出 `confirmed_fixed / partially_fixed / not_fixed`，不得由 integrator 代判：

1. BLK-1：2007-3、2017-4、2009-3 是否有“由图形/几何意义直接读积分”的正式 action；
   遮去 note 与 minimal_probe 后，能否从分格、applies_when、followup、terminal 走完。
2. BLK-2：2016-2 是否有分段/绝对值拆区间、各段积分保留独立常数、由整体原函数可导推出
   拼接连续并确定常数的完整可执行路线。
3. BLK-4：positive_instance_mapping 是否已清除原审计证伪的错引；2004–2023 的新引用可按
   允许题面独立抽查，2024–2026 只能使用仓库内已有审计/派工证据，不得开 papers。
4. BLK-6：2008-18 与 2026-20 是否留在 int1v 的主考点 scope，且分别有非循环的
   变限积分基本定理证明路线、以及完整 transformed_problem 才可移交 mvt 的路线；不得自行改 scope。
5. 顺带项：A7 单调性、A2 定积分换元条件、周期原函数充要条件、retired rule 限定、
   帕普斯-古尔丁 out_of_scope 是否与全局语义一致。
6. regression：原合法 routes/excluded candidates 是否无不当消失；R2 双向一致；
   status/teaching_use/doc_version/status_history 未被修复方改动；lint 基线是否保持。
7. L-1 全局同步与“遮 note + minimal_probe”自检。
8. 对新发现的问题区分 direct blocker 与 non-blocking，并给出**可直接落地的处方**；
   若不通过，明确哪一字段怎样改。

## 输出

在最终回复给出完整复核报告正文，首部必须含 reviewer 身份、artifact HEAD、lint、
`files_changed: []`、未读 solutions/与 2024–2026 papers、逐 blocker verdict、status recommendation、
confidence limits。不得创建或修改任何文件。
