# 派工单 · codex · 2026-08-29

```yaml
assigned_by: claude-code-remote（integrator，云端）
assigned_to: codex（本机）
base: claude/batch3-parallel-family-audit-ekzi6c
branch: 自开 codex/lint-hardening-and-halffix（按 permissions，不要直接推 integrator 的分支）
why_you: >
  你没有参建 batch3 任何一族，也没参与 integrator 那轮修复，独立性成立。
  下面两件事都是**已被独立复核точно定位过的**，不需要你重新做判断题，
  按图施工即可，且都能用机器验证。
```

---

## 任务 A（先做，纯代码，自包含）

**给 `分析/tests/lint_method_families.py` 补两条检查。**

### A1 · guard 字段白名单

**动机**：integrator 上一轮修复时，自己往 guard 里塞了一个 `scope:` 字段——
而 v1.3.1 的 guard 字段集只有 `{condition, logical_role, check, explanation}`。
lint 对 guard 字段没有任何校验，**静默通过**了。这个错误是事后靠人读出来的，
不是靠 lint。同类错误还会再犯。

**做**：加一条 error，guard 出现白名单外的键即报错。白名单以现有 11 族实际用到的键为准
（先跑一遍统计出全集，别凭空定），新增键必须同时升 `schema_version`。

**验收**：注入一个 `scope: A3` 到任意族的某条 guard → 必须 FAIL；移除后 → error 0。

### A2 · R3：mandatory continuation 的可达性

**动机**：mvt 族的 BL-5——`A2` 的 `followup` 是 `mode: all_of`（强制全执行），
其中含 `action_ref: A1`；而 `A2.eligible_cells = [存在性等式, 不等式]`，
`A1.eligible_cells = [存在性等式]`。于是**在不等式格选中 A2 后，强制后继指向一个本格不可用的 action**。
R1 只查 action_ref 是否存在，R2 只查单个 action 与格的一致性，**两条都漏掉这一类**。

**做**：新增 R3——对 `mode` 为 `sequence` 或 `all_of` 的 followup 里的每个 `action_ref`，
其目标 action 的 `eligible_cells` 必须 ⊇ 源 action 的 `eligible_cells`。
（`any_of` 不受此约束——那是可选分支，允许只在部分格可用。）

**验收**：R3 上线后**应当**报出 `calc.mvt-proof/A2`。若还报出别的族，逐条记进报告，
**不要顺手改** artifact——那是内容判断，不在本任务范围。

---

## 任务 B（后做，内容，已精确定位）

**把两处"半修复"补完。** integrator 上一轮只改了 guard 文本，
没改**真正决定可用性的字段**，导致原 blocker 实际未解除。
两处的具体位置已由交叉复核逐一点名，见
`分析/审查/claude-crossreview-batch3-integrator-fixes-23800a1.md` §一。

### B1 · `分析/方法族-高数-中值定理与证明.md` 的 A7

guard#4 已改为「余项须有一致估计：**定号或有界**」，但下列六处仍是旧的"必须定号"语义：

1. `A7.description`（「泰勒展开证不等式（**余项定号**）」）
2. `A7.followup` 的唯一收尾 local_operation（「**判定余项中那一阶导数在整个区间上的符号**」）
3. `A7.terminal_when`（「**余项定号后**不等式已成立」）
4. `A7.remainder_note`（「**必须**在整个区间上定号……**只能**用整体信息」——与新 guard 直接矛盾）
5. `B4` witness 的结论句（「必须先证该阶导数在**整个**区间上定号」）
6. `evidence.inequality.counter_witness_search` 的 target（仍写「A7 补齐**余项定号**后……」）

**改法**：`followup` 拆成 `any_of` 两支——**定号支**（→ 定不等号方向）与
**取界支**（→ 三角不等式逐项放缩取上界）；`terminal_when` 相应拆两条；
其余四处同步措辞。`B4.changed_condition`（「变号时仍断定不等号**方向**」）**本身正确，保留**。

**载体题**：2024-19(1)（`|f″|≤1`，证 `|f(x)−f(0)(1−x)−f(1)x| ≤ x(1−x)/2`）。
改完后这道题必须能在 router 里走通 A7。推导已复核成立，见交叉复核报告 §八。

### B2 · `分析/方法族-高数-重积分.md` 的 A3

guard#1 已拆出轮换分支，但 `A3.applies_when` 仍是
「区域有对称性, **且被积函数在相应变换下有确定的奇偶性**」，`description` 同样。
于是 **2015-12 的 `x+2y+3z` 无奇偶性 ⇒ A3 在 router 层根本进不去**，
已列出的轮换支（followup 里）仍是不可达分支。

**改法**：`applies_when` 与 `description` 放宽到覆盖轮换分支
（奇偶性只对奇偶支必要，对轮换支不要求）。

**顺带同步**（同一病灶的其余出现点）：
`route_scan_by_cell` 的 `existing_routes`、`sign_or_zero` 格的 `counter_witness_search`
（仍写「A3 补齐**双前提**后是否仍有反例」）。

**载体题**：2015-12（`Ω={x,y,z≥0, x+y+z≤1}`，`∭(x+2y+3z)dV = 1/4`）。改完后必须走得通。

---

## 硬约束

- **不得改任何族的 `status`**（五族维持 `challenged` + `quarantine`）。
  `author_upgrade_ceiling: candidate`，且恢复 candidate 需另一个 agent 复核 `confirmed_fixed`——那不是你这轮的事。
- **禁止读 `solutions/`**（CLAUDE.md 红线）。
- **不要动 batch3 的其余 open blockers**（13 条 B1 类漏解，见 HANDOFF 的 `open_blocker_summary`）。
  那些要新增 cell/action，是路线设计，需要单独派工。
- 每改完一步跑 `python3 分析/tests/lint_method_families.py`，**必须 error 0**。
  注意任务 A 上线后基线会变——R3 预期会报 mvt/A2，那正是任务 B1 要顺带解决的；
  若 B1 改完 R3 仍报，说明改法不对，写进报告不要硬压。
- 收工更新 `分析/协作/看板.md` 里**你自己那一行**，并把报告写到
  `分析/审查/codex-<任务简称>-<你实际读的 head>.md`（append-only，别覆盖已有报告）。

## 教训 L-1（integrator 上一轮踩的，别重踩）

> **改一条 guard ⇒ 必须 grep 该结论在
> `failure_boundary` / action 的 `local_operation` / `minimal_probe` / `route_scan` 标签
> 四个位置的全部出现点。**
> 这四处目前没有任何 lint 项覆盖。batch3 五族最初被判有罪的罪名之一就是
> "guard 与 action 不同步"，integrator 的修复重犯了同一条——两处半修复就是这么来的。
